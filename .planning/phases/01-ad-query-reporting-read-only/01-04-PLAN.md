---
phase: 01-ad-query-reporting-read-only
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-03
files_modified:
  - Public/Format-AdmanReport.ps1
  - Public/Export-AdmanReportCsv.ps1
  - Public/Export-AdmanReportHtml.ps1
  - Public/Get-AdmanInventoryReport.ps1
  - Public/Start-Adman.ps1
  - tests/Report.Inventory.Tests.ps1
  - tests/Render.Tests.ps1
  - tests/Menu.Tests.ps1
  - adman.psd1
autonomous: true
requirements:
  - RPT-01
  - RPT-02
  - RPT-03
  - RPT-06
must_haves:
  truths:
    - Format-AdmanReport renders a PSCustomObject[] as a console table and degrades to the same table if a grid picker fails.
    - Export-AdmanReportCsv writes UTF8 CSV with -NoTypeInformation.
    - Export-AdmanReportHtml writes a self-contained single-file HTML report using ConvertTo-Html -Head with embedded CSS; no external CSS, JS, or PS6+ parameters.
    - Get-AdmanInventoryReport returns computer objects with OS version and basic AD attributes.
  artifacts:
    - Public/Format-AdmanReport.ps1
    - Public/Export-AdmanReportCsv.ps1
    - Public/Export-AdmanReportHtml.ps1
    - Public/Get-AdmanInventoryReport.ps1
    - tests/Report.Inventory.Tests.ps1
    - tests/Render.Tests.ps1
  key_links:
    - Any report verb -> PSCustomObject[] -> Format-AdmanReport / Export-AdmanReportCsv / Export-AdmanReportHtml.
    - Capability probe -> grid picker availability -> silent fallback to console table.
    - Start-Adman -> Public verb -> PSCustomObject[] -> output-format prompt -> renderer dispatch.
---

<objective>
Implement the output layer: three pure renderers that consume the D-03 schema, plus the OS/inventory report verb. This plan delivers RPT-01 (console + optional grid), RPT-02 (CSV), RPT-03 (self-contained HTML), and RPT-06 (inventory).

Purpose: Report verbs return raw data; renderers decide how to present it. Keeping them separate preserves MENU-04 (one code path, two speeds) and keeps the module dependency-free.
Output: Working renderers and inventory report with unit tests proving 5.1/7 parity.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ad-query-reporting-read-only/01-CONTEXT.md
@.planning/phases/01-ad-query-reporting-read-only/01-RESEARCH.md
@.planning/phases/01-ad-query-reporting-read-only/01-UI-SPEC.md
@.planning/phases/01-ad-query-reporting-read-only/01-VALIDATION.md
@.planning/phases/00-foundation-safety-harness/00-PATTERNS.md
@Public/Test-AdmanCapability.ps1
@tests/Mocks/ActiveDirectory.psm1
@adman.psd1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement console and CSV renderers</name>
  <files>Public/Format-AdmanReport.ps1, Public/Export-AdmanReportCsv.ps1, tests/Render.Tests.ps1</files>
  <read_first>
    - 01-UI-SPEC.md §Renderer behavior, §HTML Embedded CSS Contract
    - 01-CONTEXT.md D-04
    - 01-RESEARCH.md §Pattern 4
  </read_first>
  <action>
    Create Public/Format-AdmanReport.ps1 for RPT-01:
    - Accept [Parameter(Mandatory,ValueFromPipeline)]$InputObject and an optional [switch]$UseGridView.
    - Collect pipeline input into a list.
    - If -UseGridView is present, probe capability: on Desktop edition with interactive session and Get-Command Out-GridView available, call Out-GridView -PassThru:$false inside try/catch; on Core edition with Get-Module -ListAvailable Microsoft.PowerShell.ConsoleGuiTools, call Out-ConsoleGridView inside try/catch. On any failure, degrade to Format-Table -AutoSize | Out-String -Width 4096.
    - Default (no -UseGridView) emits Format-Table -AutoSize | Out-String -Width 4096.
    - Do not write files; emit strings to the pipeline/host.
    Create Public/Export-AdmanReportCsv.ps1 for RPT-02:
    - Accept [Parameter(Mandatory,ValueFromPipeline)]$InputObject and [Parameter(Mandatory)]$Path.
    - Collect pipeline input and call Export-Csv -Path $Path -NoTypeInformation -Encoding UTF8.
    - Validate the parent directory exists before exporting; throw a clear error if not.
    Create tests/Render.Tests.ps1 covering console table output, CSV encoding/NoTypeInformation, grid-view fallback on failure, and PS 5.1/7 parity.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Render.Tests.ps1 -Output Detailed</automated>
  </verify>
  <acceptance_criteria>
    - Format-AdmanReport emits a string containing column headers and row values.
    - Export-AdmanReportCsv creates a file with no #TYPE line and UTF8 encoding.
    - Grid-view failure degrades to console table without throwing.
    - Render tests pass.
  </acceptance_criteria>
  <done>Console and CSV renderers are implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Implement self-contained HTML renderer</name>
  <files>Public/Export-AdmanReportHtml.ps1, tests/Render.Tests.ps1</files>
  <read_first>
    - 01-UI-SPEC.md §HTML Embedded CSS Contract
    - 01-CONTEXT.md D-04
    - 01-RESEARCH.md §Pattern 4, Pitfall 6
  </read_first>
  <action>
    Create Public/Export-AdmanReportHtml.ps1 for RPT-03:
    - Accept [Parameter(Mandatory,ValueFromPipeline)]$InputObject, [Parameter(Mandatory)]$Path, and an optional [string]$Title = 'adman report'.
    - Collect pipeline input into a list.
    - Use a single here-string CSS fragment passed to ConvertTo-Html -Head $css -Title $Title. Embed the CSS from the UI-SPEC contract (body, h1, .metadata, table, th, td, zebra striping, .true/.false classes).
    - Pipe the list to ConvertTo-Html -Head $css -Title $Title, then Out-File -FilePath $Path -Encoding UTF8.
    - Do NOT use -CssUri, -Charset, -Meta, or -Transitional (PS6+ only).
    - Validate the parent directory exists before writing.
    Update tests/Render.Tests.ps1 to assert the output file contains <style>...</style>, contains no <link rel="stylesheet"> tag, and contains the report title.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Render.Tests.ps1 -Output Detailed</automated>
  </verify>
  <acceptance_criteria>
    - Export-AdmanReportHtml writes a single .html file containing embedded CSS.
    - The file contains no external stylesheet link.
    - ConvertTo-Html is invoked with only 5.1-safe parameters (-Head, -Title).
    - HTML tests pass.
  </acceptance_criteria>
  <done>HTML renderer writes a self-contained file and passes tests.</done>
</task>

<task type="auto">
  <name>Task 3: Implement inventory report, wire renderers to menu, and update manifest</name>
  <files>Public/Get-AdmanInventoryReport.ps1, Public/Start-Adman.ps1, tests/Report.Inventory.Tests.ps1, tests/Menu.Tests.ps1, adman.psd1</files>
  <read_first>
    - Public/Start-Adman.ps1
    - Private/Reporting/ConvertTo-AdmanResult.ps1
    - tests/Mocks/ActiveDirectory.psm1
    - 01-CONTEXT.md D-02
    - 01-RESEARCH.md §Pattern 3 inventory
  </read_first>
  <action>
    Create Public/Get-AdmanInventoryReport.ps1 for RPT-06:
    - Loop ManagedOUs and call Get-ADComputer -Filter * with the exact D-02 computer Properties list plus OperatingSystem, OperatingSystemVersion, OperatingSystemServicePack, IPv4Address, DNSHostName, -SearchBase $root, -SearchScope Subtree, -ResultPageSize 1000, -Server $script:Config.DC.
    - Map each result through ConvertTo-AdmanResult -ObjectType Computer and pass through Test-AdmanInManagedScope.
    - Add a Bucket column with value 'Inventory'.
    Modify Public/Start-Adman.ps1 so renderer dispatch is separated from parameter prompting per D-04:
    - Keep Private/Menu/Read-AdmanActionParams focused on verb parameters only; do NOT add output-format choices to that helper.
    - After the selected Public verb returns its PSCustomObject[], present an inline output-format prompt (1=console, 2=CSV, 3=HTML, 4=grid if available, B=back, Q=quit).
    - For CSV/HTML, prompt for the output path and validate the parent directory exists before invoking the renderer; on invalid path, re-prompt once then treat a second failure as 'B'.
    - Dispatch to Format-AdmanReport / Export-AdmanReportCsv / Export-AdmanReportHtml with the verb's output.
    - Honor 'B' to return to the top-level menu and 'Q' to exit Start-Adman from the output-format prompt.
    Update tests/Menu.Tests.ps1 (created in 01-01) to mock the new output-format/path prompts and assert that B returns to the top-level menu and Q exits from the output-format prompt.
    Update adman.psd1 FunctionsToExport to add Format-AdmanReport, Export-AdmanReportCsv, Export-AdmanReportHtml, and Get-AdmanInventoryReport.
    Create tests/Report.Inventory.Tests.ps1 covering OS version presence, IPv4Address/DNSHostName, scope, and ResultPageSize 1000.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Report.Inventory.Tests.ps1,tests/Render.Tests.ps1,tests/Menu.Tests.ps1 -Output Detailed</automated>
  </verify>
  <acceptance_criteria>
    - Get-AdmanInventoryReport is exported and returns computer objects with OperatingSystem, OperatingSystemVersion, IPv4Address, and DNSHostName.
    - Read-AdmanActionParams remains limited to verb parameters; output-format and path prompting live in Start-Adman and run after the verb returns.
    - Start-Adman dispatches output-format choices to the correct renderer and prompts for CSV/HTML paths.
    - tests/Menu.Tests.ps1 covers B/Q navigation from the output-format prompt.
    - adman.psd1 FunctionsToExport lists all new renderers and Get-AdmanInventoryReport.
    - Inventory, render, and menu tests pass.
  </acceptance_criteria>
  <done>Inventory report exists, renderers are wired to the menu, and the manifest is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Report data -> renderer | D-03 objects only; no raw AD types. |
| Renderer -> filesystem | CSV/HTML files are written to operator-specified paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | CSV/HTML file written to unexpected path | low | mitigate | Validate parent directory exists; do not create parent directories automatically (fail explicitly so the operator notices the path). |
| T-04-02 | Information Disclosure | HTML report leaks raw AD attributes | low | mitigate | Renderers consume the D-03 schema only; no raw AD object properties are rendered. |
| T-04-03 | Tampering | HTML contains executable JavaScript | low | mitigate | Use ConvertTo-Html -Head with a static CSS fragment; no JavaScript, no external resources (D-04). |
| T-04-04 | Denial of Service | Large result set exhausts memory in pipeline collector | low | accept | Phase 1 targets typical IT team OUs; bulk/streaming optimization deferred. |
| T-04-SC | Tampering | Package installs | high | accept | No packages introduced this phase. |
</threat_model>

<verification>
- Run Invoke-Pester -Path tests/Report.Inventory.Tests.ps1,tests/Render.Tests.ps1,tests/Menu.Tests.ps1 and confirm 0 failures.
- Run Invoke-ScriptAnalyzer -Path Public/Format-AdmanReport.ps1,Public/Export-AdmanReportCsv.ps1,Public/Export-AdmanReportHtml.ps1,Public/Get-AdmanInventoryReport.ps1 -Settings PSScriptAnalyzerSettings.psd1 and confirm clean.
- Open a generated HTML file and confirm it is a single self-contained file with embedded style and no external link.
- Confirm adman.psd1 FunctionsToExport contains all Phase 1 public verbs.
</verification>

<success_criteria>
- Format-AdmanReport, Export-AdmanReportCsv, Export-AdmanReportHtml, and Get-AdmanInventoryReport are exported.
- Console, CSV, and HTML renderers pass unit tests and use only 5.1-safe ConvertTo-Html parameters.
- Start-Adman routes output-format choices to the correct renderer.
- Inventory report includes OS version and computer attributes.
</success_criteria>

<output>
Create `.planning/phases/01-ad-query-reporting-read-only/01-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

This plan creates or modifies the following symbols and files:

- Functions:
  - `Format-AdmanReport` (Public/Format-AdmanReport.ps1, new) — RPT-01 console renderer + optional grid picker.
  - `Export-AdmanReportCsv` (Public/Export-AdmanReportCsv.ps1, new) — RPT-02 CSV export.
  - `Export-AdmanReportHtml` (Public/Export-AdmanReportHtml.ps1, new) — RPT-03 self-contained HTML export.
  - `Get-AdmanInventoryReport` (Public/Get-AdmanInventoryReport.ps1, new) — RPT-06 OS/inventory report.
- Modified functions:
  - `Start-Adman` (Public/Start-Adman.ps1, modified) — adds output-format prompt and renderer dispatch.
- Test files:
  - `tests/Render.Tests.ps1` (new) — RPT-01/02/03 renderer parity.
  - `tests/Report.Inventory.Tests.ps1` (new) — RPT-06 inventory contract.
- Manifest:
  - `adman.psd1` (modified) — adds Format-AdmanReport, Export-AdmanReportCsv, Export-AdmanReportHtml, Get-AdmanInventoryReport to FunctionsToExport.
- CSS fragment:
  - Embedded in Export-AdmanReportHtml; matches the UI-SPEC contract colors, zebra striping, and .true/.false classes.
- Result schema extension:
  - `Bucket` column value 'Inventory' for inventory report objects.
