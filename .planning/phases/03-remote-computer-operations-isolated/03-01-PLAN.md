---
phase: 03-remote-computer-operations-isolated
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/adman.schema.json
  - config/adman.defaults.json
  - config/adman.example.json
  - Private/Config/Initialize-AdmanConfig.ps1
  - adman.psm1
  - Private/Remoting/Connect-AdmanTarget.ps1
  - Private/Remoting/Test-AdmanTransportError.ps1
  - tests/Remoting.Ladder.Tests.ps1
  - tests/Remoting.Cache.Tests.ps1
  - tests/Remoting.Skipped.Tests.ps1
  - tests/Remoting.TimeCaps.Tests.ps1
  - tests/Config.Load.Tests.ps1
  - tests/Config.FailClosed.Tests.ps1
  - tests/Config.NoSecrets.Tests.ps1
  - tests/Config.RoundTrip.Tests.ps1
autonomous: true
requirements:
  - RMT-01
  - RMT-02
user_setup: []
must_haves:
  truths:
    - Each target host is probed with the fixed WinRM -> CIM/WSMan -> CIM/DCOM -> skip ladder and the winning transport name is cached per host for the process (D-04, D-05, RMT-01)
    - Test-WSMan on Windows PowerShell 5.1 is wrapped with a hard timeout so dead hosts cannot hang the menu (D-02, RMT-02)
    - Unreachable or cap-exceeded hosts return 'Skipped' as a first-class non-error outcome; no exception escapes the connector for a dead host (D-06, RMT-02)
    - Two new config timeout keys exist with shipped defaults and are seeded into existing configs the same way the deny-list is seeded (D-02)
  artifacts:
    - Private/Remoting/Connect-AdmanTarget.ps1
    - Private/Remoting/Test-AdmanTransportError.ps1
    - config/adman.schema.json (transport.timeouts.perHostProbeCap, transport.timeouts.totalInventoryRemoteCap)
    - config/adman.defaults.json (perHostProbeCap=10, totalInventoryRemoteCap=120)
    - config/adman.example.json (annotated timeout keys)
    - adman.psm1 ($script:TransportCache initialization)
    - tests/Remoting.Ladder.Tests.ps1
    - tests/Remoting.Cache.Tests.ps1
    - tests/Remoting.Skipped.Tests.ps1
    - tests/Remoting.TimeCaps.Tests.ps1
  key_links:
    - Connect-AdmanTarget reads transport.timeouts.perHostProbeCap from $script:Config and enforces it as a per-host ceiling across the whole ladder
    - Cache key is uppercase computer name; cache stores only the transport name string, never live session objects (D-04)
    - Test-AdmanTransportError maps raw HRESULTs to short operator strings used by the query layer in 03-02
    - Initialize-AdmanConfig seeds missing timeout keys from shipped defaults so existing .store/config.json files do not break on upgrade
    - Ladder tests mock Test-WSMan, New-CimSessionOption, New-CimSession, and Remove-CimSession via -ModuleName adman to prove protocol selection without network calls
---

<objective>
Build the per-host transport probe and process-only cache that Phase 3 remote enrichment depends on. This plan ships `Connect-AdmanTarget` with the fixed WinRM -> CIM/WSMan -> CIM/DCOM -> skip ladder (D-05), a job-wrapped `Test-WSMan` timeout for Windows PowerShell 5.1 (D-02), a process-only transport-name cache keyed by uppercase computer name (D-04), and the two new config timeout keys `transport.timeouts.perHostProbeCap` and `transport.timeouts.totalInventoryRemoteCap` (D-02). Unreachable hosts are reported as `'Skipped'` (D-06, RMT-02). No Public verbs change in this plan.

Purpose: Quarantine transport detection in one Private connector so the inventory report and future live-action verbs never branch on protocol directly.
Output: Working ladder + cache + timeout config + unit-test suite covering ladder order, cache behavior, skipped outcomes, and cap enforcement.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remote-computer-operations-isolated/03-CONTEXT.md
@.planning/phases/03-remote-computer-operations-isolated/03-RESEARCH.md
@.planning/phases/00-foundation-safety-harness/00-CONTEXT.md
@.planning/phases/01-ad-query-reporting-read-only/01-CONTEXT.md
@Private/Config/Initialize-AdmanConfig.ps1
@config/adman.schema.json
@config/adman.defaults.json
@config/adman.example.json
@adman.psm1
@Public/Test-AdmanCapability.ps1
@tests/Config.Load.Tests.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add timeout config keys and module cache slot</name>
  <files>config/adman.schema.json, config/adman.defaults.json, config/adman.example.json, Private/Config/Initialize-AdmanConfig.ps1, adman.psm1, tests/Config.Load.Tests.ps1, tests/Config.FailClosed.Tests.ps1, tests/Config.NoSecrets.Tests.ps1, tests/Config.RoundTrip.Tests.ps1</files>
  <read_first>
    - config/adman.schema.json (transport.timeouts block, required list)
    - config/adman.defaults.json (transport.timeouts current values)
    - config/adman.example.json (transport annotations)
    - Private/Config/Initialize-AdmanConfig.ps1 (Test-AdmanConfigValid nested timeout checks, DenyList seed pattern)
    - adman.psm1 (module-scoped variable initialization pattern)
    - tests/Config.Load.Tests.ps1 (New-AdmanTestConfig transport.timeouts shape)
  </read_first>
  <behavior>
    - Test 1: Test-AdmanConfigValid accepts a config with transport.timeouts.perHostProbeCap=10 and transport.timeouts.totalInventoryRemoteCap=120
    - Test 2: Test-AdmanConfigValid throws when transport.timeouts.perHostProbeCap is missing
    - Test 3: Test-AdmanConfigValid throws when transport.timeouts.totalInventoryRemoteCap is missing
    - Test 4: config/adman.defaults.json carries perHostProbeCap=10 and totalInventoryRemoteCap=120
    - Test 5: Initialize-AdmanConfig seeds missing timeout keys from shipped defaults into an existing config that lacks them, preserving existing WinRM/CIM values
    - Test 6: $script:TransportCache is initialized as an empty hashtable in adman.psm1
    - Test 7: All four existing config test builders include the new timeout keys and still pass
  </behavior>
  <action>
    Extend config/adman.schema.json: under transport.timeouts, add perHostProbeCap (integer, minimum 1, default 10) and totalInventoryRemoteCap (integer, minimum 1, default 120) to the properties block and add both names to the transport.timeouts.required array. Extend config/adman.defaults.json: under transport.timeouts add "perHostProbeCap": 10 and "totalInventoryRemoteCap": 120 with _comment annotations explaining each caps what (D-02). Extend config/adman.example.json with matching annotated entries. Extend Private/Config/Initialize-AdmanConfig.ps1: in Test-AdmanConfigValid, add checks that transport.timeouts.perHostProbeCap and totalInventoryRemoteCap are present integers >= 1; also add a seed step (mirroring the DenyList seed pattern) that, when either timeout key is absent from the loaded config, loads config/adman.defaults.json and inserts the missing timeout defaults into the in-memory config before validation, preserving any existing WinRM/CIM values. Extend adman.psm1: add `$script:TransportCache = @{}` alongside the other module-scoped variables. Update the New-AdmanTestConfig builders in tests/Config.Load.Tests.ps1, tests/Config.FailClosed.Tests.ps1, tests/Config.NoSecrets.Tests.ps1, and tests/Config.RoundTrip.Tests.ps1 so the transport.timeouts hashtable includes perHostProbeCap=10 and totalInventoryRemoteCap=120.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Config.Load.Tests.ps1, tests/Config.FailClosed.Tests.ps1, tests/Config.NoSecrets.Tests.ps1, tests/Config.RoundTrip.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    Schema and defaults carry the two new timeout keys with correct defaults. Loader validates the keys and seeds missing ones from defaults. Module initializes an empty transport cache. All existing config tests pass with the extended builders.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Connect-AdmanTarget ladder + cache + timeout wrapper</name>
  <files>Private/Remoting/Connect-AdmanTarget.ps1, Private/Remoting/Test-AdmanTransportError.ps1</files>
  <read_first>
    - Public/Test-AdmanCapability.ps1 (existing WinRM/CIM/DCOM probe pattern against the DC)
    - .planning/phases/03-remote-computer-operations-isolated/03-RESEARCH.md (Pattern 1 and Pitfall 1 examples)
    - adman.psm1 (module variable initialization)
  </read_first>
  <behavior>
    - Test 1: Connect-AdmanTarget returns 'WinRM' when Test-WSMan succeeds and does not call CIM steps
    - Test 2: Connect-AdmanTarget returns 'CimWsman' when Test-WSMan fails but explicit WSMAN CIM session succeeds
    - Test 3: Connect-AdmanTarget returns 'CimDcom' when WinRM and WSMAN fail but explicit DCOM CIM session succeeds
    - Test 4: Connect-AdmanTarget returns 'Skipped' when all ladder steps fail
    - Test 5: Connect-AdmanTarget caches the result keyed by uppercase computer name; a second call with lowercase name returns the cached value without re-probing
    - Test 6: Connect-AdmanTarget respects perHostProbeCap and marks 'Skipped' if the cap is exceeded before classification completes
    - Test 7: Test-AdmanTransportError maps '0x800706BA' and 'RPC server is unavailable' to 'RPC server unavailable (DCOM firewall)'; maps '0x80070005' to 'Access denied'; maps '0x8009030e' and 'ANONYMOUS LOGON' to 'Double-hop blocked'; leaves other messages as a single-line prefix
  </behavior>
  <action>
    Create Private/Remoting/Connect-AdmanTarget.ps1 with a single `Connect-AdmanTarget` function (Private, non-exported). The function accepts `[Parameter(Mandatory)][string]$ComputerName`. It computes `$key = $ComputerName.ToUpperInvariant()`, checks `$script:TransportCache` and returns the cached value on hit. On miss, it reads `$cap = [int]$script:Config.transport.timeouts.perHostProbeCap`, starts a `[System.Diagnostics.Stopwatch]`, and runs the ladder strictly in this order (D-05): (1) `Test-WSMan` via an internal `Test-WSManWithTimeout` helper that wraps `Test-WSMan -ComputerName $ComputerName` in a `Start-Job` with `Wait-Job -Timeout $remainingSeconds` so PowerShell 5.1 cannot hang (Pitfall 1); if the job completes with any non-null result, set transport to 'WinRM'. (2) If still 'Skipped' and elapsed < cap, `New-CimSessionOption -Protocol Wsman` + `New-CimSession -ComputerName $ComputerName -SessionOption $wsmanOpt -OperationTimeoutSec $remainingCap -ErrorAction Stop`; on success remove the session and set 'CimWsman'. (3) If still 'Skipped' and elapsed < cap, `New-CimSessionOption -Protocol Dcom` + `New-CimSession -ComputerName $ComputerName -SessionOption $dcomOpt -OperationTimeoutSec $remainingCap -ErrorAction Stop`; on success remove the session and set 'CimDcom'. (4) Else 'Skipped'. Cache the result under `$script:TransportCache[$key]` and return it. Every ladder step must run inside a `try/catch` so failure of one step moves to the next; no terminating error escapes for a dead host. The internal `Test-WSManWithTimeout` helper must clean up the job with `Stop-Job`/`Remove-Job -ErrorAction SilentlyContinue` on timeout to avoid orphan jobs. Create Private/Remoting/Test-AdmanTransportError.ps1 with a single `Test-AdmanTransportError` function that accepts an `[Exception]$Exception` and returns a short operator-facing string: detect RPC-unavailable HRESULTs and return 'RPC server unavailable (DCOM firewall)'; detect access-denied HRESULTs and return 'Access denied'; detect ANONYMOUS LOGON / '0x8009030e' and return 'Double-hop blocked'; detect WinRM unreachable text and return 'WinRM unreachable'; for anything else return the first line of the message prefixed with 'Remote error: '. This helper is consumed by the query layer in 03-02 and by ladder logging; it must never throw.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Remoting.Ladder.Tests.ps1, tests/Remoting.Cache.Tests.ps1, tests/Remoting.Skipped.Tests.ps1, tests/Remoting.TimeCaps.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    Ladder order is fixed and exercised by mocks. Cache returns the same transport for repeated calls without re-probing. Cap enforcement marks late hosts as Skipped. Error translator maps common HRESULTs to short strings. No exception escapes for dead hosts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit-test the ladder, cache, skipped outcomes, and caps</name>
  <files>tests/Remoting.Ladder.Tests.ps1, tests/Remoting.Cache.Tests.ps1, tests/Remoting.Skipped.Tests.ps1, tests/Remoting.TimeCaps.Tests.ps1</files>
  <read_first>
    - tests/Report.Inventory.Tests.ps1 (module import + mock setup pattern)
    - tests/Mocks/ActiveDirectory.psm1 (existing mock surface)
    - .planning/phases/03-remote-computer-operations-isolated/03-RESEARCH.md (Validation Architecture test map)
  </read_first>
  <behavior>
    - Test 1 (Ladder): given Test-WSMan succeeds, Connect-AdmanTarget returns 'WinRM' and New-CimSession is never invoked
    - Test 2 (Ladder): given Test-WSMan fails + WSMAN CIM succeeds + DCOM would also succeed, the function returns 'CimWsman' (stops at first success)
    - Test 3 (Ladder): given Test-WSMan fails + WSMAN fails + DCOM succeeds, returns 'CimDcom'
    - Test 4 (Ladder): given all steps fail, returns 'Skipped'
    - Test 5 (Cache): second call with a different case returns the cached transport and does not invoke Test-WSMan
    - Test 6 (Skipped): dead host returns 'Skipped' and remains in the output set without throwing
    - Test 7 (TimeCaps): when Test-WSMan exceeds perHostProbeCap, the function returns 'Skipped' without attempting CIM steps
    - Test 8 (Error translation): Test-AdmanTransportError maps known exception messages to the expected short strings
  </behavior>
  <action>
    Create tests/Remoting.Ladder.Tests.ps1, tests/Remoting.Cache.Tests.ps1, tests/Remoting.Skipped.Tests.ps1, and tests/Remoting.TimeCaps.Tests.ps1 following the existing Pester 6 BeforeAll pattern (PSFramework stub on $TestDrive, import module, seed $script:Config with ManagedOUs, DC, and the new timeout keys). Use `Mock Test-WSMan -ModuleName adman { ... }`, `Mock New-CimSessionOption -ModuleName adman { [pscustomobject]@{ Protocol = $Protocol } }`, `Mock New-CimSession -ModuleName adman { ... }`, and `Mock Remove-CimSession -ModuleName adman { }` to simulate each protocol step without network calls. Drive the ladder outcome via a `$script:MockTransportState` hashtable set per It block. For cache tests, invoke Connect-AdmanTarget twice with different cases and assert the second invocation does not call Test-WSMan. For cap tests, set `$script:Config.transport.timeouts.perHostProbeCap` to a small value (e.g., 0.1 second) and mock Test-WSMan to sleep slightly longer, then assert the result is 'Skipped' and CIM steps were not invoked. For error-translation tests, call Test-AdmanTransportError with constructed exceptions and assert the mapped strings.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Remoting.Ladder.Tests.ps1, tests/Remoting.Cache.Tests.ps1, tests/Remoting.Skipped.Tests.ps1, tests/Remoting.TimeCaps.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    All eight behavior tests pass. Ladder order, caching, skipped outcomes, and cap enforcement are verified with mocks and run in under a few seconds.
  </done>
</task>

</tasks>

<risk_notes>
## Risk / migration notes

- **Config upgrade:** Existing `.store/config.json` files may lack the new timeout keys. Initialize-AdmanConfig seeds missing keys from `config/adman.defaults.json` so the tool does not fail-closed on upgrade, but operators should verify the defaults (10s per host, 120s total) are appropriate for their fleet before relying on them in production.
- **Live session reuse rejected (D-04):** The cache stores only a transport name string. Do NOT extend it to hold `CimSession` or `PSSession` objects in this phase; session lifetime across firewall sleep/trust changes is explicitly deferred.
- **PowerShell 5.1 timeout reality:** `Test-WSMan` has no native timeout parameter on 5.1. The `Start-Job` wrapper is the chosen cross-edition compromise. It adds per-call overhead but guarantees the menu cannot hang on dead hosts.
- **Parallel deferred:** Serial probes are intentional (D-02). Parallel fleet probes are explicitly out of scope for Phase 3 and must not be introduced here.
- **No live actions:** This plan ships only transport detection. Remote live actions (service restart, gpupdate, etc.) remain v2 scope `RMT-V01` and must not be added.
</risk_notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> connector | Operator calls inventory report; connector probes targets on their behalf |
| connector -> target host | WinRM/WSMan/DCOM network probes; no credential forwarding beyond OS Kerberos/NTLM |
| connector -> cache | Module-scoped hashtable stores only transport name, no live sessions |
| connector -> caller | Return value is a transport name or 'Skipped'; raw exceptions are caught and translated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Denial of Service | Test-WSMan on dead host | high | mitigate | Job-wrapped timeout with hard cap; dead hosts return 'Skipped' without hanging the menu (D-02, Pitfall 1) |
| T-03-02 | Information Disclosure | Verbose error messages from target | medium | mitigate | Test-AdmanTransportError maps raw HRESULTs/stack traces to short operator strings; no stack trace crosses the connector boundary |
| T-03-03 | Tampering | Cache poisoning via malformed key | low | mitigate | Key is uppercase computer name only; no user input beyond the computer name is accepted; cache value is validated against a fixed set |
| T-03-04 | Elevation of Privilege | Credential reuse on hop host | high | mitigate | No live session objects are cached; no CredSSP transport option exists in Phase 3 (D-07) |
| T-03-SC | Tampering | Built-in module reliance | low | accept | CimCmdlets and Microsoft.WSMan.Management are OS components; no gallery install needed; Package Legitimacy Audit in 03-RESEARCH.md confirms OK verdicts |
</threat_model>

<verification>
- `Invoke-Pester -Path tests/Remoting.Ladder.Tests.ps1 -Tag Unit` — ladder order green
- `Invoke-Pester -Path tests/Remoting.Cache.Tests.ps1 -Tag Unit` — cache behavior green
- `Invoke-Pester -Path tests/Remoting.Skipped.Tests.ps1 -Tag Unit` — Skipped first-class outcome green
- `Invoke-Pester -Path tests/Remoting.TimeCaps.Tests.ps1 -Tag Unit` — per-host cap green
- `Invoke-Pester -Path tests/Config.Load.Tests.ps1, tests/Config.FailClosed.Tests.ps1, tests/Config.NoSecrets.Tests.ps1, tests/Config.RoundTrip.Tests.ps1 -Tag Unit` — no regressions
- `Invoke-ScriptAnalyzer -Path Private/Remoting/ -Settings PSScriptAnalyzerSettings.psd1` — lint clean
</verification>

<success_criteria>
- `Connect-AdmanTarget` exists in `Private/Remoting/Connect-AdmanTarget.ps1` and returns exactly one of 'WinRM', 'CimWsman', 'CimDcom', 'Skipped'
- Ladder order is fixed: Test-WSMan first, then explicit WSMAN CIM session, then explicit DCOM CIM session, then Skipped (D-05)
- Process-only cache is keyed by uppercase computer name and stores only the transport name string (D-04)
- `Test-WSMan` is wrapped with a job timeout so PowerShell 5.1 cannot hang on dead hosts (D-02)
- Dead/unreachable hosts return 'Skipped' without throwing (D-06, RMT-02)
- `transport.timeouts.perHostProbeCap` (default 10) and `transport.timeouts.totalInventoryRemoteCap` (default 120) are present in schema, defaults, example, and loader validation
- Existing configs missing the new keys are seeded from defaults by `Initialize-AdmanConfig`
- `$script:TransportCache` is initialized in `adman.psm1`
- All new and existing unit tests pass; PSScriptAnalyzer reports zero findings in `Private/Remoting/`
</success_criteria>

<output>
Create `.planning/phases/03-remote-computer-operations-isolated/03-01-SUMMARY.md` when done.
</output>
