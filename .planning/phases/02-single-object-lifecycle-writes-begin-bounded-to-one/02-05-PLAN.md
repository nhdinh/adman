---
phase: 02-single-object-lifecycle-writes-begin-bounded-to-one
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - Public/Add-AdmanGroupMember.ps1
  - Public/Remove-AdmanGroupMember.ps1
  - tests/Group.Add.Tests.ps1
  - tests/Group.Remove.Tests.ps1
  - tests/Group.Protected.Tests.ps1
autonomous: true
requirements:
  - GRP-01
  - GRP-02
  - GRP-03
user_setup: []
must_haves:
  truths:
    - Admin can add a user to one or more groups through the gate (GRP-01)
    - Admin can remove a user from a group through the gate (GRP-02)
    - The tool refuses adding any principal to a protected group per SAFE-06 (GRP-03)
    - Removing a principal FROM a protected group is allowed as remediation (D-04 asymmetry)
    - The audit record names both the member DN (target) and the group DN (group field)
    - Every Public verb declares [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')]
  artifacts:
    - Public/Add-AdmanGroupMember.ps1
    - Public/Remove-AdmanGroupMember.ps1
  key_links:
    - Both verbs call Invoke-AdmanMutation with $Parameters['GroupIdentity']; the gate performs dual resolution (member via Resolve-AdmanTarget, group via Resolve-AdmanGroup)
    - Test-AdmanGroupAllowed refuses Add when the group's own objectSid is in $script:ProtectedSIDs (direct SID equality, NOT IN_CHAIN)
    - Test-AdmanGroupAllowed SKIPS the protected-SID check on Remove (remediation allowed)
    - Write-AdmanAudit emits the group field alongside target when -Group is supplied
---

<objective>
Ship the two AD group-membership Public verbs (GRP-01/02/03). Both route through the AD gate's dual-resolution path built in Plan 02-01: the member is resolved via the existing Resolve-AdmanTarget and checked by the existing Test-AdmanTargetAllowed; the group is resolved via Resolve-AdmanGroup and checked by Test-AdmanGroupAllowed. The D-04 asymmetry (Add is strict, Remove is remediation) is enforced in the gate.

Purpose: GRP-01/02 are the two-object mutations that exercise the D-04 dual-resolution matrix. GRP-03's protected-group refusal is the safety-critical invariant.
Output: Two Public verbs + three test files. No Private/ changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-CONTEXT.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-RESEARCH.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-PATTERNS.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-01-SUMMARY.md
@Public/Find-AdmanUser.ps1
@Private/Safety/Invoke-AdmanMutation.ps1
@Private/Safety/Resolve-AdmanGroup.ps1
@Private/Safety/Test-AdmanGroupAllowed.ps1
@tests/Find.User.Tests.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add/Remove-AdmanGroupMember (GRP-01/02/03)</name>
  <files>Public/Add-AdmanGroupMember.ps1, Public/Remove-AdmanGroupMember.ps1, tests/Group.Add.Tests.ps1, tests/Group.Remove.Tests.ps1, tests/Group.Protected.Tests.ps1</files>
  <read_first>
    - Public/Find-AdmanUser.ps1 (WR-01 init check, comment-based help shape)
    - Private/Safety/Invoke-AdmanMutation.ps1 (the dual-resolution group path built in Plan 02-01)
    - Private/Safety/Test-AdmanGroupAllowed.ps1 (the three group-side checks + Remove asymmetry)
    - tests/Find.User.Tests.ps1 (BeforeAll pattern)
  </read_first>
  <behavior>
    - Test 1: Add-AdmanGroupMember calls Invoke-AdmanMutation with -Verb 'Add-ADGroupMember', -Targets @($Identity), and $Parameters containing GroupIdentity
    - Test 2: Remove-AdmanGroupMember calls Invoke-AdmanMutation with -Verb 'Remove-ADGroupMember', -Targets @($Identity), and $Parameters containing GroupIdentity
    - Test 3: the gate refuses Add-AdmanGroupMember when the group's objectSid is in $script:ProtectedSIDs (GRP-03); the refusal is logged 'Refused' with reason matching 'protected identity'
    - Test 4: the gate ALLOWS Remove-AdmanGroupMember when the group's objectSid is in $script:ProtectedSIDs (D-04 asymmetry — remediation); member-side checks still apply
    - Test 5: the audit record for a group mutation contains BOTH target (member DN) and group (group DN) fields
    - Test 6: both verbs throw the WR-01 init message when uninitialized
    - Test 7: both verbs forward -Force to the gate
  </behavior>
  <action>
    Create Public/Add-AdmanGroupMember.ps1 per GRP-01/D-04: comment-based help (.SYNOPSIS with GRP-01, .DESCRIPTION explaining the dual-resolution matrix — member-side checks unchanged, group-side refuses protected/deny-listed/gMSA groups on Add, .EXAMPLE), [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')], parameters [Parameter(Mandatory)][ValidateNotNullOrEmpty()][string]$Identity (the member user/computer), [Parameter(Mandatory)][ValidateNotNullOrEmpty()][string]$GroupIdentity (the group), [switch]$Force. WR-01 init check. Build $params = @{ GroupIdentity = $GroupIdentity } and call Invoke-AdmanMutation -Verb 'Add-ADGroupMember' -Targets @($Identity) -Parameters $params -Force:$Force -WhatIf:$WhatIfPreference -Confirm:$false. Create Public/Remove-AdmanGroupMember.ps1 per GRP-02/D-04: identical shape but -Verb 'Remove-ADGroupMember'; .DESCRIPTION notes the asymmetry — removing a principal FROM a protected group is allowed as remediation (member-side checks still apply; group-side protected check is skipped). Create tests/Group.Add.Tests.ps1, tests/Group.Remove.Tests.ps1, tests/Group.Protected.Tests.ps1 with the seven behavior tests above. For Test 3, mock Resolve-AdmanGroup to return a group whose objectSid is in the protected set, mock Test-AdmanGroupAllowed to return @{ Allowed=$false; Reason='group is a protected identity (SID ...)' }, and assert the gate writes a 'Refused' audit record and skips the write wrapper. For Test 4, mock Test-AdmanGroupAllowed to return @{ Allowed=$true; Reason='' } (the protected check is skipped on Remove) and assert the write wrapper IS called. For Test 5, assert the Write-AdmanAudit mock receives -Group 'CN=Mock Group,...' alongside -Targets.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Group.Add.Tests.ps1, tests/Group.Remove.Tests.ps1, tests/Group.Protected.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    All seven behavior tests pass. Add-AdmanGroupMember routes through the gate with GroupIdentity in $Parameters. Remove-AdmanGroupMember routes through the gate with the same shape. The gate refuses Add to a protected group (GRP-03) and allows Remove from a protected group (D-04 asymmetry). The audit record names both the member DN (target) and the group DN (group field). Both verbs throw the WR-01 init message when uninitialized.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates the following new symbols:

**New Public functions (exported in adman.psd1 by Plan 02-06):**
- `Add-AdmanGroupMember` (Public/Add-AdmanGroupMember.ps1)
- `Remove-AdmanGroupMember` (Public/Remove-AdmanGroupMember.ps1)

**New test files:**
- tests/Group.Add.Tests.ps1
- tests/Group.Remove.Tests.ps1
- tests/Group.Protected.Tests.ps1
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> Public verb | The operator supplies Identity (member) and GroupIdentity (group) |
| Public verb -> gate | The verb builds $Parameters and calls Invoke-AdmanMutation; the gate performs dual resolution and per-side policy checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Elevation of Privilege | Add-AdmanGroupMember to protected group | high | mitigate | Test-AdmanGroupAllowed refuses when the group's own objectSid is in $script:ProtectedSIDs by DIRECT SID equality (not IN_CHAIN); deny-RID and gMSA checks also apply (D-04, GRP-03) |
| T-02-04 | Repudiation | Group mutation audit | medium | mitigate | The audit record names BOTH the member DN (target) and the group DN (group field); preview and confirmation render both sides (D-04) |
</threat_model>

<verification>
- `Invoke-Pester -Path tests/Group.Add.Tests.ps1, tests/Group.Remove.Tests.ps1, tests/Group.Protected.Tests.ps1 -Tag Unit` — 7/7 green
- `Invoke-Pester -Path tests/ -Tag Unit` — full suite green (no regressions)
</verification>

<success_criteria>
- Add-AdmanGroupMember routes through the gate with dual resolution (GRP-01)
- Remove-AdmanGroupMember routes through the gate with dual resolution (GRP-02)
- The gate refuses Add to a protected group by direct SID equality (GRP-03)
- The gate allows Remove from a protected group as remediation (D-04 asymmetry)
- The audit record names both the member DN and the group DN
- Both verbs declare [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')]
- All existing tests still pass (no regressions)
</success_criteria>

<output>
Create `.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-05-SUMMARY.md` when done.
</output>
