---
phase: 02-single-object-lifecycle-writes-begin-bounded-to-one
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/adman.schema.json
  - config/adman.defaults.json
  - config/adman.example.json
  - Private/Safety/AdmanWriteVerbs.ps1
  - Private/Safety/Invoke-AdmanMutation.ps1
  - Private/Safety/Invoke-AdmanLocalMutation.ps1
  - Private/Safety/Resolve-AdmanCreateTarget.ps1
  - Private/Safety/Resolve-AdmanLocalTarget.ps1
  - Private/Safety/Resolve-AdmanGroup.ps1
  - Private/Safety/Test-AdmanTargetAllowed.ps1
  - Private/Safety/Test-AdmanLocalTargetAllowed.ps1
  - Private/Safety/Test-AdmanGroupAllowed.ps1
  - Private/Safety/Confirm-AdmanAction.ps1
  - Private/AD/Adman.AD.Write.ps1
  - Private/Local/Adman.Local.Write.ps1
  - Private/Audit/Write-AdmanAudit.ps1
  - Private/Utility/New-AdmanRandomPassword.ps1
  - Private/Utility/Test-AdmanPasswordComplexity.ps1
  - rules/AdmanSafetyRules.psm1
  - tests/Mocks/ActiveDirectory.psm1
  - tests/Safety.GateOrder.Tests.ps1
  - tests/Safety.Gate.Tests.ps1
  - tests/Audit.Schema.Tests.ps1
  - tests/Utility.Password.Tests.ps1
  - tests/Local.Gate.Tests.ps1
  - tests/Config.Load.Tests.ps1
  - tests/Config.FailClosed.Tests.ps1
  - tests/Config.NoSecrets.Tests.ps1
  - tests/Config.RoundTrip.Tests.ps1
autonomous: true
requirements:
  - USER-02
  - USER-04
  - LUSR-01
  - LUSR-02
  - GRP-01
  - GRP-02
  - GRP-03
user_setup: []
must_haves:
  truths:
    - The AD gate accepts New-ADUser and routes creates through a synthetic pre-create target so preview and audit name the to-be-created DN (D-01)
    - The sibling local gate Invoke-AdmanLocalMutation exists and reuses Confirm-AdmanAction / Write-AdmanAudit / Assert-AdmanBulkPolicy verbatim (D-02)
    - Test-AdmanGroupAllowed refuses adding any principal whose group SID is in the protected set, by direct SID equality (D-04, GRP-03)
    - Remove-LocalUser demands typed-count confirmation even at count=1 (D-03)
    - New-AdmanRandomPassword produces a 20-char SecureString with at least one char from each of 4 classes via CSPRNG (D-05)
    - Write-AdmanAudit accepts the group field and the MACHINE\username target shape without breaking the existing D-03 schema (D-02/D-04)
    - The audit writer source still contains zero occurrences of the banned secret tokens (Test 2c invariant)
  artifacts:
    - Private/Safety/Invoke-AdmanLocalMutation.ps1
    - Private/Safety/Resolve-AdmanCreateTarget.ps1
    - Private/Safety/Resolve-AdmanLocalTarget.ps1
    - Private/Safety/Resolve-AdmanGroup.ps1
    - Private/Safety/Test-AdmanLocalTargetAllowed.ps1
    - Private/Safety/Test-AdmanGroupAllowed.ps1
    - Private/Local/Adman.Local.Write.ps1
    - Private/Utility/New-AdmanRandomPassword.ps1
    - Private/Utility/Test-AdmanPasswordComplexity.ps1
  key_links:
    - Invoke-AdmanMutation ValidateSet == Get-AdmanAllowedWriteVerbs == Adman.AD.Write.* wrapper set (drift-test triple, extended for New-ADUser)
    - Invoke-AdmanLocalMutation ValidateSet == Adman.Local.Write.* wrapper set (local drift pair)
    - Test-AdmanTargetAllowed create-branch runs ONLY managed-OU scope against the parent OU DN for IsSynthetic targets
    - Test-AdmanLocalTargetAllowed create-branch runs ONLY machine-in-scope + name-shape validation for IsSynthetic local targets (skips RID-500 and Administrators membership — no SID exists yet)
    - Invoke-AdmanLocalMutation create-verb wiring for New-LocalUser: synthetic-target resolver branch + Get-LocalUser uniqueness pre-flight + TOCTOU closure via New-LocalUser's own collision throw (Failure OUTCOME audit) — strictly parallel to D-01's AD-create pattern
    - Confirm-AdmanAction overrides bulkConfirmThreshold to 1 when Verb is Remove-LocalUser
    - Write-AdmanAudit emits group field only when -Group is supplied; emits MACHINE\username target when Targets carry Machine+Name+SID (no DN); emits preDeleteState on local-target detail when the resolved object carries PreDeleteState (D-03)
    - Adman.AD.Write.Add/Remove-ADGroupMember swap Identity/Members: -Identity $Parameters['GroupIdentity'] -Members $o.DistinguishedName, with GroupIdentity stripped from the splat (D-04 dual-resolution shape)
    - Adman.AD.Write.Set-ADAccountPassword splits the splat: Set-ADAccountPassword receives only password parameters (Reset/NewPassword); ChangePasswordAtLogon is stripped and routed to a follow-up Set-ADUser call after the reset succeeds (HIGH #4 review fix)
    - Adman.AD.Write.Set-ADAccountPassword strips 'Unlock' from the splat and calls Unlock-ADAccount after the reset succeeds when $Parameters['Unlock'] -eq $true (B5 fix)
    - Adman.AD.Write.Unlock-ADAccount computes $server = ($Parameters.ContainsKey('Server') -and $Parameters['Server']) ? $Parameters['Server'] : $script:Config.DC, strips 'Server' from the splat, and passes exactly one -Server (HIGH #3 review fix — prevents duplicate-parameter collision when the gate forwards a PDCe override)
    - Every Adman.Local.Write.* wrapper strips 'ComputerName' from the splat before invoking the LocalAccounts cmdlet (B3 fix); Add/Remove-LocalGroupMember also strip 'Group' and pass it explicitly
    - Test-AdmanLocalTargetAllowed machine-in-scope check resolves the computer object via Resolve-AdmanTarget -Targets @("$Machine`$") (trailing `$` required for the sAMAccountName Identity bind)
    - Invoke-AdmanMutation wraps the wrapper call in try/catch: on wrapper throw, writes Write-AdmanAudit -Result 'Failure' -Reason $_.Exception.Message, then rethrows (HIGH #1 review fix — closes the PENDING-orphan gap; same pattern in Invoke-AdmanLocalMutation)
---

<objective>
Build the cross-cutting gate infrastructure every Phase 2 verb plan depends on. This plan extends the Phase 0/1 safety spine with the four mutation families' shared machinery: the D-01 create path, the D-02 sibling local gate, the D-04 dual-resolution group policy, the D-05 CSPRNG password plumbing, and the audit/confirm extensions those require. After this plan lands, Wave 2 verb plans can each be a thin Public/ + tests/ addition with no shared-file conflicts.

Purpose: All four mutation families (AD user, AD computer, local user, group membership) flow through one gate shape. Building the machinery once, in one plan, prevents four verb plans from colliding on the same Private/Safety files.
Output: Extended gate + sibling local gate + new resolvers/policy checks + CSPRNG utility + audit/confirm extensions + config schema additions + mocks + Wave 0 test scaffolds. No Public verbs ship in this plan.

Scope note (24 files): the file count is cross-cutting by design — CONTEXT.md D-01..D-05 each pin changes that span config schema (3 files), the gate + sibling gate (2 files), three new resolvers + two new policy checks (5 files), the audit writer + confirm engine (2 files), the AD + Local wrapper files (2 files), the CSPRNG utility + complexity validator (2 files), the AST guard rules module (1 file), and the test scaffolds + mocks those require (7 files). Splitting this plan further would force Wave 2 verb plans to share Private/Safety files and re-introduce the conflicts this plan exists to prevent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-CONTEXT.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-RESEARCH.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-PATTERNS.md
@.planning/spikes/004-secure-password-generation/Invoke-Spike.ps1
@Private/Safety/Invoke-AdmanMutation.ps1
@Private/Safety/Resolve-AdmanTarget.ps1
@Private/Safety/Test-AdmanTargetAllowed.ps1
@Private/Safety/Confirm-AdmanAction.ps1
@Private/Safety/AdmanWriteVerbs.ps1
@Private/AD/Adman.AD.Write.ps1
@Private/Audit/Write-AdmanAudit.ps1
@Private/Config/Initialize-AdmanConfig.ps1
@config/adman.schema.json
@config/adman.defaults.json
@rules/AdmanSafetyRules.psm1
@tests/Safety.GateOrder.Tests.ps1
@tests/Safety.Gate.Tests.ps1
@tests/Audit.Schema.Tests.ps1
@tests/Mocks/ActiveDirectory.psm1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config schema + D-05 password plumbing (CSPRNG generator + complexity validator + mocks) + existing config fixture updates</name>
  <files>config/adman.schema.json, config/adman.defaults.json, config/adman.example.json, Private/Utility/New-AdmanRandomPassword.ps1, Private/Utility/Test-AdmanPasswordComplexity.ps1, tests/Mocks/ActiveDirectory.psm1, tests/Utility.Password.Tests.ps1, tests/Config.Load.Tests.ps1, tests/Config.FailClosed.Tests.ps1, tests/Config.NoSecrets.Tests.ps1, tests/Config.RoundTrip.Tests.ps1</files>
  <read_first>
    - config/adman.schema.json (current required-key list — must be extended, not replaced)
    - config/adman.defaults.json (current defaults shape — _comment stripping contract)
    - config/adman.example.json (annotated example pattern)
    - Private/Config/Initialize-AdmanConfig.ps1 (Test-AdmanConfigValid nested-key checks around line 121-141)
    - .planning/spikes/004-secure-password-generation/Invoke-Spike.ps1 (lines 37-92 — the CSPRNG recipe to lift verbatim)
    - tests/Mocks/ActiveDirectory.psm1 (existing Export-ModuleMember list at bottom)
    - tests/Config.Load.Tests.ps1 (New-AdmanTestConfig builder at lines 43-68 — gains the security block)
    - tests/Config.FailClosed.Tests.ps1 (config builder pattern — gains the security block)
    - tests/Config.NoSecrets.Tests.ps1 (config builder pattern — gains the security block)
    - tests/Config.RoundTrip.Tests.ps1 (config builder pattern — gains the security block)
  </read_first>
  <behavior>
    - Test 1: New-AdmanRandomPassword -Length 20 returns a [securestring] of length 20
    - Test 2: 100 generated passwords each contain at least one uppercase, one lowercase, one digit, one symbol
    - Test 3: 100 generated passwords contain zero ambiguous glyphs (case-sensitive match against 0, O, o, l, 1, I)
    - Test 4: New-AdmanRandomPassword -Length 3 throws "Length must be >= 4"
    - Test 5: Test-AdmanPasswordComplexity accepts a SecureString meeting length+4-classes; throws a precise reason for each failing class (length, upper, lower, digit, symbol)
    - Test 6: config schema requires security.passwordSource and security.passwordGeneration.length; defaults file carries Generate + 20
    - Test 7: Test-AdmanConfigValid throws when security.passwordSource is missing
    - Test 8: config schema carries security.mustChangeAtNextLogon (boolean, default $true); defaults file ships it as $true; Test-AdmanConfigValid accepts a config that omits it (optional key with shipped default)
    - Test 9 (MEDIUM #5 review fix — existing config fixture regression): all four existing config test suites (Config.Load, Config.FailClosed, Config.NoSecrets, Config.RoundTrip) pass with the new required security top-level key — their New-AdmanTestConfig / equivalent builders gain a security block carrying passwordSource='Generate' and passwordGeneration=@{length=20} so the schema-required key is present in every fixture
  </behavior>
  <action>
    Extend config/adman.schema.json: add a "security" object to properties with required ["passwordSource","passwordGeneration"], where passwordSource is an enum ["Generate","Prompt","Ask"] default "Generate", and passwordGeneration is an object with required ["length"] integer default 20 minimum 8. Add "security" to the top-level required array. ALSO add an OPTIONAL "security.mustChangeAtNextLogon" boolean (NOT in the security.required array — optional with shipped default $true) consumed by New-AdmanUser / Set-AdmanUserPassword in Plan 02-02 (D-05 config-overridable per-installation). ALSO add "safety.requireManagedGroupOU" (boolean, default $false) to the existing safety object in the schema — reserved for the D-04 opt-in group-OU enforcement; schema-only here, consumed by future enforcement if enabled. Extend config/adman.defaults.json with a security block carrying passwordSource="Generate", passwordGeneration.length=20, AND mustChangeAtNextLogon=$true (each with a _comment noting D-05), AND add requireManagedGroupOU=$false under the existing safety block (with a _comment noting D-04 opt-in). Mirror all additions in config/adman.example.json with annotation. Extend Test-AdmanConfigValid in Private/Config/Initialize-AdmanConfig.ps1 to throw "Config validation failed: 'security.passwordSource' is required." when absent, validate the enum value and length>=8, and (when present) validate mustChangeAtNextLogon is a boolean. Create Private/Utility/New-AdmanRandomPassword.ps1 by lifting the Spike 004 recipe verbatim (lines 37-92): 4-class alphabet (23 upper + 23 lower + 8 digit + 22 symbol = 76 chars, excludes 0/O/o/l/1/I), rejection-sampling Get-AdmanCsprngIndex helper (modulo-bias-free), Fisher-Yates shuffle, guarantee at least one char per class, return a read-only [securestring] (build via AppendChar then MakeReadOnly). NEVER use Get-Random (not a CSPRNG) and NEVER use [System.Web.Security.Membership]::GeneratePassword (Desktop-only). Create Private/Utility/Test-AdmanPasswordComplexity.ps1 that validates a SecureString meets length+4-classes; reads the SecureString ONCE via SecureStringToBSTR into a transient plaintext buffer for the regex checks, then ZeroFreeBSTR in a finally block; throws a precise reason per failing class. Extend tests/Mocks/ActiveDirectory.psm1 with LocalAccounts stubs (New-LocalUser, Disable-LocalUser, Enable-LocalUser, Set-LocalUser, Remove-LocalUser, Add-LocalGroupMember, Remove-LocalGroupMember, Get-LocalUser, Get-LocalGroupMember) — each declaring [CmdletBinding(SupportsShouldProcess)] so the lint gate stays clean — and add them to Export-ModuleMember. Create tests/Utility.Password.Tests.ps1 with the eight behavior tests above, following the BeforeAll pattern from tests/Find.User.Tests.ps1 (PSFramework stub on TestDrive, import mocks, import module, seed config). MEDIUM #5 REVIEW FIX (existing config fixture regression): update the config builder in each of the four existing config test files to include the new required security block so the schema-required top-level key is present. In tests/Config.Load.Tests.ps1, extend the New-AdmanTestConfig builder (lines 43-68) — add `security = @{ passwordSource = 'Generate'; passwordGeneration = @{ length = 20 }; mustChangeAtNextLogon = $true }` to the ordered hashtable. Apply the same security-block addition to the config builder in tests/Config.FailClosed.Tests.ps1, tests/Config.NoSecrets.Tests.ps1, and tests/Config.RoundTrip.Tests.ps1 (each file has its own inline builder or uses a shared pattern — locate the ordered hashtable literal and add the security key alongside the existing safety/bulk/transport/credentialPolicy keys). Without this fix, adding 'security' to the schema's top-level required array regresses every existing config test with "required key 'security' is missing."
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Utility.Password.Tests.ps1, tests/Config.Load.Tests.ps1, tests/Config.FailClosed.Tests.ps1, tests/Config.NoSecrets.Tests.ps1, tests/Config.RoundTrip.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    All nine behavior tests pass. Config schema validates a config carrying the new security block (including the optional mustChangeAtNextLogon key); Test-AdmanConfigValid throws when security.passwordSource is absent. New-AdmanRandomPassword returns a 20-char SecureString with all four character classes present and zero ambiguous glyphs across 100 samples. Test-AdmanPasswordComplexity throws "Password must be at least N characters" / "must contain at least one uppercase" / etc. on respective failures. Mocks file exports the nine LocalAccounts stubs. All four existing config test suites (Config.Load, Config.FailClosed, Config.NoSecrets, Config.RoundTrip) pass with the new required security top-level key (MEDIUM #5 review fix verified — no regression).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-01 create path + D-04 group policy + gate ValidateSet/wrapper extension</name>
  <files>Private/Safety/Resolve-AdmanCreateTarget.ps1, Private/Safety/Resolve-AdmanGroup.ps1, Private/Safety/Test-AdmanGroupAllowed.ps1, Private/Safety/Test-AdmanTargetAllowed.ps1, Private/Safety/AdmanWriteVerbs.ps1, Private/Safety/Invoke-AdmanMutation.ps1, Private/AD/Adman.AD.Write.ps1, tests/Safety.GateOrder.Tests.ps1</files>
  <read_first>
    - Private/Safety/Resolve-AdmanTarget.ps1 (resolver shape — Identity parameter set, -Server pinning)
    - Private/Safety/Test-AdmanTargetAllowed.ps1 (insertion point for the create-branch — top of function before existing (a)(b)(c)(d))
    - Private/Safety/Invoke-AdmanMutation.ps1 (the ValidateSet at line 38-40 and the fixed order at lines 48-112)
    - Private/Safety/AdmanWriteVerbs.ps1 (the 9-verb array)
    - Private/AD/Adman.AD.Write.ps1 (existing 9 wrappers — append the New-ADUser wrapper at the bottom)
    - Private/Utility/ConvertTo-AdmanNormalizedDn.ps1 (used by the create-branch scope check)
    - tests/Safety.GateOrder.Tests.ps1 (Tests 1-6 pattern — extend with Tests 7/8/9)
  </read_first>
  <behavior>
    - Test 7: New-ADUser routes through Resolve-AdmanCreateTarget (not Resolve-AdmanTarget); Test-AdmanTargetAllowed receives an IsSynthetic=$true object
    - Test 8: the create-branch skips gMSA/deny-RID/protected-membership checks and runs ONLY managed-OU scope against the parent OU DN; an out-of-scope parent OU refuses with reason 'parent OU outside managed-OU scope'
    - Test 9: Get-AdmanAllowedWriteVerbs == gate ValidateSet == Adman.AD.Write.* wrapper set (drift triple still passes with New-ADUser added)
    - Test 10: Test-AdmanGroupAllowed refuses a group whose objectSid is in $script:ProtectedSIDs when Operation='Add-ADGroupMember' (direct SID equality, NOT IN_CHAIN)
    - Test 11: Test-AdmanGroupAllowed SKIPS the protected-SID check when Operation='Remove-ADGroupMember' (asymmetric remediation allowed) but still applies deny-RID and gMSA checks
    - Test 12: Test-AdmanGroupAllowed refuses a group whose SID RID is in $script:DenyRids regardless of Operation
    - Test 13: uniqueness pre-flight — when a sAMAccountName OR CN lookup within the parent OU returns a hit, the gate refuses with a precise reason BEFORE confirm
    - Test 14: Adman.AD.Write.New-ADUser consumes $Parameters['ChangePasswordAtLogon'] (NOT hardcoded $true); when the caller passes $false the wrapper forwards $false to New-ADUser; when the key is absent the wrapper falls back to $script:Config.security.mustChangeAtNextLogon with a $true default
    - Test 15: Move-ADObject destination validation runs INSIDE the gate as a per-verb Parameters validator — when Verb='Move-ADObject' and $Parameters['TargetPath'] is not under any managed root, the gate refuses with reason matching 'TargetPath .* outside managed OU scope' BEFORE confirm, even when the caller invokes Invoke-AdmanMutation directly (bypassing the Public verb)
    - Test 16 (HIGH #1 review fix — failure outcome auditing): when the wrapper throws (e.g., New-ADUser throws ADPasswordComplexityException), the gate writes Write-AdmanAudit -Result 'Failure' -Reason <exception message> BEFORE rethrowing; the PENDING record is closed with a Failure outcome, never left as an orphan
    - Test 17 (HIGH #3 review fix — Unlock-ADAccount Server override): Adman.AD.Write.Unlock-ADAccount computes $server from $Parameters['Server'] when present (PDCe override from Unlock-AdmanUser) and falls back to $script:Config.DC when absent; the wrapper strips 'Server' from the local splat copy and passes exactly one -Server parameter to Unlock-ADAccount; passing $Parameters['Server']='pdc.mock.local' does NOT throw a duplicate-parameter error
    - Test 18 (HIGH #4 review fix — ChangePasswordAtLogon split): Adman.AD.Write.Set-ADAccountPassword strips 'ChangePasswordAtLogon' from the splat before calling Set-ADAccountPassword (that parameter does not exist on Set-ADAccountPassword); after the reset succeeds, the wrapper calls Set-ADUser -Identity <dn> -ChangePasswordAtLogon <bool> -Server <server> to apply the must-change flag; Set-ADAccountPassword never receives ChangePasswordAtLogon
  </behavior>
  <action>
    Create Private/Safety/Resolve-AdmanCreateTarget.ps1 per D-01: fabricates a PSCustomObject shaped like an ADObject WITHOUT calling Get-ADObject -Identity — carries DistinguishedName="CN=<Name>,<ParentOuDn>", SamAccountName, Name, objectClass=@('top','person','organizationalPerson','user'), objectSid=$null, memberOf=@(), ParentOuDn, IsSynthetic=$true. Create Private/Safety/Resolve-AdmanGroup.ps1 per D-04: single-shot resolver calling Get-ADGroup -Identity <id> -Server $script:Config.DC -Properties objectSid,objectClass,DistinguishedName. Create Private/Safety/Test-AdmanGroupAllowed.ps1 per D-04 with exactly three checks: (i) group objectSid direct equality against $script:ProtectedSIDs — SKIP when Operation='Remove-ADGroupMember'; (ii) group SID RID against $script:DenyRids; (iii) objectClass contains msDS-GroupManagedServiceAccount or msDS-ManagedServiceAccount. Return the Allowed/Reason hashtable shape with accumulated reasons. Modify Private/Safety/Test-AdmanTargetAllowed.ps1: insert the create-branch at the TOP of the function (before existing (a)) — when $Object.PSObject.Properties['IsSynthetic'] -and $Object.IsSynthetic, run ONLY the managed-OU scope check against $Object.ParentOuDn (reuse ConvertTo-AdmanNormalizedDn loop from step (c)) and return early; skip (a)(b)(d) entirely. Modify Private/Safety/AdmanWriteVerbs.ps1: add 'New-ADUser' to the array. Modify Private/Safety/Invoke-AdmanMutation.ps1: (1) add 'New-ADUser' to the ValidateSet; (2) branch the resolver — when Verb is 'New-ADUser', call Resolve-AdmanCreateTarget -Name $Parameters['Name'] -SamAccountName $Parameters['SamAccountName'] -ParentOuDn $Parameters['ParentOuDn'] instead of Resolve-AdmanTarget; (3) insert a uniqueness pre-flight between resolve and Test-AdmanTargetAllowed for creates — call Get-ADObject -Filter "sAMAccountName -eq '<escaped>'" -Server $script:Config.DC AND Get-ADObject -Filter "cn -eq '<escaped>'" -SearchBase <ParentOuDn> -SearchScope OneLevel -Server $script:Config.DC (both via Escape-AdmanAdFilterLiteral; the -SearchScope OneLevel is REQUIRED — AD enforces CN uniqueness within the immediate parent container only, and the default Subtree scope would over-refuse a valid create when a same-CN object exists in a child OU), refuse closed when either returns a hit with reason "sAMAccountName '<x>' already exists" or "CN '<x>' already exists in parent OU"; (4) when Verb is 'Add-ADGroupMember' or 'Remove-ADGroupMember', perform dual resolution — resolve the member via Resolve-AdmanTarget (existing) and the group via Resolve-AdmanGroup -Identity $Parameters['GroupIdentity'], run Test-AdmanTargetAllowed on the member (unchanged) and Test-AdmanGroupAllowed -Operation $Verb on the group, refuse when either side denies, and pass -Group $group.DistinguishedName to Write-AdmanAudit and -Group to Confirm-AdmanAction; (5) PER-VERB PARAMETERS VALIDATOR for Move-ADObject (warning fix — enforcement point is the gate, not the Public verb): when Verb='Move-ADObject', BEFORE Test-AdmanTargetAllowed, validate $Parameters['TargetPath'] is under a managed root — normalize via ConvertTo-AdmanNormalizedDn, loop $script:Config.ManagedOUs with component-boundary anchoring ($t -eq $r -or $t.EndsWith(',' + $r)), refuse closed with reason "TargetPath '<x>' is outside managed OU scope." when no root matches. This is the safety enforcement point; direct gate callers cannot bypass it. Public verbs (Move-AdmanUser / Move-AdmanComputer) MAY keep their early check for UX fail-fast, but the gate check is the authoritative enforcement. (6) HIGH #1 REVIEW FIX (failure outcome auditing): wrap the wrapper invocation `& "Adman.AD.Write.$Verb" -Objects $allowed.ToArray() -Parameters $Parameters -WhatIf:$confirm.WhatIf -Confirm:$false` in a try/catch block. On catch, write Write-AdmanAudit -CorrelationId $cid -Verb $Verb -Targets $allowed.ToArray() -Result 'Failure' -Reason $_.Exception.Message -WhatIf:$confirm.WhatIf, then rethrow the original exception (so the caller still sees the error). This closes the PENDING-orphan gap: every wrapper throw now produces a paired Failure outcome record instead of leaving the PENDING reservation open. Append Adman.AD.Write.New-ADUser to Private/AD/Adman.AD.Write.ps1: one wrapper per the existing shape — [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')], iterate -Objects, call New-ADUser -Name $o.Name -SamAccountName $o.SamAccountName -UserPrincipalName $Parameters['UserPrincipalName'] -Path $o.ParentOuDn -AccountPassword $Parameters['AccountPassword'] -Enabled $true -ChangePasswordAtLogon $changePwd -Server $script:Config.DC -WhatIf:$WhatIfPreference -Confirm:$false -ErrorAction Stop inside a ShouldProcess guard. CHANGEPASSWORDATLOGON CONSUMPTION (warning fix): the wrapper MUST NOT hardcode -ChangePasswordAtLogon $true. Compute $changePwd at the top of the wrapper: when $Parameters.ContainsKey('ChangePasswordAtLogon'), use [bool]$Parameters['ChangePasswordAtLogon']; otherwise fall back to $script:Config.security.mustChangeAtNextLogon with a $true default when the config key is absent. This honors the caller's intent (New-AdmanUser / Set-AdmanUserPassword pass the resolved value through $Parameters) and the D-05 config-overridable default. MODIFY the existing Adman.AD.Write.Add-ADGroupMember and Adman.AD.Write.Remove-ADGroupMember wrappers (D-04 dual-resolution shape): each wrapper now receives the GROUP DN via $Parameters['GroupIdentity'] (resolved once by the gate) and the MEMBER objects via -Objects. The wrapper MUST swap Identity/Members when calling the real cmdlet — call Add-ADGroupMember -Identity $Parameters['GroupIdentity'] -Members $o.DistinguishedName -Server $script:Config.DC (and Remove-ADGroupMember -Identity $Parameters['GroupIdentity'] -Members $o.DistinguishedName -Server $script:Config.DC for the remove wrapper), and REMOVE 'GroupIdentity' from the local splat copy before invoking the cmdlet (so the splat does not pass GroupIdentity as an unknown parameter). The ShouldProcess target string names both sides: "$($o.DistinguishedName) -> $($Parameters['GroupIdentity'])". MODIFY Adman.AD.Write.Set-ADAccountPassword (HIGH #4 review fix — ChangePasswordAtLogon split): the wrapper MUST NOT pass ChangePasswordAtLogon to Set-ADAccountPassword (that parameter does not exist on Set-ADAccountPassword; it belongs on Set-ADUser). Compute $changePwd at the top: when $Parameters.ContainsKey('ChangePasswordAtLogon'), use [bool]$Parameters['ChangePasswordAtLogon']; otherwise fall back to $script:Config.security.mustChangeAtNextLogon with a $true default. Build a local splat copy $p = $Parameters.Clone(); $p.Remove('ChangePasswordAtLogon'); $p.Remove('Unlock'); then call Set-ADAccountPassword -Identity $o.DistinguishedName -Server $server @p -WhatIf:$WhatIfPreference -Confirm:$false -ErrorAction Stop. AFTER the reset succeeds, call Set-ADUser -Identity $o.DistinguishedName -ChangePasswordAtLogon $changePwd -Server $server -WhatIf:$WhatIfPreference -Confirm:$false -ErrorAction Stop to apply the must-change flag. Both calls sit inside ONE ShouldProcess guard (one gate invocation, one audit pair); the Set-ADUser call is skipped under -WhatIf because ShouldProcess returned $false. ALSO (B5 fix): the wrapper detects $Parameters['Unlock'] -eq $true, strips 'Unlock' from the local splat copy, and AFTER the reset succeeds calls Unlock-ADAccount -Identity $o.DistinguishedName -Server $server -WhatIf:$WhatIfPreference -Confirm:$false -ErrorAction Stop. MODIFY Adman.AD.Write.Unlock-ADAccount (HIGH #3 review fix — Server override collision): the current wrapper hardcodes -Server $script:Config.DC and then splats @Parameters, which duplicates -Server when the gate forwards a PDCe override via $Parameters['Server']. Fix: compute $server at the top of the wrapper — when $Parameters.ContainsKey('Server') -and $Parameters['Server'], use [string]$Parameters['Server']; otherwise use $script:Config.DC. Build a local splat copy $p = $Parameters.Clone(); $p.Remove('Server'); then call Unlock-ADAccount -Identity $o.DistinguishedName -Server $server @p -WhatIf:$WhatIfPreference -Confirm:$false -ErrorAction Stop. This passes exactly one -Server parameter and honors the PDCe override from Unlock-AdmanUser. Extend tests/Safety.GateOrder.Tests.ps1 with Tests 7-18 above.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Safety.GateOrder.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    Tests 7-18 pass alongside existing Tests 1-6. The drift-test triple (Get-AdmanAllowedWriteVerbs == gate ValidateSet == wrapper set) holds with New-ADUser added. The create-branch refuses an out-of-scope parent OU. The uniqueness pre-flight refuses on sAMAccountName or CN collision. Test-AdmanGroupAllowed refuses Add to a protected group by direct SID equality and skips that check on Remove. Adman.AD.Write.New-ADUser consumes $Parameters['ChangePasswordAtLogon'] with config fallback (no hardcoded $true). The Move-ADObject destination validator runs inside the gate and refuses out-of-scope TargetPath values even when the Public verb is bypassed. The gate writes a Failure audit record when the wrapper throws (HIGH #1 — no PENDING orphan). Adman.AD.Write.Unlock-ADAccount honors $Parameters['Server'] without a duplicate-parameter collision (HIGH #3). Adman.AD.Write.Set-ADAccountPassword splits ChangePasswordAtLogon to Set-ADUser after the reset (HIGH #4).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-02 local gate + D-03 Remove-LocalUser override + audit/confirm extensions + AST guard extension</name>
  <files>Private/Safety/Invoke-AdmanLocalMutation.ps1, Private/Safety/Resolve-AdmanLocalTarget.ps1, Private/Safety/Test-AdmanLocalTargetAllowed.ps1, Private/Local/Adman.Local.Write.ps1, Private/Safety/Confirm-AdmanAction.ps1, Private/Audit/Write-AdmanAudit.ps1, rules/AdmanSafetyRules.psm1, tests/Safety.Gate.Tests.ps1, tests/Audit.Schema.Tests.ps1, tests/Local.Gate.Tests.ps1</files>
  <read_first>
    - Private/Safety/Invoke-AdmanMutation.ps1 (the fixed order to mirror — lines 48-112)
    - Private/Safety/Confirm-AdmanAction.ps1 (line 45 — the threshold read to override per-verb)
    - Private/Audit/Write-AdmanAudit.ps1 (the $rec ordered hashtable at lines 63-79 and the target normalization at lines 52-61)
    - rules/AdmanSafetyRules.psm1 (the banned-verb array and Export-ModuleMember list)
    - tests/Safety.Gate.Tests.ps1 (the AST-guard pattern to extend for LocalAccounts)
    - tests/Audit.Schema.Tests.ps1 (Test 1 exact-key-set assertion, Test 2c source-hygiene assertion — both must still pass after extension)
  </read_first>
  <behavior>
    - Test 1: Invoke-AdmanLocalMutation runs the same fixed order as Invoke-AdmanMutation (Resolve-AdmanLocalTarget -> Test-AdmanLocalTargetAllowed -> Assert-AdmanBulkPolicy -> Confirm-AdmanAction -> Write-AdmanAudit PENDING -> Adman.Local.Write.<Verb> -> Write-AdmanAudit OUTCOME)
    - Test 2: Resolve-AdmanLocalTarget throws "Remote targets arrive in Phase 3" when -ComputerName is not $null, '.', $env:COMPUTERNAME, or 'localhost'
    - Test 3: Test-AdmanLocalTargetAllowed refuses a target whose LocalRid is '500' (built-in Administrator matched by RID, never by name)
    - Test 4: Test-AdmanLocalTargetAllowed refuses a target that is a member of local Administrators (S-1-5-32-544) when Verb is Disable-LocalUser / Remove-LocalUser / Set-LocalUser; uses Get-LocalGroupMember with try/catch + WMI Win32_GroupUser fallback on 0x80070534; refuses closed when enumeration totally fails
    - Test 5: Test-AdmanLocalTargetAllowed refuses when the target machine's AD computer object is outside managed-OU scope (machine-in-scope via Resolve-AdmanTarget on $env:COMPUTERNAME)
    - Test 6: Confirm-AdmanAction overrides bulkConfirmThreshold to 1 when Verb='Remove-LocalUser' (typed-count confirmation even at count=1)
    - Test 7: Write-AdmanAudit emits target="MACHINE\username" and targets[0]={machine,name,sid} when Targets carry Machine+Name+SID (no DistinguishedName)
    - Test 8: Write-AdmanAudit emits a group field alongside target when -Group is supplied
    - Test 9: the no-secret source-hygiene test (Test 2c) still passes — the writer source contains zero banned tokens (the words 'password', 'secret', 'credential', 'apiKey', 'privateKey' must NOT appear even in comments)
    - Test 10: the AST guard flags any Public/*.ps1 that names a LocalAccounts mutation cmdlet directly (New-LocalUser, Disable-LocalUser, Enable-LocalUser, Set-LocalUser, Remove-LocalUser, Add-LocalGroupMember, Remove-LocalGroupMember)
    - Test 11 (BLOCKER fix — local create synthetic target): New-LocalUser routes through a create-branch in Resolve-AdmanLocalTarget that fabricates a synthetic local target (Machine, proposed Name, SID=$null, LocalRid=$null, IsSynthetic=$true) WITHOUT calling Get-LocalUser; Test-AdmanLocalTargetAllowed receives the synthetic object
    - Test 12 (BLOCKER fix — synthetic skip-checks): Test-AdmanLocalTargetAllowed for an IsSynthetic local target SKIPS the SID-dependent checks (RID-500, local-Administrators membership) and runs ONLY (i) machine-in-scope via Resolve-AdmanTarget on the machine's AD computer object AND (ii) name-shape validation (Name not null/empty, no path-separator or control characters, length <= 20 to match New-LocalUser's own constraint); refuses closed when the machine is out of scope or the name is malformed
    - Test 13 (BLOCKER fix — uniqueness pre-flight + TOCTOU): for Verb='New-LocalUser', Invoke-AdmanLocalMutation runs a uniqueness pre-flight AFTER resolve and BEFORE confirm — Get-LocalUser -Name <proposed> on the target machine must return zero hits, else the gate refuses with reason "local user '<x>' already exists on <machine>"; the TOCTOU window between pre-flight and write is closed by letting New-LocalUser itself throw on collision and recording Result='Failure' in the OUTCOME audit write (mirrors D-01's AD-create closure; Write-AdmanAudit's ValidateSet accepts 'Failure', NOT 'Failed')
    - Test 14 (HIGH #1 review fix — local gate failure outcome auditing): when the local wrapper throws (e.g., New-LocalUser throws on collision), Invoke-AdmanLocalMutation writes Write-AdmanAudit -Result 'Failure' -Reason <exception message> BEFORE rethrowing; the PENDING record is closed with a Failure outcome, never left as an orphan (mirrors the AD gate fix in Task 2)
  </behavior>
  <action>
    Create Private/Safety/Invoke-AdmanLocalMutation.ps1 per D-02: mirror the AD gate's fixed order byte-for-byte but swap the resolver to Resolve-AdmanLocalTarget, the policy to Test-AdmanLocalTargetAllowed, and the wrapper namespace to Adman.Local.Write.*. ValidateSet is the seven-verb local set: New-LocalUser, Disable-LocalUser, Enable-LocalUser, Set-LocalUser, Remove-LocalUser, Add-LocalGroupMember, Remove-LocalGroupMember. The audit Target records MACHINE\username + local SID (no DN); the result object's Targets array formats as "{Machine}\{Name}". CREATE-VERB WIRING (BLOCKER fix): when Verb='New-LocalUser', (1) call Resolve-AdmanLocalTarget with the create-branch flag so it returns the synthetic target instead of throwing on a non-existent user; (2) insert a uniqueness pre-flight BETWEEN resolve and Test-AdmanLocalTargetAllowed — call Get-LocalUser -Name $Parameters['Name'] -ErrorAction SilentlyContinue on the target machine; when a hit returns, refuse closed with reason "local user '<x>' already exists on <machine>" BEFORE confirm; (3) TOCTOU closure — let New-LocalUser itself throw on collision (race between pre-flight and write); the wrapper's -ErrorAction Stop propagates the throw and the OUTCOME audit write records Result='Failure' with the exception message (the 00-05 writer already supports non-Success outcomes; Write-AdmanAudit's ValidateSet accepts 'Failure', NOT 'Failed'). This mirrors D-01's AD-create closure exactly: same skip-checks shape, same uniqueness pre-flight + TOCTOU-via-throw pattern, keeping the two gates structurally symmetric so the drift tests and AST guard stay mechanical. HIGH #1 REVIEW FIX (failure outcome auditing): wrap the wrapper invocation `& "Adman.Local.Write.$Verb" -Objects $allowed.ToArray() -Parameters $Parameters -WhatIf:$confirm.WhatIf -Confirm:$false` in a try/catch block. On catch, write Write-AdmanAudit -CorrelationId $cid -Verb $Verb -Targets $allowed.ToArray() -Result 'Failure' -Reason $_.Exception.Message -WhatIf:$confirm.WhatIf, then rethrow the original exception. This mirrors the AD gate fix in Task 2 and closes the PENDING-orphan gap for local mutations. Create Private/Safety/Resolve-AdmanLocalTarget.ps1 per D-02: validate -ComputerName to localhost in Phase 2 (accept $null, '.', $env:COMPUTERNAME, 'localhost'; throw "Remote targets arrive in Phase 3. -ComputerName '<x>' is not localhost." otherwise); materialize one PSCustomObject per local username via Get-LocalUser carrying Machine, Name, SID, Enabled, FullName, LocalRid (SID split on '-' last segment). CREATE-BRANCH (BLOCKER fix — strictly parallel to D-01's AD-create synthetic pattern): when invoked with -Verb 'New-LocalUser' (or a -Create switch), DO NOT call Get-LocalUser; instead fabricate a synthetic local target PSCustomObject carrying Machine=<validated localhost>, Name=<proposed name>, SID=$null, LocalRid=$null, Enabled=$null, IsSynthetic=$true. The synthetic object flows through the local gate's fixed order unchanged: Test-AdmanLocalTargetAllowed (create-branch below) -> Confirm-AdmanAction -> Write-AdmanAudit PENDING -> Adman.Local.Write.New-LocalUser -> OUTCOME. SAFE-10 preserved: preview and audit Target name the to-be-created MACHINE\username. D-03 pre-delete state capture: when the gate's Verb is 'Remove-LocalUser', Resolve-AdmanLocalTarget ALSO captures (a) group memberships via Get-LocalGroupMember per local group (orphaned-SID-tolerant: try/catch around each call, skip groups that throw 0x80070534 with a warning note in the captured state) and (b) the profile path via Get-CimInstance Win32_UserProfile filtered by SID (tolerate $null when no profile exists); the captured state is attached to the returned PSCustomObject as a PreDeleteState property carrying @{ GroupMemberships=@(...); ProfilePath='...' } and is persisted by Write-AdmanAudit into targets[0] detail for Remove-LocalUser records (the audit writer's per-target detail shape gains an optional preDeleteState field for local targets when present). Create Private/Safety/Test-AdmanLocalTargetAllowed.ps1 per D-02 with three checks for non-synthetic targets: (a) LocalRid -eq '500' -> reason 'built-in local Administrator (RID-500)'; (b) local Administrators membership via Get-LocalGroupMember -Name 'Administrators' with try/catch — on 0x80070534 / 0x534 fall back to Get-CimInstance Win32_GroupUser filtered by GroupComponent.Name='Administrators', on total enumeration failure ADD a refusal reason (fail-closed); refuse when target SID is in the admin set AND Verb is in @('Disable-LocalUser','Remove-LocalUser','Set-LocalUser'); (c) machine-in-scope — resolve the target machine's AD computer object via Resolve-AdmanTarget -Targets @("$($Object.Machine)`$") (the trailing `$` is REQUIRED: Get-ADObject -Identity needs the sAMAccountName form of a computer, which is the machine name with a trailing dollar sign; without it the Identity bind fails) then Test-AdmanTargetAllowed, refuse when not Allowed. CREATE-BRANCH (BLOCKER fix — strictly parallel to D-01's AD-create skip-checks shape): insert at the TOP of the function — when $Object.PSObject.Properties['IsSynthetic'] -and $Object.IsSynthetic, SKIP checks (a) and (b) entirely (no SID exists yet, no group memberships exist yet) and run ONLY (i) the machine-in-scope check (c) AND (ii) name-shape validation: Name is not null/empty/whitespace, contains no path separators ('\','/'), no control characters, and length <= 20 (matching New-LocalUser's own Name constraint); refuse closed when the machine is out of scope or the name is malformed. Return the Allowed/Reason hashtable shape with accumulated reasons. Create Private/Local/Adman.Local.Write.ps1 with seven wrappers (one per local verb), each [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')], iterating -Objects, calling the matching LocalAccounts cmdlet with -WhatIf:$WhatIfPreference -Confirm:$false -ErrorAction Stop; NO -Server parameter (LocalAccounts cmdlets have none). CRITICAL (B3 fix): every local wrapper MUST strip 'ComputerName' from the local $Parameters splat copy BEFORE splatting — LocalAccounts cmdlets do not accept a -ComputerName parameter, so leaving it in the splat makes every local verb throw 'A parameter cannot be found that matches parameter name ComputerName'. Pattern per wrapper: $p = $Parameters.Clone(); $p.Remove('ComputerName'); then splat $p. For Add-LocalGroupMember / Remove-LocalGroupMember wrappers, ALSO remove 'Group' from the splat copy and pass it explicitly: Add-LocalGroupMember -Group $Parameters['Group'] -Member $o.Name @p (avoids the redundant explicit+splat double-pass). Modify Private/Safety/Confirm-AdmanAction.ps1: at line 45, override the threshold to 1 when $Verb -eq 'Remove-LocalUser' (D-03); also accept an optional [string]$Group parameter and render "$count object(s) -> group $Group" in the ShouldProcess/Read-Host prompt when supplied (D-04). Modify Private/Audit/Write-AdmanAudit.ps1: add [string]$Group parameter; extend the target normalization to emit "MACHINE\username" when a target carries Machine+Name but no DistinguishedName, and the per-target detail to emit @{machine,name,sid} for local targets vs @{dn,sid,objectClass} for AD targets, PLUS the optional preDeleteState field on local targets when the resolved object carries PreDeleteState (D-03); add the group field to the $rec ordered hashtable ONLY when -Group is supplied (preserves the exact-key-set Test 1 invariant for non-group records). CRITICAL: the writer source must continue to contain ZERO occurrences of the tokens 'password', 'secret', 'credential', 'apiKey', 'privateKey' (case-insensitive, including comments) — Test 2c asserts this; phrase any new comments without those words (e.g., use "no-secret-key regex" is FORBIDDEN — use "the banned-token regex" or "the schema test" instead). Modify rules/AdmanSafetyRules.psm1: add $script:AdmanBannedLocalWriteVerbs with the seven LocalAccounts cmdlets, add a Get-AdmanBannedLocalWriteVerbs function, extend Export-ModuleMember. Extend tests/Safety.Gate.Tests.ps1 with a new It block asserting no Public/*.ps1 names a banned LocalAccounts cmdlet. Extend tests/Audit.Schema.Tests.ps1 with Test 1b (group field present alongside target when -Group supplied), Test 1c (MACHINE\username shape for local targets), Test 2d ('group','machine','name' keys do NOT match the banned-token regex). Create tests/Local.Gate.Tests.ps1 with the thirteen behavior tests above.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Local.Gate.Tests.ps1, tests/Safety.Gate.Tests.ps1, tests/Audit.Schema.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    All fourteen behavior tests pass. The local gate runs the same fixed order as the AD gate. Resolve-AdmanLocalTarget throws on non-localhost -ComputerName AND fabricates a synthetic target for New-LocalUser without calling Get-LocalUser. Test-AdmanLocalTargetAllowed refuses RID-500, local-Administrators members (with WMI fallback on orphaned-SID), and out-of-scope machines for non-synthetic targets; for synthetic (IsSynthetic) targets it skips SID-dependent checks and runs only machine-in-scope + name-shape validation. The New-LocalUser uniqueness pre-flight refuses on collision before confirm; New-LocalUser's own throw closes TOCTOU with a Failure OUTCOME audit record. The local gate writes a Failure audit record when the wrapper throws (HIGH #1 — no PENDING orphan). Confirm-AdmanAction forces typed-count for Remove-LocalUser at count=1. Write-AdmanAudit emits MACHINE\username target shape and the group field conditionally. The writer source still passes Test 2c (zero banned tokens). The AST guard flags Public/ files naming LocalAccounts cmdlets directly.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates the following new symbols (files, functions, config keys) that subsequent plans and the plan-review convergence pass must recognize as Phase 2 outputs:

**New Private functions:**
- `Resolve-AdmanCreateTarget` (Private/Safety/Resolve-AdmanCreateTarget.ps1)
- `Resolve-AdmanLocalTarget` (Private/Safety/Resolve-AdmanLocalTarget.ps1)
- `Resolve-AdmanGroup` (Private/Safety/Resolve-AdmanGroup.ps1)
- `Test-AdmanLocalTargetAllowed` (Private/Safety/Test-AdmanLocalTargetAllowed.ps1)
- `Test-AdmanGroupAllowed` (Private/Safety/Test-AdmanGroupAllowed.ps1)
- `Invoke-AdmanLocalMutation` (Private/Safety/Invoke-AdmanLocalMutation.ps1)
- `New-AdmanRandomPassword` (Private/Utility/New-AdmanRandomPassword.ps1)
- `Test-AdmanPasswordComplexity` (Private/Utility/Test-AdmanPasswordComplexity.ps1)
- `Get-AdmanCsprngIndex` (Private/Utility/New-AdmanRandomPassword.ps1, helper)
- `Adman.Local.Write.New-LocalUser`, `Adman.Local.Write.Disable-LocalUser`, `Adman.Local.Write.Enable-LocalUser`, `Adman.Local.Write.Set-LocalUser`, `Adman.Local.Write.Remove-LocalUser`, `Adman.Local.Write.Add-LocalGroupMember`, `Adman.Local.Write.Remove-LocalGroupMember` (Private/Local/Adman.Local.Write.ps1)
- `Adman.AD.Write.New-ADUser` (Private/AD/Adman.AD.Write.ps1, appended)
- `Get-AdmanBannedLocalWriteVerbs` (rules/AdmanSafetyRules.psm1)

**New config keys (schema + defaults + example):**
- `security.passwordSource` (enum: Generate|Prompt|Ask, default Generate)
- `security.passwordGeneration.length` (integer, default 20, min 8)
- `security.mustChangeAtNextLogon` (boolean, OPTIONAL in schema, shipped default $true — consumed by New-AdmanUser / Set-AdmanUserPassword in Plan 02-02 and by the Adman.AD.Write.New-ADUser wrapper as the ChangePasswordAtLogon fallback)
- `safety.requireManagedGroupOU` (boolean, default $false — reserved for D-04 opt-in; schema-only here, consumed by future group-OU enforcement if enabled)

**Modified Private functions (signature/behavior extensions):**
- `Invoke-AdmanMutation` — ValidateSet gains 'New-ADUser'; create-branch resolver swap; uniqueness pre-flight; dual-resolution group path; per-verb Parameters validator for Move-ADObject TargetPath (gate-side enforcement); try/catch around wrapper call writing Failure audit on throw (HIGH #1 review fix)
- `Invoke-AdmanLocalMutation` — create-verb wiring for New-LocalUser (synthetic-target resolver branch + uniqueness pre-flight + TOCTOU-via-throw closure, mirroring D-01's AD-create pattern); try/catch around wrapper call writing Failure audit on throw (HIGH #1 review fix)
- `Test-AdmanTargetAllowed` — create-branch for IsSynthetic targets
- `Test-AdmanLocalTargetAllowed` — create-branch for IsSynthetic local targets (skips SID-dependent checks; runs machine-in-scope + name-shape validation only)
- `Resolve-AdmanLocalTarget` — create-branch fabricates synthetic local target for New-LocalUser (no Get-LocalUser call); D-03 pre-delete state capture for Remove-LocalUser
- `Confirm-AdmanAction` — per-verb threshold override (Remove-LocalUser -> 1); optional -Group parameter for two-object rendering
- `Write-AdmanAudit` — new -Group parameter; MACHINE\username target shape; conditional group field; optional preDeleteState field on local-target detail (D-03)
- `Get-AdmanAllowedWriteVerbs` — array gains 'New-ADUser'
- `Adman.AD.Write.Add-ADGroupMember` / `Adman.AD.Write.Remove-ADGroupMember` — D-04 dual-resolution shape: -Identity $Parameters['GroupIdentity'] -Members $o.DistinguishedName; GroupIdentity stripped from splat
- `Adman.AD.Write.Set-ADAccountPassword` — splits ChangePasswordAtLogon to a follow-up Set-ADUser call after the reset succeeds (HIGH #4 review fix); detects $Parameters['Unlock'], strips it, calls Unlock-ADAccount after reset succeeds (B5 fix; one gate invocation, one audit pair)
- `Adman.AD.Write.Unlock-ADAccount` — computes $server from $Parameters['Server'] with $script:Config.DC fallback; strips 'Server' from splat; passes exactly one -Server (HIGH #3 review fix)
- `Adman.AD.Write.New-ADUser` — consumes $Parameters['ChangePasswordAtLogon'] with config fallback to security.mustChangeAtNextLogon (default $true); NOT hardcoded

**New test files:**
- tests/Utility.Password.Tests.ps1
- tests/Local.Gate.Tests.ps1

**Modified test files (MEDIUM #5 review fix — existing config fixture regression):**
- tests/Config.Load.Tests.ps1 — New-AdmanTestConfig builder gains the security block (passwordSource='Generate', passwordGeneration.length=20, mustChangeAtNextLogon=$true) so the schema-required top-level key is present
- tests/Config.FailClosed.Tests.ps1 — config builder gains the security block
- tests/Config.NoSecrets.Tests.ps1 — config builder gains the security block
- tests/Config.RoundTrip.Tests.ps1 — config builder gains the security block

**New mocks (tests/Mocks/ActiveDirectory.psm1):**
- New-LocalUser, Disable-LocalUser, Enable-LocalUser, Set-LocalUser, Remove-LocalUser, Add-LocalGroupMember, Remove-LocalGroupMember, Get-LocalUser, Get-LocalGroupMember
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> gate | The operator supplies targets, parameters, and passwords; the gate is the single enforcement point |
| gate -> AD DS | LDAP writes against the directory; -Server pinned to prevent replication-lag confused-deputy |
| gate -> local SAM | LocalAccounts mutations against the local machine's SAM database |
| gate -> audit file | JSONL append under named mutex; fail-closed on PENDING write failure |
| password source -> SecureString | CSPRNG output or Read-Host -AsSecureString; never marshaled to plaintext |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | New-ADUser create path | high | mitigate | Uniqueness pre-flight (sAMAccountName + CN within parent OU, -SearchScope OneLevel) refuses closed before confirm; New-ADUser throws on collision and OUTCOME records Result='Failure' (D-01; Write-AdmanAudit ValidateSet accepts 'Failure', NOT 'Failed'); gate try/catch writes Failure audit on any wrapper throw (HIGH #1 review fix) |
| T-02-02 | Information Disclosure | Password handling in create/reset | critical | mitigate | SecureString born in New-AdmanRandomPassword / Read-Host -AsSecureString; passed ONLY into Set-ADAccountPassword -NewPassword / New-ADUser -AccountPassword; never marshaled to plaintext (no BSTR conversion anywhere except the transient, ZeroFreeBSTR-guarded buffer inside Test-AdmanPasswordComplexity); audit writer never receives $Parameters (D-05) |
| T-02-03 | Elevation of Privilege | Group membership add | high | mitigate | Test-AdmanGroupAllowed refuses when the group's own objectSid is in $script:ProtectedSIDs by DIRECT SID equality (not IN_CHAIN); deny-RID and gMSA checks also apply (D-04, GRP-03) |
| T-02-04 | Repudiation | Audit writer | high | mitigate | Named-mutex serialization; fail-closed PENDING write; the writer source contains zero banned tokens (Test 2c); no-secret-key regex extended to cover group/machine/name fields; gate try/catch writes Failure outcome on wrapper throw so no PENDING orphan survives (HIGH #1 review fix) |
| T-02-05 | Elevation of Privilege | Local account mutation | high | mitigate | Test-AdmanLocalTargetAllowed refuses RID-500 (built-in Administrator matched by RID, never by name); refuses local-Administrators members on weakening verbs; machine-in-scope check via AD computer object (D-02) |
| T-02-06 | Information Disclosure | Get-LocalGroupMember orphaned SID | medium | mitigate | try/catch + WMI Win32_GroupUser fallback on 0x80070534; refuse closed on total enumeration failure (Pitfall 3) |
| T-02-07 | Tampering | sAMAccountName collision | medium | mitigate | Uniqueness pre-flight + New-ADUser throw on collision; length validated <= 20 (Pitfall 6) |
| T-02-08 | Elevation of Privilege | Move-ADObject -TargetPath | high | mitigate | Destination OU validated under managed roots before confirm (reuses D-01 parent-OU scope check); wired as a per-verb Parameters validator in the gate |
| T-02-11 | Tampering | Unlock-ADAccount Server parameter collision | medium | mitigate | Adman.AD.Write.Unlock-ADAccount computes $server from $Parameters['Server'] with $script:Config.DC fallback and strips 'Server' from the splat, passing exactly one -Server (HIGH #3 review fix) |
| T-02-12 | Tampering | ChangePasswordAtLogon routed to wrong cmdlet | medium | mitigate | Adman.AD.Write.Set-ADAccountPassword strips ChangePasswordAtLogon from the splat and routes it to a follow-up Set-ADUser call after the reset succeeds (HIGH #4 review fix) |
| T-02-SC | Tampering | Module installs (Pester/PSScriptAnalyzer/PSFramework) | high | mitigate | All three already installed and verified in Phase 0/1; no new packages this phase; Package Legitimacy Audit in RESEARCH.md confirms OK verdicts |
</threat_model>

<verification>
- `Invoke-Pester -Path tests/Utility.Password.Tests.ps1 -Tag Unit` — 8/8 green
- `Invoke-Pester -Path tests/Safety.GateOrder.Tests.ps1 -Tag Unit` — Tests 1-15 green (existing 1-6 + new 7-15)
- `Invoke-Pester -Path tests/Local.Gate.Tests.ps1 -Tag Unit` — 13/13 green
- `Invoke-Pester -Path tests/Safety.Gate.Tests.ps1 -Tag Unit` — existing + new LocalAccounts AST guard green
- `Invoke-Pester -Path tests/Audit.Schema.Tests.ps1 -Tag Unit` — Test 1 (exact key set), Test 1b (group field), Test 1c (MACHINE\username), Test 2a/2b/2c/2d green
- `Invoke-Pester -Path tests/ -Tag Unit` — full suite green (no regressions in Phase 0/1 tests)
</verification>

<success_criteria>
- The AD gate accepts New-ADUser and routes creates through a synthetic pre-create target
- The sibling local gate Invoke-AdmanLocalMutation exists and reuses Confirm/Audit/BulkPolicy verbatim
- The local gate routes New-LocalUser through a synthetic-target create-branch (strictly parallel to D-01's AD-create: same skip-checks shape, same uniqueness pre-flight + TOCTOU-via-throw closure)
- Test-AdmanGroupAllowed refuses protected-group adds by direct SID equality, skips the check on Remove
- Remove-LocalUser demands typed-count confirmation even at count=1
- New-AdmanRandomPassword produces a 20-char CSPRNG SecureString with 4 character classes
- Write-AdmanAudit accepts the group field and MACHINE\username target shape without breaking the D-03 schema
- The audit writer source still contains zero banned tokens (Test 2c invariant)
- The drift-test triple (allow-list == ValidateSet == wrapper set) holds with New-ADUser added
- The AST guard flags Public/ files naming LocalAccounts cmdlets directly
- Adman.AD.Write.New-ADUser consumes $Parameters['ChangePasswordAtLogon'] with config fallback (no hardcoded $true)
- The Move-ADObject destination validator runs inside the gate (per-verb Parameters validator), enforcing managed-OU scope even when Public verbs are bypassed
- The gate writes a Failure audit record when the wrapper throws (HIGH #1 review fix — no PENDING orphan)
- Adman.AD.Write.Unlock-ADAccount honors $Parameters['Server'] without a duplicate-parameter collision (HIGH #3 review fix)
- Adman.AD.Write.Set-ADAccountPassword splits ChangePasswordAtLogon to Set-ADUser after the reset (HIGH #4 review fix)
- All four existing config test suites (Config.Load, Config.FailClosed, Config.NoSecrets, Config.RoundTrip) pass with the new required security top-level key (MEDIUM #5 review fix)
- All existing Phase 0/1 tests still pass (no regressions)
</success_criteria>

<output>
Create `.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-01-SUMMARY.md` when done.
</output>
