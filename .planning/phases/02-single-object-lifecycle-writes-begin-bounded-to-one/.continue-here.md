---
context: phase
phase: 02-single-object-lifecycle-writes-begin-bounded-to-one
status: in_progress
last_updated: 2026-07-16T08:39:03.260Z
---

# Continue Here — Phase 02

## Current State

UAT complete: **8 passed / 4 issues / 12 total** (tests 1,4,6,7,8,9,10,11 pass; tests 2,3,5,12 issues). UAT committed as `7b86539`. **6 open gaps** remain (G-02-2 through G-02-9, minus resolved G-02-1/G-02-7).

## Completed Work

- All 12 UAT tests executed in lab shell (lab.local, lab-dc01)
- G-02-1 fixed inline: `$script:StorePath = '.store'` init in `adman.psm1` (StrictMode cold-start)
- G-02-7 fixed inline: `$script:LocalMachineScopeCache = @{}` init in `adman.psm1` (StrictMode cold-start)
- Both inline fixes verified during UAT but **NOT yet committed**

## Remaining Work

1. **Commit inline UAT code fixes** (4 files uncommitted in working tree) → `/gsd-quick`
   - `adman.psm1`, `Private/Config/Initialize-AdmanConfig.ps1`, `Private/Foundation/Test-AdmanAuditWritable.ps1`, `Public/Initialize-Adman.ps1`
2. **Diagnose 6 open gaps + create fix plans** → `/gsd-verify-work 02` (resumes into diagnose_issues) or `/gsd-plan-phase 02 --gaps`
   - Preliminary root causes already documented in 02-UAT.md Gaps section
3. **Execute gap-closure plans** → `/gsd-execute-phase 02 --gaps-only`
4. **Re-verify fixed tests (2,3,5,11,12)** in lab shell → `/gsd-verify-work 02`
5. **Phase completion**: no `*-VERIFICATION.md` exists (execute-phase verify never ran) → `/gsd-execute-phase 02`

## Decisions Made

- Fixed StrictMode cold-start bugs inline during UAT (G-02-1, G-02-7) — blocked all testing; one-line inits unblocked immediately; still uncommitted
- Test 5 marked issue after user clarified NEITHER Disable nor Enable prompted — confirmation expectation is the core of the test

## Blockers (priority order)

| Gap | Severity | Issue |
|-----|----------|-------|
| **G-02-5** | BLOCKER | Confirmation disarmed for all 20 mutation call sites — public verbs forward `-Confirm:$false` into gate; `Confirm-AdmanAction.ps1:81` prompt condition sees inherited `$ConfirmPreference='None'`. Also kills typed-count (Remove-LocalUser). **Core safety property OFF on cmdlet path.** |
| **G-02-3** | BLOCKER | Create flows dead — `Write-AdmanAudit.ps1:78` (`$t.objectSid.Value`) throws under StrictMode for pre-create fabricated targets (objectSid null); fail-closed refuses New-ADUser. Workaround: New-ADUser out-of-band. |
| **G-02-4 / G-02-2** | BLOCKER | Menu prompts crash on non-DN input (identity prompt: sAMAccountName crashes app; parent-OU prompt: raw `Get-ADObject` crash at `Invoke-AdmanMutation.ps1:90`). **Likely ONE shared fix: identity/DN resolver + re-prompt.** Workaround: enter full DNs at menu prompts. |
| **G-02-8** | Major | Protected-group remediation asymmetry dead — member-side check refuses `Remove-ADGroupMember` for any protected-group member ('recursive member of protected group'). Workaround: Remove-ADGroupMember out-of-band. |
| **G-02-6** | Minor | Refusal reasons not surfaced to operator (only `Denied:1` on screen). |
| **G-02-9** | Minor | Group-refusal audit omits member DN. |

## Human Actions Pending (non-blocking)

- Lab cleanup: fixtures `uat-reset1`, `uat-outside1` (CN=Users), `uat-protected1` (**STILL in Domain Admins** — adman removal failed per G-02-8; remove out-of-band), `UAT-PC01`, OUs `moved-in`/`outside-dest`; Default Domain Policy lockout threshold now 3
- Lab shell must be **ELEVATED + local admin** for local-SAM verbs (Test 11); `runas /netonly` alone is not elevated

## Required Reading

`.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-UAT.md` — Gaps section has per-gap root-cause analysis, reproduction evidence, and fix sketches.

## Next Action

Commit the 4 uncommitted inline-fix files via `/gsd-quick`, then `/gsd-verify-work 02` to resume into gap diagnosis + fix planning (or `/gsd-plan-phase 02 --gaps` directly).

## Context Notes

- Lab: domain lab.local, DC lab-dc01, managed OU `OU=adman-test,DC=lab,DC=local`
- Lab shell reaches SAME repo via `\\tsclient\C` mapping
- Audit: `.store/audit/audit-YYYYMMDD.jsonl`
- Phase completion ALSO needs `*-VERIFICATION.md` (status 'missing')
