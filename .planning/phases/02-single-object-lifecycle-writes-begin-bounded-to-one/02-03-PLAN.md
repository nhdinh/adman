---
phase: 02-single-object-lifecycle-writes-begin-bounded-to-one
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - Public/Disable-AdmanComputer.ps1
  - Public/Enable-AdmanComputer.ps1
  - Public/Move-AdmanComputer.ps1
  - Public/Reset-AdmanComputerAccount.ps1
  - tests/Computer.Disable.Tests.ps1
  - tests/Computer.Move.Tests.ps1
  - tests/Computer.Reset.Tests.ps1
autonomous: true
requirements:
  - COMP-02
  - COMP-03
  - COMP-04
user_setup: []
must_haves:
  truths:
    - Admin can disable and enable a computer through the gate (COMP-02)
    - Admin can move a computer to another OU within managed scope (COMP-03)
    - Admin can reset a computer account via the AD-side Set-ADAccountPassword -Reset with honest guidance on which method applies (COMP-04)
    - Every Public verb is a thin prompt-and-dispatch callable directly by a senior (MENU-04)
    - Every Public verb declares [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')]
  artifacts:
    - Public/Disable-AdmanComputer.ps1
    - Public/Enable-AdmanComputer.ps1
    - Public/Move-AdmanComputer.ps1
    - Public/Reset-AdmanComputerAccount.ps1
  key_links:
    - Disable/Enable-AdmanComputer call Invoke-AdmanMutation -Verb 'Disable-ADAccount' / 'Enable-ADAccount' (computer objects are AD security principals; the same wrappers serve both user and computer)
    - Move-AdmanComputer validates -TargetPath under managed roots before calling the gate (same pattern as Move-AdmanUser)
    - Reset-AdmanComputerAccount calls Invoke-AdmanMutation -Verb 'Set-ADAccountPassword' with $Parameters['Reset']=$true and emits guidance text naming the on-machine Test-ComputerSecureChannel -Repair runbook step
---

<objective>
Ship the four AD computer lifecycle Public verbs (COMP-02/03/04). Computer objects are AD security principals, so disable/enable/move reuse the exact same gate verbs as the user path. Reset-AdmanComputerAccount uses the AD-side "Reset Account" semantics (Set-ADAccountPassword -Reset, the ADUC equivalent) plus honest guidance on when the on-machine Test-ComputerSecureChannel -Repair runbook step applies.

Purpose: COMP-02/03/04 complete the AD-side single-object lifecycle. The reset verb's guidance text is a COMP-04 requirement ("with guidance on which method applies"), not a nice-to-have.
Output: Four Public verbs + three test files. No Private/ changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-CONTEXT.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-RESEARCH.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-PATTERNS.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-01-SUMMARY.md
@Public/Find-AdmanComputer.ps1
@Public/Move-AdmanUser.ps1
@Private/Safety/Invoke-AdmanMutation.ps1
@tests/Find.Computer.Tests.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Disable/Enable/Move-AdmanComputer (COMP-02/03)</name>
  <files>Public/Disable-AdmanComputer.ps1, Public/Enable-AdmanComputer.ps1, Public/Move-AdmanComputer.ps1, tests/Computer.Disable.Tests.ps1, tests/Computer.Move.Tests.ps1</files>
  <read_first>
    - Public/Find-AdmanComputer.ps1 (WR-01 init check, comment-based help shape)
    - Public/Move-AdmanUser.ps1 (move destination validation pattern — reuse verbatim)
    - Private/Safety/Invoke-AdmanMutation.ps1 (the gate call signature)
    - tests/Find.Computer.Tests.ps1 (BeforeAll pattern)
  </read_first>
  <behavior>
    - Test 1: Disable-AdmanComputer calls Invoke-AdmanMutation with -Verb 'Disable-ADAccount' and -Targets @($Identity)
    - Test 2: Enable-AdmanComputer calls Invoke-AdmanMutation with -Verb 'Enable-ADAccount' and -Targets @($Identity)
    - Test 3: Disable/Enable-AdmanComputer forward -Force to the gate
    - Test 4: Move-AdmanComputer validates -TargetPath under managed roots BEFORE calling the gate; throws "TargetPath '<x>' is outside managed OU scope." when out of scope
    - Test 5: Move-AdmanComputer calls Invoke-AdmanMutation with -Verb 'Move-ADObject' and $Parameters containing TargetPath when in scope
    - Test 6: all three verbs throw the WR-01 init message when $script:Config.ManagedOUs is absent
  </behavior>
  <action>
    Create Public/Disable-AdmanComputer.ps1 and Public/Enable-AdmanComputer.ps1: identical thin shape to Disable/Enable-AdmanUser — comment-based help (.SYNOPSIS with COMP-02, .DESCRIPTION noting computer objects are AD security principals served by the same gate verbs, .EXAMPLE), [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')], [Parameter(Mandatory)][ValidateNotNullOrEmpty()][string]$Identity, [switch]$Force; WR-01 init check; call Invoke-AdmanMutation -Verb 'Disable-ADAccount' / 'Enable-ADAccount' -Targets @($Identity) -Force:$Force -WhatIf:$WhatIfPreference -Confirm:$false. Create Public/Move-AdmanComputer.ps1: same shape as Move-AdmanUser — comment-based help (.SYNOPSIS with COMP-03), [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')], [Parameter(Mandatory)][ValidateNotNullOrEmpty()][string]$Identity, [Parameter(Mandatory)][ValidateNotNullOrEmpty()][string]$TargetPath, [switch]$Force; WR-01 init check; validate -TargetPath under managed roots via ConvertTo-AdmanNormalizedDn (component-boundary anchored: $t -eq $r -or $t.EndsWith(',' + $r)); throw "TargetPath '<x>' is outside managed OU scope." when no root matches; call Invoke-AdmanMutation -Verb 'Move-ADObject' -Targets @($Identity) -Parameters @{ TargetPath = $TargetPath } -Force:$Force -WhatIf:$WhatIfPreference -Confirm:$false. Create tests/Computer.Disable.Tests.ps1 and tests/Computer.Move.Tests.ps1 with the six behavior tests above, following the BeforeAll pattern from tests/Find.Computer.Tests.ps1 (PSFramework stub on TestDrive, import tests/Mocks/ActiveDirectory.psm1 -Force FIRST, import adman.psd1 -Force, seed $script:Config). Use Pester 6 syntax: Mock -ModuleName adman, Should -Invoke -ModuleName adman.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Computer.Disable.Tests.ps1, tests/Computer.Move.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    All six behavior tests pass. Disable/Enable-AdmanComputer route through the gate with the correct verb. Move-AdmanComputer validates the destination OU under managed roots before calling the gate and throws a precise out-of-scope message. All three verbs throw the WR-01 init message when uninitialized.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reset-AdmanComputerAccount (COMP-04) with honest guidance</name>
  <files>Public/Reset-AdmanComputerAccount.ps1, tests/Computer.Reset.Tests.ps1</files>
  <read_first>
    - Public/Find-AdmanComputer.ps1 (WR-01 init check, comment-based help shape)
    - Private/Safety/Invoke-AdmanMutation.ps1 (the gate call signature; the Set-ADAccountPassword wrapper's @Parameters splat)
    - tests/Find.Computer.Tests.ps1 (BeforeAll pattern)
    - .planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-RESEARCH.md (COMP-04 row: Set-ADAccountPassword -Reset AD-side + Test-ComputerSecureChannel -Repair local-side)
  </read_first>
  <behavior>
    - Test 1: Reset-AdmanComputerAccount calls Invoke-AdmanMutation with -Verb 'Set-ADAccountPassword' and $Parameters containing Reset=$true
    - Test 2: Reset-AdmanComputerAccount emits guidance text naming BOTH methods: the AD-side "Reset Account" (this verb, ADUC equivalent) AND the on-machine Test-ComputerSecureChannel -Repair runbook step; the guidance is emitted via Write-PSFMessage (the established diagnostic pattern) AND surfaced on the return object's Guidance property — NOT via Write-Host (the CLAUDE.md PSAvoidUsingWriteHost suppression covers only the TUI-rendering module, not Public verbs)
    - Test 3: the guidance text states that the AD-side reset breaks the secure channel until the machine rejoins or the channel is repaired on-machine
    - Test 4: Reset-AdmanComputerAccount forwards -Force to the gate
    - Test 5: Reset-AdmanComputerAccount throws the WR-01 init message when uninitialized
  </behavior>
  <action>
    Create Public/Reset-AdmanComputerAccount.ps1 per COMP-04: comment-based help (.SYNOPSIS with COMP-04, .DESCRIPTION explaining the two methods — AD-side "Reset Account" = Set-ADAccountPassword -Reset (ADUC semantics, breaks the secure channel until rejoin/repair) vs on-machine Test-ComputerSecureChannel -Repair (runbook step, runs ON the affected machine, out-of-gate, requires local admin), .EXAMPLE). [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')], [Parameter(Mandatory)][ValidateNotNullOrEmpty()][string]$Identity, [switch]$Force. WR-01 init check. Build $params = @{ Reset = $true } and call Invoke-AdmanMutation -Verb 'Set-ADAccountPassword' -Targets @($Identity) -Parameters $params -Force:$Force -WhatIf:$WhatIfPreference -Confirm:$false. After the gate call returns (and NOT under -WhatIf), emit the guidance text via Write-PSFMessage -Level Host (the established diagnostic pattern from Phase 0/1 — the CLAUDE.md PSAvoidUsingWriteHost suppression covers ONLY the TUI-rendering module, NOT Public verbs; Write-Host here would trip the lint gate) AND attach it to the return object's Guidance property so pipeline callers can surface it: "AD-side reset complete for <Identity>. The secure channel is now broken until the machine rejoins the domain OR the channel is repaired on-machine. To repair the channel ON the affected machine (requires local admin), run: Test-ComputerSecureChannel -Repair -Credential (Get-Credential). If the machine cannot rejoin, use the ADUC 'Reset Account' context menu or this verb again." The guidance text is a COMP-04 requirement ("with guidance on which method applies") — it must name BOTH methods and state the trade-off. Create tests/Computer.Reset.Tests.ps1 with the five behavior tests above. For Tests 2/3, mock Write-PSFMessage -ModuleName adman and assert the emitted text contains 'Test-ComputerSecureChannel' and 'rejoin' (or 'repair'); ALSO assert the return object's Guidance property carries the same text (pipeline-surfaced, not just the diagnostic stream).
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Computer.Reset.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    All five behavior tests pass. Reset-AdmanComputerAccount routes through the gate with Reset=$true in $Parameters. The guidance text names both the AD-side reset and the on-machine Test-ComputerSecureChannel -Repair runbook step, and states that the AD-side reset breaks the secure channel until rejoin/repair. The verb throws the WR-01 init message when uninitialized.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates the following new symbols:

**New Public functions (exported in adman.psd1 by Plan 02-06):**
- `Disable-AdmanComputer` (Public/Disable-AdmanComputer.ps1)
- `Enable-AdmanComputer` (Public/Enable-AdmanComputer.ps1)
- `Move-AdmanComputer` (Public/Move-AdmanComputer.ps1)
- `Reset-AdmanComputerAccount` (Public/Reset-AdmanComputerAccount.ps1)

**New test files:**
- tests/Computer.Disable.Tests.ps1
- tests/Computer.Move.Tests.ps1
- tests/Computer.Reset.Tests.ps1
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> Public verb | The operator supplies Identity and TargetPath |
| Public verb -> gate | The verb builds $Parameters and calls Invoke-AdmanMutation; the gate enforces all policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Elevation of Privilege | Move-AdmanComputer -TargetPath | high | mitigate | Destination OU validated under managed roots BEFORE the gate call (component-boundary anchored via ConvertTo-AdmanNormalizedDn); throws precise out-of-scope message (Pitfall 7) |
| T-02-09 | Denial of Service | Reset-AdmanComputerAccount breaks secure channel | medium | accept | The AD-side reset inherently breaks the channel until rejoin/repair — this is the documented ADUC semantics. The guidance text (COMP-04 requirement) names the on-machine Test-ComputerSecureChannel -Repair runbook step so the operator knows the recovery path. The action is confirmed + audited through the gate. |
</threat_model>

<verification>
- `Invoke-Pester -Path tests/Computer.Disable.Tests.ps1, tests/Computer.Move.Tests.ps1 -Tag Unit` — 6/6 green
- `Invoke-Pester -Path tests/Computer.Reset.Tests.ps1 -Tag Unit` — 5/5 green
- `Invoke-Pester -Path tests/ -Tag Unit` — full suite green (no regressions)
</verification>

<success_criteria>
- Disable/Enable-AdmanComputer route through the gate (COMP-02)
- Move-AdmanComputer validates the destination OU under managed roots before the gate call (COMP-03)
- Reset-AdmanComputerAccount routes through the gate with Reset=$true and emits guidance naming BOTH the AD-side reset and the on-machine Test-ComputerSecureChannel -Repair runbook step (COMP-04)
- All four verbs declare [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')]
- All four verbs are thin prompt-and-dispatch callable directly by a senior (MENU-04)
- All existing tests still pass (no regressions)
</success_criteria>

<output>
Create `.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-03-SUMMARY.md` when done.
</output>
