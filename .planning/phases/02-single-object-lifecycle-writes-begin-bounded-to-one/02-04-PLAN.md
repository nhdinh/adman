---
phase: 02-single-object-lifecycle-writes-begin-bounded-to-one
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - Public/New-AdmanLocalUser.ps1
  - Public/Set-AdmanLocalUser.ps1
  - Public/Remove-AdmanLocalUser.ps1
  - Public/Add-AdmanLocalGroupMember.ps1
  - Public/Remove-AdmanLocalGroupMember.ps1
  - tests/Local.User.Tests.ps1
  - tests/Local.Group.Tests.ps1
autonomous: true
requirements:
  - LUSR-01
  - LUSR-02
user_setup: []
must_haves:
  truths:
    - Admin can create/disable/enable/reset-password/remove a local user on localhost via the LocalAccounts module, mutations through the local gate (LUSR-01)
    - Admin can manage local group membership (e.g., local Administrators) on localhost (LUSR-02)
    - Remove-AdmanLocalUser demands typed-count confirmation even at count=1 and captures pre-delete state in the audit record (D-03)
    - Every Public verb validates -ComputerName to localhost in Phase 2 (throws "Remote targets arrive in Phase 3" otherwise)
    - Every Public verb is a thin prompt-and-dispatch callable directly by a senior (MENU-04)
    - Every Public verb declares [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')]
  artifacts:
    - Public/New-AdmanLocalUser.ps1
    - Public/Set-AdmanLocalUser.ps1
    - Public/Remove-AdmanLocalUser.ps1
    - Public/Add-AdmanLocalGroupMember.ps1
    - Public/Remove-AdmanLocalGroupMember.ps1
  key_links:
    - All five verbs call Invoke-AdmanLocalMutation (the sibling local gate built in Plan 02-01), never the AD gate
    - Remove-AdmanLocalUser relies on the gate's Confirm-AdmanAction per-verb threshold override (Remove-LocalUser -> 1) for typed-count confirmation
    - New-AdmanLocalUser sources the password per security.passwordSource (D-05) identical to New-AdmanUser
---

<objective>
Ship the five local (per-machine) user/group lifecycle Public verbs (LUSR-01/02). All five route through the sibling local gate Invoke-AdmanLocalMutation built in Plan 02-01. Phase 2 validates -ComputerName to localhost only; the verb signatures are stable for Phase 3's transport ladder.

Purpose: LUSR-01/02 cover the local SAM database lifecycle. The D-03 Remove-LocalUser override (typed-count even at count=1 + pre-delete state capture) is the highest-blast-radius verb in this plan.
Output: Five Public verbs + two test files. No Private/ changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-CONTEXT.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-RESEARCH.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-PATTERNS.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-01-SUMMARY.md
@Public/Find-AdmanUser.ps1
@Public/New-AdmanUser.ps1
@Private/Safety/Invoke-AdmanLocalMutation.ps1
@Private/Utility/New-AdmanRandomPassword.ps1
@Private/Utility/Test-AdmanPasswordComplexity.ps1
@tests/Find.User.Tests.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New/Set/Remove-AdmanLocalUser (LUSR-01)</name>
  <files>Public/New-AdmanLocalUser.ps1, Public/Set-AdmanLocalUser.ps1, Public/Remove-AdmanLocalUser.ps1, tests/Local.User.Tests.ps1</files>
  <read_first>
    - Public/Find-AdmanUser.ps1 (WR-01 init check, comment-based help shape)
    - Public/New-AdmanUser.ps1 (D-05 password sourcing pattern — reuse verbatim)
    - Private/Safety/Invoke-AdmanLocalMutation.ps1 (the local gate call signature)
    - Private/Utility/New-AdmanRandomPassword.ps1 (D-05 Generate path)
    - Private/Utility/Test-AdmanPasswordComplexity.ps1 (D-05 Prompt path validator)
    - tests/Find.User.Tests.ps1 (BeforeAll pattern)
  </read_first>
  <behavior>
    - Test 1: New-AdmanLocalUser calls Invoke-AdmanLocalMutation with -Verb 'New-LocalUser' and $Parameters containing Name, Password (SecureString), ComputerName
    - Test 2: New-AdmanLocalUser sources the password via New-AdmanRandomPassword when -Password is not supplied and security.passwordSource='Generate'
    - Test 3: Set-AdmanLocalUser calls Invoke-AdmanLocalMutation with -Verb 'Set-LocalUser' for password reset; sources the password per D-05
    - Test 4: Remove-AdmanLocalUser calls Invoke-AdmanLocalMutation with -Verb 'Remove-LocalUser' and relies on the gate's per-verb threshold override for typed-count confirmation
    - Test 5: Remove-AdmanLocalUser help text states plainly: irreversible, no Recycle-Bin equivalent
    - Test 6: all three verbs validate -ComputerName to localhost (accept $null, '.', $env:COMPUTERNAME, 'localhost'; throw "Remote targets arrive in Phase 3" otherwise)
    - Test 7: all three verbs throw the WR-01 init message when uninitialized
    - Test 8: Disable/Enable local user is covered by Set-AdmanLocalUser with -Enable/$true or -Disable switch (or a dedicated parameter) routing to 'Enable-LocalUser'/'Disable-LocalUser'
  </behavior>
  <action>
    Create Public/New-AdmanLocalUser.ps1 per LUSR-01/D-02/D-05: comment-based help, [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')], parameters [Parameter(Mandatory)][ValidateNotNullOrEmpty()][string]$Name, [securestring]$Password (optional), [string]$ComputerName, [switch]$Force. WR-01 init check. Phase 2 localhost validation: accept $null, '.', $env:COMPUTERNAME, 'localhost'; throw "Remote targets arrive in Phase 3. -ComputerName '<x>' is not localhost." otherwise. D-05 password sourcing identical to New-AdmanUser (Generate default via New-AdmanRandomPassword, Prompt via Read-Host -AsSecureString + Test-AdmanPasswordComplexity). Build $params = @{ Name; Password; ComputerName } and call Invoke-AdmanLocalMutation -Verb 'New-LocalUser' -Targets @($Name) -Parameters $params -Force:$Force -WhatIf:$WhatIfPreference -Confirm:$false. Create Public/Set-AdmanLocalUser.ps1: comment-based help, [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High', DefaultParameterSetName='Reset')], parameters [Parameter(Mandatory)][ValidateNotNullOrEmpty()][string]$Name, [Parameter(ParameterSetName='Reset')][securestring]$Password (when supplied, routes to 'Set-LocalUser' for password reset), [Parameter(ParameterSetName='Enable')][switch]$Enable, [Parameter(ParameterSetName='Disable')][switch]$Disable, [string]$ComputerName, [switch]$Force. PARAMETER SETS (warning fix): exactly three parameter sets — 'Reset' (Name + Password), 'Enable' (Name + Enable), 'Disable' (Name + Disable). Enable and Disable are mutually exclusive (different sets). Password cannot be combined with Enable or Disable (different sets). When the caller supplies no switch and no -Password, the default 'Reset' set binds and the verb throws "Parameter set cannot be resolved: supply -Password, -Enable, or -Disable." (no silent no-op). WR-01 init check + localhost validation. When -Enable is present, call Invoke-AdmanLocalMutation -Verb 'Enable-LocalUser'; when -Disable is present, call Invoke-AdmanLocalMutation -Verb 'Disable-LocalUser'; when -Password is supplied, source per D-05 and call Invoke-AdmanLocalMutation -Verb 'Set-LocalUser' with $params = @{ Name; Password; ComputerName }. Create Public/Remove-AdmanLocalUser.ps1 per D-03: comment-based help with .DESCRIPTION stating plainly "This action is IRREVERSIBLE. Local accounts have no Recycle Bin or quarantine OU equivalent. The gate requires typed-count confirmation even at count=1. Pre-delete state (local SID, name, group memberships, profile path) is captured in the audit record for manual re-create." [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')], [Parameter(Mandatory)][ValidateNotNullOrEmpty()][string]$Name, [string]$ComputerName, [switch]$Force. WR-01 init check + localhost validation. Build $params = @{ ComputerName } and call Invoke-AdmanLocalMutation -Verb 'Remove-LocalUser' -Targets @($Name) -Parameters $params -Force:$Force -WhatIf:$WhatIfPreference -Confirm:$false. The gate's Confirm-AdmanAction per-verb threshold override (built in Plan 02-01) forces typed-count confirmation; the audit record captures the pre-delete state via the resolved local target's PreDeleteState property (group memberships + profile path, captured by Resolve-AdmanLocalTarget in Plan 02-01). Create tests/Local.User.Tests.ps1 with the eight behavior tests above. For Test 6, assert each verb throws "Remote targets arrive in Phase 3" when -ComputerName is 'otherhost'. Add a parameter-set test: Set-AdmanLocalUser with neither -Password nor -Enable/-Disable throws the parameter-set resolution error; Set-AdmanLocalUser -Enable -Disable throws a parameter-set conflict error.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Local.User.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    All eight behavior tests pass. New-AdmanLocalUser routes through the local gate with D-05 password sourcing. Set-AdmanLocalUser handles password reset (Set-LocalUser) and enable/disable (Enable/Disable-LocalUser). Remove-AdmanLocalUser routes through the local gate and its help text states the irreversibility plainly. All three verbs validate -ComputerName to localhost and throw the WR-01 init message when uninitialized.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add/Remove-AdmanLocalGroupMember (LUSR-02)</name>
  <files>Public/Add-AdmanLocalGroupMember.ps1, Public/Remove-AdmanLocalGroupMember.ps1, tests/Local.Group.Tests.ps1</files>
  <read_first>
    - Public/Find-AdmanUser.ps1 (WR-01 init check, comment-based help shape)
    - Private/Safety/Invoke-AdmanLocalMutation.ps1 (the local gate call signature)
    - tests/Find.User.Tests.ps1 (BeforeAll pattern)
  </read_first>
  <behavior>
    - Test 1: Add-AdmanLocalGroupMember calls Invoke-AdmanLocalMutation with -Verb 'Add-LocalGroupMember' and $Parameters containing Group and ComputerName
    - Test 2: Remove-AdmanLocalGroupMember calls Invoke-AdmanLocalMutation with -Verb 'Remove-LocalGroupMember' and $Parameters containing Group and ComputerName
    - Test 3: both verbs validate -ComputerName to localhost (throw "Remote targets arrive in Phase 3" otherwise)
    - Test 4: both verbs throw the WR-01 init message when uninitialized
    - Test 5: both verbs forward -Force to the gate
  </behavior>
  <action>
    Create Public/Add-AdmanLocalGroupMember.ps1 and Public/Remove-AdmanLocalGroupMember.ps1 per LUSR-02/D-02: comment-based help (.SYNOPSIS with LUSR-02, .DESCRIPTION noting the local gate's policy checks apply — RID-500 refusal, local-Administrators membership check with orphaned-SID tolerance, machine-in-scope), [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')], parameters [Parameter(Mandatory)][ValidateNotNullOrEmpty()][string]$Name (the local username to add/remove), [Parameter(Mandatory)][ValidateNotNullOrEmpty()][string]$Group (the local group name, e.g. 'Administrators'), [string]$ComputerName, [switch]$Force. WR-01 init check + localhost validation (accept $null, '.', $env:COMPUTERNAME, 'localhost'; throw "Remote targets arrive in Phase 3" otherwise). Build $params = @{ Group; ComputerName } and call Invoke-AdmanLocalMutation -Verb 'Add-LocalGroupMember' / 'Remove-LocalGroupMember' -Targets @($Name) -Parameters $params -Force:$Force -WhatIf:$WhatIfPreference -Confirm:$false. Create tests/Local.Group.Tests.ps1 with the five behavior tests above.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Local.Group.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    All five behavior tests pass. Add/Remove-AdmanLocalGroupMember route through the local gate with Group and ComputerName in $Parameters. Both verbs validate -ComputerName to localhost and throw the WR-01 init message when uninitialized.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates the following new symbols:

**New Public functions (exported in adman.psd1 by Plan 02-06):**
- `New-AdmanLocalUser` (Public/New-AdmanLocalUser.ps1)
- `Set-AdmanLocalUser` (Public/Set-AdmanLocalUser.ps1)
- `Remove-AdmanLocalUser` (Public/Remove-AdmanLocalUser.ps1)
- `Add-AdmanLocalGroupMember` (Public/Add-AdmanLocalGroupMember.ps1)
- `Remove-AdmanLocalGroupMember` (Public/Remove-AdmanLocalGroupMember.ps1)

**New test files:**
- tests/Local.User.Tests.ps1
- tests/Local.Group.Tests.ps1
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> Public verb | The operator supplies Name/Group/ComputerName and optionally a SecureString password |
| Public verb -> local gate | The verb builds $Parameters and calls Invoke-AdmanLocalMutation; the gate enforces local policy |
| local gate -> local SAM | LocalAccounts mutations against the local machine's SAM database |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Information Disclosure | New/Set-AdmanLocalUser password handling | critical | mitigate | SecureString born in New-AdmanRandomPassword / Read-Host -AsSecureString; passed ONLY into the gate's $Parameters; never marshaled to plaintext; audit writer never receives $Parameters (D-05) |
| T-02-05 | Elevation of Privilege | Local account mutation on protected accounts | high | mitigate | The local gate's Test-AdmanLocalTargetAllowed refuses RID-500 (built-in Administrator by RID, never by name) and local-Administrators members on weakening verbs; machine-in-scope check via AD computer object (D-02) |
| T-02-06 | Information Disclosure | Get-LocalGroupMember orphaned SID | medium | mitigate | The local gate's policy check uses try/catch + WMI Win32_GroupUser fallback on 0x80070534; refuses closed on total enumeration failure (Pitfall 3) |
| T-02-10 | Tampering | Remove-AdmanLocalUser irreversibility | high | mitigate | D-03: typed-count confirmation even at count=1 (gate per-verb threshold override); pre-delete state capture in the audit record (local SID, name, group memberships, profile path); help text states plainly: irreversible, no Recycle-Bin equivalent |
</threat_model>

<verification>
- `Invoke-Pester -Path tests/Local.User.Tests.ps1 -Tag Unit` — 8/8 green
- `Invoke-Pester -Path tests/Local.Group.Tests.ps1 -Tag Unit` — 5/5 green
- `Invoke-Pester -Path tests/ -Tag Unit` — full suite green (no regressions)
</verification>

<success_criteria>
- New-AdmanLocalUser creates a local user through the local gate with D-05 password sourcing (LUSR-01)
- Set-AdmanLocalUser handles password reset and enable/disable through the local gate (LUSR-01)
- Remove-AdmanLocalUser routes through the local gate with typed-count confirmation and pre-delete state capture (LUSR-01, D-03)
- Add/Remove-AdmanLocalGroupMember manage local group membership through the local gate (LUSR-02)
- All five verbs validate -ComputerName to localhost in Phase 2
- All five verbs declare [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')]
- All five verbs are thin prompt-and-dispatch callable directly by a senior (MENU-04)
- All existing tests still pass (no regressions)
</success_criteria>

<output>
Create `.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-04-SUMMARY.md` when done.
</output>
