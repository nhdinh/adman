---
phase: 04-bulk-workflows-highest-blast-radius-last
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - Private/Menu/Get-AdmanMenuDefinition.ps1
  - Public/Start-Adman.ps1
  - adman.psd1
  - tests/Menu.BulkWorkflow.Tests.ps1
  - tests/Module.Manifest.Tests.ps1
  - tests/Safety.NoHardDelete.Tests.ps1
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - FLOW-04
  - BULK-01
  - BULK-02
  - BULK-03
  - BULK-04
must_haves:
  truths:
    - The menu exposes bulk action, onboarding, offboarding, and restore entries that dispatch to the same Public verbs a senior calls directly (MENU-04).
    - Onboarding, offboarding, and restore menu entries set `SkipOutputPrompt = $true` so `Start-Adman` does not force their status/checklist text through the generic output-format prompt (review finding).
    - Every new menu entry PromptSpec Name + FixedParameters key resolves to a declared parameter on the target verb (existing MENU-10 contract).
    - adman.psd1 FunctionsToExport explicitly lists the four new Phase 4 verbs (SAFE-08).
    - The AST guard and recursive PSScriptAnalyzer pass over all new Public and Private files.
    - The hard-delete source guard covers all `Public/` and `Private/` source files, not only new Phase 4 files (review finding).
    - The full Pester suite is green before the phase seals using `tests/PesterConfiguration.psd1` (review finding).
  artifacts:
    - Private/Menu/Get-AdmanMenuDefinition.ps1 (extended)
    - Public/Start-Adman.ps1 (extended)
    - adman.psd1 (extended FunctionsToExport)
    - tests/Menu.BulkWorkflow.Tests.ps1
    - tests/Module.Manifest.Tests.ps1 (extended)
    - tests/Safety.NoHardDelete.Tests.ps1 (extended)
  key_links:
    - Menu entries -> Invoke-AdmanBulkAction / Start-AdmanUserOnboarding / Start-AdmanUserOffboarding / Restore-AdmanQuarantinedUser.
    - Manifest FunctionsToExport -> runtime export boundary.
    - Start-Adman SkipOutputPrompt -> workflow/checklist output behavior.
---

<objective>
Wire the new bulk and workflow verbs into the TUI and module manifest, then prove the phase exit gates.

Purpose: The menu is the primary product surface; if the new capabilities are not reachable from `Start-Adman`, they are not usable by the target audience.
Output: Menu entries, output-prompt skip contract, manifest exports, updated contract tests, extended hard-delete guard, and a green lint + test gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-bulk-workflows-highest-blast-radius-last/04-CONTEXT.md
@.planning/phases/04-bulk-workflows-highest-blast-radius-last/04-RESEARCH.md
@.planning/phases/04-bulk-workflows-highest-blast-radius-last/04-PATTERNS.md
@C:/Users/nhdinh/dev/adman/Private/Menu/Get-AdmanMenuDefinition.ps1
@C:/Users/nhdinh/dev/adman/Private/Menu/Read-AdmanActionParams.ps1
@C:/Users/nhdinh/dev/adman/Public/Start-Adman.ps1
@C:/Users/nhdinh/dev/adman/adman.psd1
@C:/Users/nhdinh/dev/adman/tests/Menu.Tests.ps1
@C:/Users/nhdinh/dev/adman/tests/Module.Manifest.Tests.ps1
@C:/Users/nhdinh/dev/adman/tests/Safety.NoHardDelete.Tests.ps1
@C:/Users/nhdinh/dev/adman/tests/PesterConfiguration.psd1
@C:/Users/nhdinh/dev/adman/Public/Invoke-AdmanBulkAction.ps1
@C:/Users/nhdinh/dev/adman/Public/Start-AdmanUserOnboarding.ps1
@C:/Users/nhdinh/dev/adman/Public/Start-AdmanUserOffboarding.ps1
@C:/Users/nhdinh/dev/adman/Public/Restore-AdmanQuarantinedUser.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bulk and workflow entries to the menu definition with output-prompt skip</name>
  <files>Private/Menu/Get-AdmanMenuDefinition.ps1, tests/Menu.BulkWorkflow.Tests.ps1</files>
  <read_first>
    - Private/Menu/Get-AdmanMenuDefinition.ps1
    - Private/Menu/Read-AdmanActionParams.ps1
    - Public/Invoke-AdmanBulkAction.ps1
    - Public/Start-AdmanUserOnboarding.ps1
    - Public/Start-AdmanUserOffboarding.ps1
    - Public/Restore-AdmanQuarantinedUser.ps1
  </read_first>
  <behavior>
    - `Get-AdmanMenuDefinition` returns entries for `Invoke-AdmanBulkAction`, `Start-AdmanUserOnboarding`, `Start-AdmanUserOffboarding`, and `Restore-AdmanQuarantinedUser`.
    - Each entry's PromptSpec Names and FixedParameters keys match declared parameters on the target verb.
    - Bulk action menu entry uses a numeric choice for `Action` and optional prompts for `Path`, `TargetPath`, and `GroupIdentity`.
    - Onboarding, offboarding, and restore entries set `SkipOutputPrompt = $true`; the bulk entry does not.
  </behavior>
  <action>
    Edit Private/Menu/Get-AdmanMenuDefinition.ps1:
      - Add a section separator `--- Bulk & workflows ---`.
      - Add entry for `Invoke-AdmanBulkAction`:
        - PromptSpec: `Action` (required, Choices = @('Disable','Enable','Move','AddGroup','RemoveGroup')), `Path` (required, text), `TargetPath` (optional, text), `GroupIdentity` (optional, text).
        - `SkipOutputPrompt = $false`.
      - Add entry for `Start-AdmanUserOnboarding`:
        - PromptSpec: `FirstName` (required), `LastName` (required).
        - `SkipOutputPrompt = $true`.
      - Add entry for `Start-AdmanUserOffboarding`:
        - PromptSpec: `Identity` (required, Type='AdIdentity').
        - `SkipOutputPrompt = $true`.
      - Add entry for `Restore-AdmanQuarantinedUser`:
        - PromptSpec: `Identity` (required, Type='AdIdentity').
        - `SkipOutputPrompt = $true`.
      - Ensure `Properties` is the empty `[string[]]@()` for all four write entries.
    Create tests/Menu.BulkWorkflow.Tests.ps1 covering:
      - Each Phase 4 verb appears in the menu.
      - Each entry's PromptSpec/FixedParameters key resolves to a declared parameter.
      - Bulk entry exposes the expected Action choices.
      - Onboarding, offboarding, and restore entries have `SkipOutputPrompt = $true`; bulk entry does not.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Menu.BulkWorkflow.Tests.ps1 -Tag Unit -Output Detailed</automated>
  </verify>
  <acceptance_criteria>
    - tests/Menu.BulkWorkflow.Tests.ps1 passes.
    - All four Phase 4 verbs have menu entries.
    - Workflow entries set `SkipOutputPrompt = $true`.
    - No PromptSpec/FixedParameters collision or undeclared parameter exists.
  </acceptance_criteria>
  <done>Menu wired for bulk and workflows with output-prompt skip contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export the new verbs and update manifest contract tests</name>
  <files>adman.psd1, tests/Module.Manifest.Tests.ps1</files>
  <read_first>
    - adman.psd1
    - tests/Module.Manifest.Tests.ps1
  </read_first>
  <behavior>
    - `Test-ModuleManifest` reports `Invoke-AdmanBulkAction`, `Start-AdmanUserOnboarding`, `Start-AdmanUserOffboarding`, and `Restore-AdmanQuarantinedUser` in `ExportedFunctions`.
    - `Invoke-AdmanMutation` remains absent from exports.
    - Module.Manifest.Tests.ps1 asserts the four Phase 4 exports explicitly.
  </behavior>
  <action>
    Edit adman.psd1: append `'Invoke-AdmanBulkAction'`, `'Start-AdmanUserOnboarding'`, `'Start-AdmanUserOffboarding'`, and `'Restore-AdmanQuarantinedUser'` to `FunctionsToExport`.
    Edit tests/Module.Manifest.Tests.ps1: add a new It block (or extend the existing Phase 2 block) asserting the four Phase 4 verbs appear in `ExportedFunctions`. Keep the existing Phase 2 assertions intact.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Module.Manifest.Tests.ps1 -Tag Unit -Output Detailed</automated>
  </verify>
  <acceptance_criteria>
    - tests/Module.Manifest.Tests.ps1 passes.
    - All four Phase 4 verbs are exported; `Invoke-AdmanMutation` is not.
  </acceptance_criteria>
  <done>Manifest exports updated and contract-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 4 exit gate — menu skip contract, lint, hard-delete guard, and full test suite</name>
  <files>Public/Start-Adman.ps1, tests/Safety.NoHardDelete.Tests.ps1, (all files touched in Phase 4)</files>
  <read_first>
    - Public/Start-Adman.ps1
    - PSScriptAnalyzerSettings.psd1
    - tests/PesterConfiguration.psd1
    - tests/Safety.NoHardDelete.Tests.ps1
  </read_first>
  <action>
    Edit Public/Start-Adman.ps1:
      - After `$reportData = & $Verb @params`, check `$entry.SkipOutputPrompt`. If true, skip the output-format prompt block and `continue` the top-level menu loop so workflow/checklist text is not forced through the generic renderer.
      - If `SkipOutputPrompt` is false or absent, keep the existing output-format prompt behavior unchanged.
    Extend tests/Safety.NoHardDelete.Tests.ps1:
      - Add an It block that scans all `.ps1` files under `Public/` and `Private/` recursively for the literal name of the forbidden hard-delete AD cmdlet and asserts zero matches (excludes planning/docs/tests directories by scope).
      - Keep the existing wrapper-specific assertions intact.
    Run recursive PSScriptAnalyzer against the repo root using PSScriptAnalyzerSettings.psd1; fix any new violations in Phase 4 files.
    Run the full unit suite via `Invoke-Pester -Configuration (Import-PowerShellDataFile tests/PesterConfiguration.psd1)` (or `Invoke-Pester -Path tests -Tag Unit` from repo root if the PSD1 cannot be loaded).
    If integration tests require a live lab, run only unit-tagged tests and document the lab manual verification in the phase summary.
  </action>
  <verify>
    <automated>Invoke-ScriptAnalyzer -Path . -Recurse -Settings PSScriptAnalyzerSettings.psd1; Invoke-Pester -Configuration (Import-PowerShellDataFile tests/PesterConfiguration.psd1)</automated>
  </verify>
  <acceptance_criteria>
    - PSScriptAnalyzer reports zero diagnostics.
    - All unit-tagged Pester tests pass.
    - The hard-delete literal is absent from all `Public/` and `Private/` source files.
    - `Start-Adman` honors `SkipOutputPrompt` for workflow entries.
  </acceptance_criteria>
  <done>Phase 4 passes lint, AST/text guard, menu-skip contract, and unit-test gate.</done>
</task>

</tasks>

## Review Findings Addressed

### Review finding: Menu output-format prompt is awkward for workflow/checklist text
**Disposition:** Implemented. Added a `SkipOutputPrompt` property to menu entries and updated `Start-Adman` to skip the generic output-format prompt when it is `$true` for onboarding, offboarding, and restore (Tasks 1 and 3). Tests verify the property (Task 1).

### Review finding: Exit-gate test config references `pester.config.json` instead of the repo's `tests/PesterConfiguration.psd1`
**Disposition:** Implemented. Task 3 now uses `Invoke-Pester -Configuration (Import-PowerShellDataFile tests/PesterConfiguration.psd1)` with a path/tag fallback.

### Review finding: No-hard-delete check is scoped only to new Phase 4 source files
**Disposition:** Implemented. Extended `tests/Safety.NoHardDelete.Tests.ps1` with a repo-wide source scan over `Public/` and `Private/` for the forbidden hard-delete cmdlet literal (Task 3).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Menu -> Public verbs | Operator selection crosses into parameterized function dispatch |
| Manifest -> runtime | Static export boundary controls what is callable externally |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-17 | Elevation of privilege | Menu exposes a bulk action without proper confirmation | medium | mitigate | Menu entry dispatches to the same `Invoke-AdmanBulkAction` engine that enforces cap-after-filter and typed-count confirmation. |
| T-04-18 | Tampering | Manifest accidentally exports the private gate | high | mitigate | FunctionsToExport is explicit; verify `Invoke-AdmanMutation` is absent and the four new verbs are present. |
| T-04-19 | Information disclosure | Menu PromptSpec leaks internal parameter names | low | accept | Parameter names are already public API; no secrets in parameter names. |
| T-04-20 | Tampering | Hard-delete cmdlet introduced anywhere in source tree | high | mitigate | Repo-wide AST/text guard over `Public/` and `Private/` plus the existing wrapper-allow-list test. |
</threat_model>

<verification>
- `Invoke-Pester -Path tests/Menu.BulkWorkflow.Tests.ps1,tests/Module.Manifest.Tests.ps1 -Tag Unit` passes.
- `Invoke-ScriptAnalyzer -Path . -Recurse -Settings PSScriptAnalyzerSettings.psd1` reports zero violations.
- Full unit suite passes via `tests/PesterConfiguration.psd1`.
- Hard-delete literal is absent from `Public/` and `Private/` source files.
</verification>

<success_criteria>
- Bulk and workflow verbs are reachable from the `Start-Adman` menu.
- Workflow menu entries skip the generic output-format prompt.
- Manifest exports the four new verbs explicitly while keeping the gate private.
- Recursive lint and the full unit test suite are green.
- No hard-delete verb literal is introduced in `Public/` or `Private/`.
</success_criteria>

<output>
Create `.planning/phases/04-bulk-workflows-highest-blast-radius-last/04-04-SUMMARY.md` when done.
</output>
