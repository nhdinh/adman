---
phase: 00-foundation-safety-harness
plan: 04
type: execute
wave: 4
depends_on:
  - 00-03
files_modified:
  - Private/Safety/Resolve-AdmanTarget.ps1
  - Private/Safety/Test-AdmanTargetAllowed.ps1
  - Private/Safety/Escape-AdmanLdapFilterValue.ps1
  - Private/Safety/AdmanWriteVerbs.ps1
  - Private/Safety/Confirm-AdmanAction.ps1
  - Private/Safety/Assert-AdmanBulkPolicy.ps1
  - Private/Safety/Invoke-AdmanMutation.ps1
  - Private/AD/Adman.AD.Write.ps1
  - tests/Safety.Scope.Tests.ps1
  - tests/Safety.DenyList.Tests.ps1
  - tests/Safety.Protected.Tests.ps1
  - tests/Safety.Confirm.Tests.ps1
  - tests/Safety.PreviewEqualsExecute.Tests.ps1
  - tests/Safety.NoHardDelete.Tests.ps1
  - tests/Safety.GateOrder.Tests.ps1
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-05
  - SAFE-06
  - SAFE-07
  - SAFE-08
  - SAFE-09
  - SAFE-10
user_setup: []

must_haves:
  truths:
    - "Preview and execute use ONE resolver: Resolve-AdmanTarget materializes the target array once and BOTH the -WhatIf preview and the execute loop consume that exact array (no re-query between preview and execute), so the preview cannot lie (SAFE-10, T-00-06)."
    - "Managed-OU scope uses a component-boundary-anchored DN suffix test: normalize (lowercase/trim/unescape) and accept only if targetDN -eq root OR targetDN.EndsWith(',' + root); a substring -like match is NEVER used (SAFE-07, T-00-02 landmine)."
    - "Protected-account detection runs the gMSA objectClass pre-filter FIRST (msDS-GroupManagedServiceAccount / msDS-ManagedServiceAccount), then the flat deny-list by RID, then ONE DC-side LDAP_MATCHING_RULE_IN_CHAIN (1.2.840.113556.1.4.1941) query over $script:ProtectedGroupDns bound to the TARGET — never enumerating group members client-side and never consulting adminCount (SAFE-06, T-00-03). Every DN/value interpolated into that IN_CHAIN LDAP filter is RFC-4515-escaped via Escape-AdmanLdapFilterValue so a CN containing `( ) * \` or NUL cannot break the filter (fails closed → false refusal, not a bypass — C2-L1)."
    - "The deny-list matches by objectSid/RID (a target's RID against $script:DenyRids), never by sAMAccountName, so a renamed built-in Administrator (RID-500) is still blocked (SAFE-05, T-00-08)."
    - "Confirmation is scaled to blast radius: below safety.bulkConfirmThreshold (default 5) a single default-No ShouldProcess names the count; at/above it the operator must type the exact count (-ceq), refusing on mismatch; -Force / -Confirm:$false bypass ONLY the prompt — deny/protected/scope/cap already ran (SAFE-02, T-00-10)."
    - "Invoke-AdmanMutation is the ONLY function that calls AD write cmdlets, lives in Private/, is NOT exported, and runs the fixed order Resolve -> Allow (Refused logged) -> BulkPolicy -> Confirm -> Audit PENDING -> Execute -> Audit OUTCOME; its ValidateSet is the 9-verb allow-list that deliberately excludes the hard-delete verb (SAFE-08/09, T-00-01/T-00-16)."
    - "Every destructive function declares [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')] and inner AD writes run with -WhatIf:$WhatIfPreference -Confirm:$false (no per-object re-prompt); dry-runs append audit but do not mutate (SAFE-01)."
  artifacts:
    - "Private/Safety/Resolve-AdmanTarget.ps1 (single resolver; Identity-parameter-set lookup with NO -SearchBase/-SearchScope — managed-OU scope is enforced downstream in Test-AdmanTargetAllowed step (c), SAFE-07; exact -Properties, -Server pinned) (C2-H1)"
    - "Private/Safety/Escape-AdmanLdapFilterValue.ps1 (RFC 4515 assertion-value escaping for DN/group values interpolated into LDAP filters) (C2-L1)"
    - "Private/Safety/Test-AdmanTargetAllowed.ps1 (scope + deny + protected; returns @{Allowed;Reason}; escapes DN values before building the IN_CHAIN -LDAPFilter)"
    - "Private/Safety/AdmanWriteVerbs.ps1 (Get-AdmanAllowedWriteVerbs — the 9-verb allow-list; hard-delete excluded)"
    - "Private/Safety/Confirm-AdmanAction.ps1 (ShouldProcess single + typed-count bulk + -Force)"
    - "Private/Safety/Assert-AdmanBulkPolicy.ps1 (cap/threshold reader; enforcement deferred to Phase 4/BULK-02)"
    - "Private/Safety/Invoke-AdmanMutation.ps1 (THE GATE, non-exported)"
    - "Private/AD/Adman.AD.Write.ps1 (one SupportsShouldProcess wrapper per allow-listed verb; the ONE place real AD write cmdlets are called; hard-delete verb has no wrapper)"
    - "tests/Safety.Scope.Tests.ps1, Safety.DenyList.Tests.ps1, Safety.Protected.Tests.ps1, Safety.Confirm.Tests.ps1, Safety.PreviewEqualsExecute.Tests.ps1, Safety.NoHardDelete.Tests.ps1, Safety.GateOrder.Tests.ps1"
  key_links:
    - "Resolve-AdmanTarget's returned array MUST be the identical reference passed to both the preview formatter and the execute loop inside Invoke-AdmanMutation (SAFE-10); a second Get-ADObject call between preview and execute breaks the invariant."
    - "Test-AdmanTargetAllowed reads $script:ProtectedGroupDns and $script:DenyRids produced by 00-03 Get-AdmanProtectedIdentity; the IN_CHAIN filter is built from that exact DN list (no second SID map)."
    - "AdmanWriteVerbs (allow-list), the Invoke-AdmanMutation ValidateSet, and the wrappers in Adman.AD.Write.ps1 MUST be the SAME 9 verbs; Get-AdmanBannedWriteVerbs (00-01) is the complement and the SAFE-08/09 guard enforces both."
    - "Confirm-AdmanAction reads safety.bulkConfirmThreshold from $script:Config (00-02 schema, default 5); Assert-AdmanBulkPolicy reads bulk.maxCount (placeholder; enforced Phase 4) — renaming either key silently changes confirmation/cap behavior."
---

<objective>
Build the safety core that every future mutation flows through: the single target resolver (preview ≡ execute), the scope/deny/protected policy, the scaled confirmation, the bulk-policy placeholder, the non-exported mutation gate with its fixed order, and the gate-only AD write wrappers (SAFE-01/02/05/06/07/08/09/10).

Purpose: This is the load-bearing plan of the whole project. It is proven in isolation by mocking the audit writer (00-05) and the AD layer, so the gate's ordering, refusals, and preview==execute invariant are green before the real audit writer and any real write verb exist. No Public/ write verb is added here (those arrive in Phase 2); the gate is exercised via direct invocation in tests.

Output: The seven Private safety files, the AD write wrappers, and seven Safety.* test files proving each invariant with positive + negative fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-foundation-safety-harness/00-CONTEXT.md
@.planning/phases/00-foundation-safety-harness/00-RESEARCH.md
@.planning/phases/00-foundation-safety-harness/00-PATTERNS.md
@.planning/phases/00-foundation-safety-harness/00-01-PLAN.md
@.planning/phases/00-foundation-safety-harness/00-02-PLAN.md
@.planning/phases/00-foundation-safety-harness/00-03-PLAN.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolver + target policy (Resolve-AdmanTarget, Test-AdmanTargetAllowed, AdmanWriteVerbs) — SAFE-05/06/07/10</name>
  <files>Private/Safety/Resolve-AdmanTarget.ps1, Private/Safety/Test-AdmanTargetAllowed.ps1, Private/Safety/AdmanWriteVerbs.ps1, tests/Safety.Scope.Tests.ps1, tests/Safety.DenyList.Tests.ps1, tests/Safety.Protected.Tests.ps1, tests/Safety.PreviewEqualsExecute.Tests.ps1</files>
  <behavior>
    - Test 1 (scope accept, SAFE-07): a DN equal to a managed root, or ending with ','+root, is Allowed; a sibling/same-prefix DN (e.g. root 'OU=Managed,DC=contoso,DC=com' vs target 'OU=NotManaged,OU=Managed,DC=contoso,DC=com') is REFUSED. DN matrix covers case-differences, escaping, and component-boundary spoofing.
    - Test 2 (scope spoof refused, SAFE-07): a crafted DN that merely contains the root as a substring is REFUSED (component-boundary anchoring).
    - Test 3 (deny by RID, SAFE-05): a target whose objectSid RID is in $script:DenyRids (500/501/502) is REFUSED with Reason containing 'deny-listed'; a renamed built-in Administrator (RID-500, arbitrary sAMAccountName) is still refused.
    - Test 4 (protected recursive, SAFE-06): a target that is a nested member of Domain Admins is REFUSED via a single IN_CHAIN filter bound to the TARGET via the LDAP-filter parameter set ONLY (mock Get-ADObject -LDAPFilter; assert the filter contains '1.2.840.113556.1.4.1941' AND the target's DN, and that the call does NOT use the Identity parameter set); adminCount is never read (assert source has no 'adminCount').
    - Test 5 (gMSA pre-filter first, SAFE-06): a target with objectClass containing 'msDS-GroupManagedServiceAccount' (and legacy 'msDS-ManagedServiceAccount') is REFUSED with Reason containing 'gMSA/service account (objectClass)' even when NOT in any protected group; the IN_CHAIN query STILL runs afterward (no early return) for layering, and a gMSA target that is ALSO a nested protected-group member returns a Reason containing BOTH the objectClass phrase AND the recursive-membership phrase (layered reasons, RESEARCH:547,549).
    - Test 6 (preview == execute, SAFE-10): Resolve-AdmanTarget is called once and the SAME array reference is handed to both the preview path and the execute path (assert reference/count equality; no second Get-ADObject call between them).
    - Test 7 (allow-list data, SAFE-09): Get-AdmanAllowedWriteVerbs returns exactly the 9 verbs and does NOT include the hard-delete verb (assert via the 00-01 Get-AdmanBannedWriteVerbs complement).
    - Test 8 (resolver parameter-set validity, SAFE-10; Addresses review concern C2-H1): an AST assertion over Private/Safety/Resolve-AdmanTarget.ps1 proves the Get-ADObject call that supplies `-Identity` does NOT also bind `-SearchBase` or `-SearchScope` (the Identity parameter set has neither — it has `-Partition`; a `-Identity … -SearchBase` mix cannot bind and throws 'Parameter set cannot be resolved' BEFORE safety logic). The call still pins `-Server` and `-Properties`.
    - Test 9 (LDAP-filter DN escaping, SAFE-06; Addresses review concern C2-L1): special-char-CN fixtures (a CN containing `( ) * \`) are passed through Escape-AdmanLdapFilterValue before interpolation; the protected-check `-LDAPFilter` still resolves/refuses safely and does NOT throw a malformed-filter error. Assert the helper emits `\5c \2a \28 \29 \00` for `\ * ( ) NUL` respectively.
  </behavior>
  <read_first>
    - .planning/phases/00-foundation-safety-harness/00-PATTERNS.md (§Resolve-AdmanTarget L111–L127 body L116–L124; §Test-AdmanTargetAllowed L131–L157 body L136–L154 + scope L157; §Get-AdmanProtectedIdentity consumer note L181–L185)
    - .planning/phases/00-foundation-safety-harness/00-RESEARCH.md (Pattern 2 L306–L322; Pattern 3 L324–L369; Pitfall 3 gMSA L546–L549; Pitfall 4 RID-500 L553–L556; Pitfall 5 DN substring spoof L558–L561; SAFE-09 allow-list L251–L253)
    - .planning/phases/00-foundation-safety-harness/00-CONTEXT.md (D-02 L30–L35; D-05 L51–L56)
    - Private/Safety/Get-AdmanProtectedIdentity.ps1 (00-03 — $script:ProtectedGroupDns / $script:DenyRids), rules/AdmanSafetyRules.psm1 (00-01 — Get-AdmanBannedWriteVerbs complement)
  </read_first>
  <action>
    Private/Safety/AdmanWriteVerbs.ps1: export `Get-AdmanAllowedWriteVerbs` returning the EXACT 9-verb allow-list (Disable-ADAccount, Enable-ADAccount, Move-ADObject, Set-ADUser, Set-ADComputer, Set-ADAccountPassword, Unlock-ADAccount, Add-ADGroupMember, Remove-ADGroupMember). The hard-delete verb is deliberately ABSENT (SAFE-09) — do not add it; reference it only as "the hard-delete verb (SAFE-09)" in comments, never by its literal name in this file's code/comments (the banned literal lives only in 00-01's allowlisted guard/test files). This allow-list is the single source consumed by Invoke-AdmanMutation's ValidateSet (Task 3) and Adman.AD.Write.ps1 (Task 2).
    IMPORTANT correction to the source snippet (Addresses review concern C2-H1): PATTERNS L116–L124 and the cycle-1 plan showed `Get-ADObject -Identity $id ... -SearchBase <first managed OU>`. That is an INVALID parameter-set mix — Microsoft Learn's `Get-ADObject` syntax lists `-SearchBase`/`-SearchScope` ONLY in the Filter and LdapFilter parameter sets; the Identity set has `-Partition`, NOT `-SearchBase`. `-Identity … -SearchBase` cannot bind ('Parameter set cannot be resolved using the specified named parameters') and would throw BEFORE any safety logic runs, breaking SAFE-10 (preview≡execute) and every write verb. The PLAN governs, not the snippet: DROP `-SearchBase`/`-SearchScope` from the Identity lookup. Scope is NOT weakened — it is enforced downstream in Test-AdmanTargetAllowed step (c) (component-boundary DN suffix, SAFE-07) on every target, identically for preview and execute.
    Private/Safety/Resolve-AdmanTarget.ps1: param([string[]]$Targets); foreach identity call `Get-ADObject -Identity $id -Server $script:Config.DC -Properties objectSid,objectClass,DistinguishedName,memberOf -ErrorAction Stop` — NO `-SearchBase`, NO `-SearchScope` (Identity parameter set has neither); return the array. -Server ALWAYS pinned to $script:Config.DC, -Properties exact. The function does NO policy filtering (that is Test-AdmanTargetAllowed — including the managed-OU scope check in step (c)); it only materializes canonical ADObjects. (SAFE-10: the gate calls this once.)
    Private/Safety/Test-AdmanTargetAllowed.ps1: param([ADObject]$Object); return @{Allowed=[bool]; Reason=[string]}. Order (D-02/Pitfall 3), ACCUMULATING reasons so layered signals are preserved (no early return on gMSA): (a) gMSA/sMSA objectClass pre-filter FIRST -> if objectClass contains either class, ADD reason 'gMSA/service account (objectClass)' and CONTINUE (do not return); (b) deny-list by RID -> `$rid = ([SecurityIdentifier]$Object.objectSid).Value.Split('-')[-1]`; if `$rid -in $script:DenyRids` add reason "deny-listed RID $rid" (D-05, never sAMAccountName); (c) managed-OU scope (Pitfall 5) -> normalize target DN + each root (ToLowerInvariant, trim, unescape); in-scope only if `$t -eq $root -or $t.EndsWith(',' + $root)` for SOME root; else add reason 'outside managed-OU scope' (NEVER a -like substring); (d) recursive protected membership ALWAYS runs after (a) for layering -> escape every DN/value BEFORE interpolation via Escape-AdmanLdapFilterValue (RFC 4515 assertion-value escaping — `\`→`\5c`, `*`→`\2a`, `(`→`\28`, `)`→`\29`, NUL→`\00`) so a special-char CN cannot break the filter (Addresses review concern C2-L1): `$dnEsc = Escape-AdmanLdapFilterValue -Value $Object.DistinguishedName`; build `$or` by escaping EACH group DN the same way (for each `$_` in `$script:ProtectedGroupDns`, `$g = Escape-AdmanLdapFilterValue -Value $_`; emit `(memberOf:1.2.840.113556.1.4.1941:=$g)`) then `-join ''`; and call `Get-ADObject -Server $DC -LDAPFilter "(&(distinguishedName=$dnEsc)(|$or))" -ErrorAction Stop` — bind via the LDAP-filter parameter set ONLY: the target's DN is ANDed inside the filter with the ORed IN_CHAIN group clauses, and the Identity parameter set is NOT used on this call (PowerShell rejects an Identity+LDAPFilter mix with "Parameter set cannot be resolved" BEFORE the safety check runs, so they must never be combined on one invocation); on a hit add reason 'recursive member of protected group'. Do NOT read adminCount anywhere. Return @{ Allowed = ($reasons.Count -eq 0); Reason = ($reasons -join '; ') } — a gMSA target that is ALSO a nested protected-group member returns BOTH reasons (layered, RESEARCH:547,549). This function is called identically for preview and execute (SAFE-10). NOTE: the only forbidden AD parameter-set mix in this file is Identity+LDAPFilter on ONE Get-ADObject call (PowerShell rejects it with 'Parameter set cannot be resolved' before the safety check runs). The resolver (Resolve-AdmanTarget) uses the Identity set WITHOUT -SearchBase (Identity has -Partition only); this protected check uses the LdapFilter set with RFC-4515-escaped DN values; the two never combine on one invocation.
    Tests use the DN/SID matrix from VALIDATION (case/escaping/boundary/spoof) and the mocked AD module (00-01) — never a live domain.
  </action>
  <acceptance_criteria>
    - `Select-String -Path Private/Safety/Test-AdmanTargetAllowed.ps1 -Pattern '1.2.840.113556.1.4.1941'` >=1 (IN_CHAIN OID present); `Select-String -Pattern 'adminCount'` returns 0 (never used).
    - No invalid AD parameter-set mix in the protected check: tests/Safety.Protected.Tests.ps1 asserts (via AST of Private/Safety/Test-AdmanTargetAllowed.ps1) that the recursive-membership Get-ADObject call supplies `-LDAPFilter` and does NOT also supply `-Identity` on the same invocation (Identity and LDAPFilter are mutually exclusive parameter sets).
    - Layered gMSA refusal: tests/Safety.Protected.Tests.ps1 asserts the IN_CHAIN Get-ADObject call is still invoked when the gMSA pre-filter matches (no early return), and that a gMSA-and-nested target's Reason contains both 'objectClass' and 'recursive member'.
    - Scope check is component-boundary anchored: `Select-String -Path Private/Safety/Test-AdmanTargetAllowed.ps1 -Pattern "EndsWith\('\",\$root|EndsWith\(\\\",\\`$root" ` >=1 OR an equivalent `',' + $root` EndsWith form; and `Select-String -Pattern '\-like\s+"?\*'` returns 0 (no substring match).
    - Deny matches by RID: `Select-String -Path Private/Safety/Test-AdmanTargetAllowed.ps1 -Pattern "Split\('-'\)\[-1\]"` >=1; `Select-String -Pattern 'sAMAccountName'` returns 0.
    - gMSA pre-filter present and ordered before IN_CHAIN: `Select-String -Path Private/Safety/Test-AdmanTargetAllowed.ps1 -Pattern 'msDS-GroupManagedServiceAccount'` >=1 and 'msDS-ManagedServiceAccount' >=1.
    - Resolve-AdmanTarget binds a VALID parameter set (Addresses review concern C2-H1): tests/Safety.PreviewEqualsExecute.Tests.ps1 asserts via AST of Private/Safety/Resolve-AdmanTarget.ps1 that the Get-ADObject call supplying `-Identity` does NOT also bind `-SearchBase` or `-SearchScope` (the Identity set has neither; it has `-Partition`); and source asserts `-Server` and `-Properties` are still pinned (`Select-String -Path Private/Safety/Resolve-AdmanTarget.ps1 -Pattern '\-Server '` >=1, `-Properties ` >=1). The false '-SearchBase is valid in the Identity set' NOTE is GONE from the action above and must not be reintroduced.
    - LDAP-filter DN escaping present (Addresses review concern C2-L1): `Select-String -Path Private/Safety/Escape-AdmanLdapFilterValue.ps1 -Pattern 'Escape-AdmanLdapFilterValue'` >=1 and the helper maps `\`,`*`,`(`,`)`,NUL to `\5c \2a \28 \29 \00`; Test-AdmanTargetAllowed.ps1 calls it on the target DN and each group DN before building the -LDAPFilter (`Select-String -Path Private/Safety/Test-AdmanTargetAllowed.ps1 -Pattern 'Escape-AdmanLdapFilterValue'` >=1); special-char-CN fixtures in tests/Safety.Protected.Tests.ps1 (CN containing `( ) * \`) resolve/refuse safely and do NOT throw a malformed-filter error.
    - `Invoke-Pester -Path tests/Safety.Scope.Tests.ps1,tests/Safety.DenyList.Tests.ps1,tests/Safety.Protected.Tests.ps1,tests/Safety.PreviewEqualsExecute.Tests.ps1 -Output Detailed` exits 0; the spoof-DN and renamed-RID-500 negative fixtures are REFUSED; adminCount never consulted.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "Invoke-Pester -Path tests/Safety.Scope.Tests.ps1,tests/Safety.DenyList.Tests.ps1,tests/Safety.Protected.Tests.ps1,tests/Safety.PreviewEqualsExecute.Tests.ps1 -Output Minimal"</automated>
  </verify>
  <done>Resolver materializes canonical targets once; policy refuses scope-spoof / deny-listed-RID / recursive-protected / gMSA targets with precise reasons and never uses adminCount or name-matching; preview and execute share one array.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirmation + bulk-policy + gate-only AD write wrappers — SAFE-01/02/09</name>
  <files>Private/Safety/Confirm-AdmanAction.ps1, Private/Safety/Assert-AdmanBulkPolicy.ps1, Private/AD/Adman.AD.Write.ps1, tests/Safety.Confirm.Tests.ps1, tests/Safety.NoHardDelete.Tests.ps1</files>
  <behavior>
    - Test 1 (single y/n, SAFE-02): below threshold, Confirm-AdmanAction calls $PSCmdlet.ShouldProcess once with a message containing the count; default-No (a declined ShouldProcess throws 'Operator declined' and writes a 'Cancelled' audit record via the mocked Write-AdmanAudit).
    - Test 2 (typed-count bulk, SAFE-02): at/above threshold, Confirm-AdmanAction prompts via Read-Host and REFUSES (throws 'Confirmation failed') when the typed token does NOT exactly equal the count (-ceq); accepts ONLY an exact match; empty/wrong/off-by-one tokens all refuse (matrix: threshold-1, threshold, threshold+1, wrong, empty).
    - Test 3 (-Force bypass scope-correct, SAFE-02): with -Force (or $ConfirmPreference='None'), the prompt is skipped BUT Test-AdmanTargetAllowed/Assert-AdmanBulkPolicy were still enforced upstream (asserted in the gate test Task 3); here assert Confirm-AdmanAction returns without prompting when -Force is set.
    - Test 4 (no $Confirm read, SAFE-02 Pitfall 2): Confirm-AdmanAction source contains NO read of the automatic `$Confirm` variable (uses `$ConfirmPreference -eq 'None'` instead).
    - Test 5 (wrappers are the only writers, SAFE-09): each Adman.AD.Write.<Verb> wrapper declares SupportsShouldProcess ConfirmImpact='High', pins -Server, forwards -WhatIf:$WhatIfPreference -Confirm:$false; there is NO wrapper for the hard-delete verb; the wrappers cover exactly the 9 allow-listed verbs.
    - Test 6 (cap placeholder, SAFE-02/Phase-4): Assert-AdmanBulkPolicy reads bulk.maxCount and safety.bulkConfirmThreshold from $script:Config and returns them WITHOUT enforcing the cap in Phase 0 (a switch -EnforceCap exists but is not used; enforcement is Phase 4/BULK-02).
  </behavior>
  <read_first>
    - .planning/phases/00-foundation-safety-harness/00-PATTERNS.md (§Confirm-AdmanAction L238–L261 body L244–L258 — NOTE the comparison bug flagged below; §Adman.AD.Write.ps1 L366–L370; Shared: ShouldProcess boilerplate L460–L468; -Force idiom L470–L472)
    - .planning/phases/00-foundation-safety-harness/00-RESEARCH.md (Pattern 6 L451–L473; Pitfall 1 L534–L538; Pitfall 2 L540–L544; SAFE-09 allow-list L251–L253)
    - .planning/phases/00-foundation-safety-harness/00-CONTEXT.md (D-07 L67–L73)
    - Private/Safety/AdmanWriteVerbs.ps1 (Task 1 — the 9-verb allow-list), config/adman.schema.json (safety.bulkConfirmThreshold / bulk.maxCount, 00-02)
  </read_first>
  <action>
    IMPORTANT correction to the source snippet: PATTERNS L252 / RESEARCH L468 show `if ($token -ceq "$count") { throw ... }`, which INVERTS the logic (it would refuse when the operator types the correct count). Implement the CORRECT behavior: throw (refuse) when `$token -cne "$count"` — i.e., accept ONLY an exact-count match. Do not copy the inverted comparison.
    Private/Safety/Confirm-AdmanAction.ps1: declare `[CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')]` so -WhatIf/-Confirm flow from the gate. param([string]$Verb, $Targets, [string]$CorrelationId, [switch]$Force). `$count = @($Targets).Count`; `$threshold = [int]$script:Config.safety.bulkConfirmThreshold` (default 5). If `-not $Force -and ($ConfirmPreference -ne 'None')`: when `$count -ge $threshold` -> `$token = Read-Host "Type the exact count ($count) to $Verb these $count objects"`; if (`$token -cne "$count"`) throw "Confirmation failed: expected $count. Refused." (case-sensitive exact match; no Enter-to-accept). Else (below threshold) -> if (-not `$PSCmdlet.ShouldProcess("$count object(s)", $Verb)`) { write a 'Cancelled' audit record (Write-AdmanAudit -Result 'Cancelled'); throw 'Operator declined.' }. NEVER read `$Confirm` (StrictMode); test suppression via `$ConfirmPreference -eq 'None'`. The -Force/-Confirm:$false path skips ONLY this prompt.
    Private/Safety/Assert-AdmanBulkPolicy.ps1: param([int]$Count, [switch]$EnforceCap). Read `$cap = [int]$script:Config.bulk.maxCount` and `$threshold = [int]$script:Config.safety.bulkConfirmThreshold`; emit `Write-PSFMessage -Level Debug` "count=$Count cap=$cap threshold=$threshold". In Phase 0 do NOT enforce (CAP enforcement is Phase 4/BULK-02): only when `-EnforceCap` is passed AND `$Count -gt $cap` throw "Bulk count $Count exceeds cap $cap". Return @{Cap=$cap; Threshold=$threshold}. (Phase 4 will pass -EnforceCap; Phase 0/2 single-object flows never hit the cap.)
    Private/AD/Adman.AD.Write.ps1: define one function per allow-listed verb, named exactly `Adman.AD.Write.Disable-ADAccount`, `.Enable-ADAccount`, `.Move-ADObject`, `.Set-ADUser`, `.Set-ADComputer`, `.Set-ADAccountPassword`, `.Unlock-ADAccount`, `.Add-ADGroupMember`, `.Remove-ADGroupMember`. Each: `[CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')]`; pin `-Server $script:Config.DC`; forward `-WhatIf:$WhatIfPreference -Confirm:$false`; call the matching real AD cmdlet (`Disable-ADAccount`/`Enable-ADAccount`/`Move-ADObject`/`Set-ADUser`/`Set-ADComputer`/`Set-ADAccountPassword`/`Unlock-ADAccount`/`Add-ADGroupMember`/`Remove-ADGroupMember`). There is NO wrapper for the hard-delete verb (SAFE-09) — do not create one and do not reference it by literal name here. These wrappers are the ONLY code in the repo that names the real AD write cmdlets; the 00-01 AST guard scopes to Public/, so these Private wrappers are expected and intended to contain them.
  </action>
  <acceptance_criteria>
    - Confirm-AdmanAction uses the CORRECT comparison: `Select-String -Path Private/Safety/Confirm-AdmanAction.ps1 -Pattern '\-cne\s+"?\$count|\$token\s+-cne'` >=1 (refuse on mismatch); and the inverted `-ceq "$count"` throw form is NOT present in the throw path.
    - No `$Confirm` automatic-variable read: `Select-String -Path Private/Safety/Confirm-AdmanAction.ps1 -Pattern '\$Confirm\b'` returns 0; `$ConfirmPreference -eq 'None'` present (or `-ne 'None'`).
    - Confirm-AdmanAction declares SupportsShouldProcess ConfirmImpact='High' (`Select-String -Pattern "SupportsShouldProcess" >=1` and `ConfirmImpact` >=1) and exposes a -Force switch.
    - Adman.AD.Write.ps1 contains exactly 9 functions matching `Adman.AD.Write.<Verb>` for the allow-list; `Select-String -Path Private/AD/Adman.AD.Write.ps1 -Pattern 'SupportsShouldProcess'` count >= 9; each forwards `-Confirm:$false` (`Select-String -Pattern '\-Confirm:\$false'` >=9); `Select-String -Pattern '\-Server ' ` >=9.
    - The hard-delete verb has no wrapper: the function-name list in Adman.AD.Write.ps1 equals the 9 allow-listed verbs (assert by comparing to Get-AdmanAllowedWriteVerbs in the test).
    - `Invoke-Pester -Path tests/Safety.Confirm.Tests.ps1,tests/Safety.NoHardDelete.Tests.ps1 -Output Detailed` exits 0; typed-count matrix (threshold-1/threshold/threshold+1/wrong/empty) behaves correctly; -Force skips the prompt.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "Invoke-Pester -Path tests/Safety.Confirm.Tests.ps1,tests/Safety.NoHardDelete.Tests.ps1 -Output Minimal"</automated>
  </verify>
  <done>Scaled confirmation refuses on mismatch and never reads $Confirm; -Force skips only the prompt; AD write wrappers are the sole, gate-only callers of real AD write cmdlets, exactly the 9 allow-listed verbs with no hard-delete wrapper; bulk cap is a Phase-0 placeholder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The mutation gate Invoke-AdmanMutation (THE GATE) — SAFE-08</name>
  <files>Private/Safety/Invoke-AdmanMutation.ps1, tests/Safety.GateOrder.Tests.ps1</files>
  <behavior>
    - Test 1 (fixed order, SAFE-08/04): with all collaborators mocked, Invoke-AdmanMutation calls Resolve-AdmanTarget -> Test-AdmanTargetAllowed (per target) -> Assert-AdmanBulkPolicy -> Confirm-AdmanAction -> Write-AdmanAudit(Result='PENDING') -> Adman.AD.Write.<Verb> -> Write-AdmanAudit(Result='Success') in that order (assert via a call-sequence log).
    - Test 2 (allow-list enforced, SAFE-09): the gate's ValidateSet equals Get-AdmanAllowedWriteVerbs (9 verbs); invoking with a non-allow-listed verb (incl. the hard-delete verb) is rejected by parameter validation before any AD call.
    - Test 3 (refusal logged + skipped, SAFE-05/06/07): when Test-AdmanTargetAllowed returns Allowed=$false for a target, the gate writes a 'Refused' audit record with the Reason and does NOT call the AD write wrapper for that target.
    - Test 4 (PENDING before write, SAFE-04 ordering): Write-AdmanAudit(Result='PENDING') is called BEFORE Adman.AD.Write.<Verb>; if the (mocked) PENDING write throws, the AD write wrapper is NEVER invoked (the refusal happens before the mutation).
    - Test 5 (-WhatIf flows, SAFE-01): with -WhatIf, the gate still resolves/allows/confirms and writes PENDING+OUTCOME audit with whatIf=$true, and forwards -WhatIf to the inner write (assert the wrapper received -WhatIf:$true and AD was not actually changed — mock wrapper records the switch).
    - Test 6 (canonical result): the gate returns an object with Action/Targets/Denied/Succeeded/Failed/WhatIf/CorrelationId; CorrelationId is a non-empty guid shared by the PENDING and OUTCOME audit records.
  </behavior>
  <read_first>
    - .planning/phases/00-foundation-safety-harness/00-PATTERNS.md (§Invoke-AdmanMutation L67–L107 signature L73–L83 + pipeline L86–L102 + sequencing rules L104–L107)
    - .planning/phases/00-foundation-safety-harness/00-RESEARCH.md (Pattern 1 L231–L304 body L247–L273; Anti-Patterns L509–L517)
    - .planning/phases/00-foundation-safety-harness/00-CONTEXT.md (D-03 L37–L42 audit ordering; D-07 L67–L73)
    - Private/Safety/Resolve-AdmanTarget.ps1, Test-AdmanTargetAllowed.ps1, Confirm-AdmanAction.ps1, Assert-AdmanBulkPolicy.ps1, AdmanWriteVerbs.ps1 (Tasks 1–2), Private/AD/Adman.AD.Write.ps1 (Task 2)
  </read_first>
  <action>
    Implement Private/Safety/Invoke-AdmanMutation.ps1 per PATTERNS L73–L102 / RESEARCH L247–L273, preserving the EXACT order:
      `[CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')]`
      param( [Parameter(Mandatory)][ValidateSet(<the 9 allow-listed verbs from Get-AdmanAllowedWriteVerbs>)] [string]$Verb, [Parameter(Mandatory)][string[]]$Targets, [hashtable]$Parameters = @{}, [switch]$Force )
      Body:
        `$cid = [guid]::NewGuid().ToString()`
        `$resolved = Resolve-AdmanTarget -Targets $Targets`                       # SAFE-10: ONE resolver, once
        `$allowed = @()`; foreach (`$t in `$resolved) { `$d = Test-AdmanTargetAllowed -Object `$t; if (-not `$d.Allowed) { Write-AdmanAudit -CorrelationId `$cid -Verb `$Verb -Target `$t -Result 'Refused' -Reason `$d.Reason -WhatIf:$WhatIfPreference } else { `$allowed += `$t } }   # deny/protected/scope refusals logged, skipped
        if (`$allowed.Count -eq 0) { return [pscustomobject]@{Action=`$Verb; Targets=`$Targets; Denied=`$resolved.Count; Succeeded=0; Failed=0; WhatIf=[bool]$WhatIfPreference; CorrelationId=`$cid} }
        Assert-AdmanBulkPolicy -Count `$allowed.Count                             # cap placeholder (Phase 4 enforces) + threshold source
        Confirm-AdmanAction -Verb `$Verb -Targets `$allowed -CorrelationId `$cid -Force:`$Force   # SAFE-02; honors -WhatIf via SupportsShouldProcess
        Write-AdmanAudit -CorrelationId `$cid -Verb `$Verb -Targets `$allowed -Result 'PENDING' -WhatIf:$WhatIfPreference   # 00-05 writer THROWS on failure => refusal BEFORE the write below
        & "Adman.AD.Write.`$Verb" -Objects `$allowed @Parameters -WhatIf:$WhatIfPreference -Confirm:`$false   # the ONE real write; no per-object re-prompt
        Write-AdmanAudit -CorrelationId `$cid -Verb `$Verb -Targets `$allowed -Result 'Success' -WhatIf:$WhatIfPreference   # OUTCOME best-effort
        return [pscustomobject]@{Action=`$Verb; Targets=`$allowed.DistinguishedName; Denied=(`$resolved.Count-`$allowed.Count); Succeeded=`$allowed.Count; Failed=0; WhatIf=[bool]$WhatIfPreference; CorrelationId=`$cid}
      Rules: the gate is Private/ and NOT exported (already excluded from FunctionsToExport since 00-01). It never calls an AD write cmdlet directly — only via `& "Adman.AD.Write.$Verb"`. The PENDING audit call is intentionally placed AFTER Confirm-AdmanAction and BEFORE the write (write-ahead reservation; the 00-05 writer makes the throw fail-closed). -Force is forwarded to Confirm-AdmanAction only (prompt bypass); deny/protected/scope/cap are not flag-bypassable.
    The ValidateSet must be generated from / equal to Get-AdmanAllowedWriteVerbs (Task 1) so the allow-list, the gate, and the wrappers cannot drift (key_link). If PowerShell requires a literal ValidateSet, hard-code the same 9 verbs and add a test (Test 2) asserting ValidateSet == Get-AdmanAllowedWriteVerbs.
    tests/Safety.GateOrder.Tests.ps1 mocks ALL collaborators (Resolve-AdmanTarget, Test-AdmanTargetAllowed, Assert-AdmanBulkPolicy, Confirm-AdmanAction, Write-AdmanAudit, and each Adman.AD.Write.* wrapper) and asserts order + behavior — the gate is green WITHOUT the real audit writer (00-05) and WITHOUT a live domain.
  </action>
  <acceptance_criteria>
    - The gate file exists at Private/Safety/Invoke-AdmanMutation.ps1 and is NOT in adman.psd1 FunctionsToExport (`Select-String -Path adman.psd1 -Pattern 'Invoke-AdmanMutation'` returns 0).
    - Fixed order present in source: `Select-String -Path Private/Safety/Invoke-AdmanMutation.ps1 -Pattern "Resolve-AdmanTarget","Test-AdmanTargetAllowed","Assert-AdmanBulkPolicy","Confirm-AdmanAction","Result 'PENDING'","Adman.AD.Write","Result 'Success'"` each >=1, appearing in that textual order.
    - PENDING is before the write: in source, the `Result 'PENDING'` Write-AdmanAudit call textually precedes the `& "Adman.AD.Write.`$Verb"` line (source assertion; the test enforces runtime order).
    - No direct AD write cmdlet in the gate: `Select-String -Path Private/Safety/Invoke-AdmanMutation.ps1 -Pattern 'Set-ADUser|Set-ADComputer|Disable-ADAccount|Enable-ADAccount|Move-ADObject|Set-ADAccountPassword|Unlock-ADAccount|Add-ADGroupMember|Remove-ADGroupMember|New-ADUser|New-ADComputer'` returns 0 (the gate only calls Adman.AD.Write.*).
    - ValidateSet equals the allow-list: `Invoke-Pester` Test 2 asserts `(Get-AdmanAllowedWriteVerbs)` equals the gate's [ValidateSet] values.
    - `Invoke-Pester -Path tests/Safety.GateOrder.Tests.ps1 -Output Detailed` exits 0; order, refusal-skip, PENDING-throw-blocks-write, -WhatIf-flow, and canonical-result tests all green with collaborators mocked (no real audit writer, no domain).
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "Invoke-Pester -Path tests/Safety.GateOrder.Tests.ps1 -Output Minimal; Invoke-ScriptAnalyzer -Path Private/Safety/Invoke-AdmanMutation.ps1 -Settings PSScriptAnalyzerSettings.psd1"</automated>
  </verify>
  <done>The non-exported gate runs the exact Resolve->Allow->BulkPolicy->Confirm->Audit(PENDING)->Write->Audit(OUTCOME) order, enforces the 9-verb allow-list, logs refusals and skips denied targets, proves PENDING-before-write and -WhatIf flow under full mocking, and is lint-clean.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase (plan 00-04) produces

Functions (new, non-exported/private): `Resolve-AdmanTarget`, `Test-AdmanTargetAllowed`, `Escape-AdmanLdapFilterValue` (RFC 4515 LDAP assertion-value escaping), `Get-AdmanAllowedWriteVerbs` (in AdmanWriteVerbs.ps1), `Confirm-AdmanAction`, `Assert-AdmanBulkPolicy`, `Invoke-AdmanMutation` (THE GATE), and the nine wrappers `Adman.AD.Write.{Disable-ADAccount,Enable-ADAccount,Move-ADObject,Set-ADUser,Set-ADComputer,Set-ADAccountPassword,Unlock-ADAccount,Add-ADGroupMember,Remove-ADGroupMember}`.
Functions (exported): none this plan (write verbs arrive Phase 2).
Files: `Private/Safety/Resolve-AdmanTarget.ps1`, `Private/Safety/Test-AdmanTargetAllowed.ps1`, `Private/Safety/Escape-AdmanLdapFilterValue.ps1`, `Private/Safety/AdmanWriteVerbs.ps1`, `Private/Safety/Confirm-AdmanAction.ps1`, `Private/Safety/Assert-AdmanBulkPolicy.ps1`, `Private/Safety/Invoke-AdmanMutation.ps1`, `Private/AD/Adman.AD.Write.ps1`, `tests/Safety.Scope.Tests.ps1`, `tests/Safety.DenyList.Tests.ps1`, `tests/Safety.Protected.Tests.ps1`, `tests/Safety.Confirm.Tests.ps1`, `tests/Safety.PreviewEqualsExecute.Tests.ps1`, `tests/Safety.NoHardDelete.Tests.ps1`, `tests/Safety.GateOrder.Tests.ps1`.
Contracts established: 9-verb allow-list (Get-AdmanAllowedWriteVerbs) = gate ValidateSet = AD-wrapper set; complement = Get-AdmanBannedWriteVerbs (00-01). Confirm threshold = `safety.bulkConfirmThreshold` (default 5). Cap = `bulk.maxCount` (placeholder; enforced Phase 4). OID 1.2.840.113556.1.4.1941 used for recursive membership.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller input (DN/identity) → policy | DNs/identities are attacker-influenceable; scope/deny must canonicalize and anchor, never substring-match. |
| policy decision → AD write | The gate is the single crossing where a refused target must be structurally unable to reach an AD write cmdlet. |
| preview → execute | If preview re-resolves, it can show a different set than executes (preview lies). |
| operator → confirmation | Bulk y-y-y muscle memory is the crossing where a mistyped/absent confirmation must fail closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-02 | Tampering | DN spoof escapes managed-OU scope via substring/prefix match | high | mitigate | Component-boundary-anchored suffix test (`$t -eq $root` or `$t.EndsWith(','+$root)`) after normalize; no `-like "*root*"` (Task 1). Verified by acceptance criteria: spoof-DN negative fixture REFUSED, zero `-like "*"` in source. (SAFE-07) |
| T-00-03 | Elevation of Privilege | Mutation of a protected/Tier-0 account incl. via nesting or gMSA | high | mitigate | gMSA objectClass pre-filter first, then one DC-side IN_CHAIN (1.2.840.113556.1.4.1941) bound to the target over ProtectedGroupDns; never adminCount (Task 1). Every DN/value in that IN_CHAIN LDAP filter is RFC-4515-escaped via Escape-AdmanLdapFilterValue so a special-char CN fails closed (false refusal) rather than throwing a malformed filter (C2-L1). Verified by: nested-DA refused, gMSA refused even when not nested, source has OID + zero adminCount + Escape-AdmanLdapFilterValue used on the DN/group values, special-char-CN fixtures safe. (SAFE-06) |
| T-00-06 | Tampering | -WhatIf preview shows a different target set than execute (preview lies) | high | mitigate | Single Resolve-AdmanTarget called once; identical array reference to preview + execute; Test-AdmanTargetAllowed runs in both modes (Tasks 1 & 3). The resolver uses the Identity parameter set WITHOUT -SearchBase (Identity has -Partition only); an invalid -Identity…-SearchBase mix would throw 'Parameter set cannot be resolved' before preview/execute and is blocked by the AST acceptance (C2-H1). Verified by Test 6 (reference/count equal), gate Test 5, and resolver Test 8. (SAFE-10) |
| T-00-08 | Tampering | Deny-list bypass via renamed built-in Administrator (RID-500) or localized name | high | mitigate | Match by objectSid RID against $script:DenyRids, never sAMAccountName (Task 1). Verified by: renamed-RID-500 negative fixture REFUSED, zero sAMAccountName in source. (SAFE-05) |
| T-00-01 | Tampering | A write path bypasses Invoke-AdmanMutation | high | mitigate | Gate is the sole caller of Adman.AD.Write.* which are the sole callers of real AD write cmdlets; gate non-exported; ValidateSet = allow-list (Tasks 2 & 3). Verified by: gate source has zero direct AD write cmdlets; 00-01 AST guard + 00-05 exit proof cover Public/. (SAFE-08) |
| T-00-16 | Tampering | A hard-delete verb ships (irreversible object removal) | high | mitigate | Allow-list excludes the hard-delete verb; no Adman.AD.Write wrapper for it; ValidateSet rejects it (Tasks 1–3). Verified by: allow-list == 9 verbs, wrapper count == 9, Test 2 rejects a non-allow-listed verb. (SAFE-09) |
| T-00-10 | Tampering | Bulk confirmation defeated by y-y-y muscle memory or empty/Enter accept | medium | mitigate | At/above threshold require exact-count token (-cne refuses mismatch), case-sensitive, no Enter-to-accept; below threshold default-No ShouldProcess (Task 2). Verified by typed-count matrix (off-by-one/wrong/empty refuse). (SAFE-02) |
</threat_model>

<verification>
- `powershell -NoProfile -Command "Invoke-Pester -Path tests/Safety.Scope.Tests.ps1,tests/Safety.DenyList.Tests.ps1,tests/Safety.Protected.Tests.ps1,tests/Safety.PreviewEqualsExecute.Tests.ps1,tests/Safety.Confirm.Tests.ps1,tests/Safety.NoHardDelete.Tests.ps1,tests/Safety.GateOrder.Tests.ps1 -Output Minimal"` exits 0.
- `powershell -NoProfile -Command "Invoke-ScriptAnalyzer -Path Private/Safety,Private/AD -Recurse -Settings PSScriptAnalyzerSettings.psd1"` exits 0.
- Source assertions: gate has zero direct AD write cmdlets; scope check has zero `-like "*"`; policy has zero `adminCount` and zero `sAMAccountName`; Confirm-AdmanAction has zero `$Confirm` read and uses `-cne` for the token; Resolve-AdmanTarget's `-Identity` Get-ADObject call binds NO `-SearchBase`/`-SearchScope` (AST — C2-H1); Test-AdmanTargetAllowed escapes the target DN and each group DN via Escape-AdmanLdapFilterValue before the IN_CHAIN -LDAPFilter (C2-L1).
</verification>

<success_criteria>
- Resolver is single and shared by preview+execute; scope/deny/protected refuse with precise reasons using SID/RID + component-boundary DN + IN_CHAIN, never names/adminCount/substring.
- Confirmation scales to blast radius and fails closed on mismatch; -Force skips only the prompt.
- The gate runs the fixed order, enforces the allow-list, logs refusals, proves PENDING-before-write and -WhatIf flow under full mocking, and is lint-clean.
- No hard-delete verb or wrapper exists; the 9-verb allow-list is the single source for gate + wrappers.
- All SAFE-05/06/07/10 negative fixtures (spoof DN, renamed RID-500, nested DA, gMSA-not-nested) are REFUSED.
</success_criteria>

<output>
Create `.planning/phases/00-foundation-safety-harness/00-04-SUMMARY.md` when done.
</output>
