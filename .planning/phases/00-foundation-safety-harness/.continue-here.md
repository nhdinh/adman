---
context: phase
phase: 00-foundation-safety-harness
activity: UAT / verify-work (lab integration testing of the safety gate)
status: in_progress
last_updated: 2026-07-14T04:27:03.686Z
---

# Resuming Phase 00 UAT — Lab Integration Testing

<current_state>
Phase 00 is EXECUTED (all 5 plans complete). We are in `/gsd-verify-work 0` running human UAT against a real lab domain. This session was the FIRST time the `-Tag Integration` tests actually executed against a live DC (they were `blocked` on every prior run), which surfaced three latent defects. Session paused at 76% context after fixing #1 and #2 and validating the Protected test live, with #3 awaiting a product decision.

UAT file: `.planning/phases/00-foundation-safety-harness/00-UAT.md` (full detail in its Gaps section).
</current_state>

<completed_work>
- UAT test 1 (approve PSFramework/Pester/PSScriptAnalyzer install): **pass** (prior session).
- Lab environment brought up for testing: host `HUNGDINH-PC` is joined to `pgs.ptsc.com.vn` (NOT `lab.local`), so the AD module's ambient auth was rejected. Fixed via `runas /netonly /user:LAB\Administrator powershell`. Installed pinned PSFramework 1.14.457 (manifest `RequiredVersion`, exact). Provisioned lab OU + fixtures manually.
- **Issue #1 FIXED** (quick task 260714-ek6, commits feac682 + bc3c6d7): integration tests called the deliberately non-exported `Invoke-AdmanMutation` directly → wrapped all 4 call sites in `& (Get-Module adman) { param($t) Invoke-AdmanMutation ... } <dn>`, matching the passing unit-test pattern in `tests/Safety.GateOrder.Tests.ps1`.
- **Issue #2 FIXED** (quick task 260714-fbx, commits 259f4d9 + 385db4e): integration tests only `Import-Module -Force` and never initialized the module (`adman.psm1` import is side-effect-free; `$script:Config=@{}`) → tests now call `Initialize-Adman` against a `$TestDrive` lab config. Key facts: `AdmanProtectedGroup` must be a **DN** (resolved live to Domain Admins) or the nested-admin refusal is vacuous; new required env var `ADMAN_TEST_DC` mirrors the `ADMAN_TEST_OU` gate.
- **VALIDATED LIVE:** `Safety.Protected.Integration.Tests.ps1` **PASSES** — nested-admin refused + `Refused` audit record with precise reason = **SAFE-06 proven end-to-end**. gMSA/RID-500 `Inconclusive` (fixtures absent — acceptable, not a failure).
- Issue #3 found and fully documented (commit 6b9da8f).
</completed_work>

<remaining_work>
1. **DECIDE issue #3 fork** (blocking):
   - **(a) RECOMMENDED** — the WhatIf test targets the child USER fixtures under the OU, matching the gate's resolve-identity-as-is semantics. Minimal, correct.
   - (b) — make the gate expand OU targets into child objects. Product change / scope creep; OU-expansion is NOT a stated phase-00 requirement.
2. Apply the WhatIf test fix (option a). Optionally harden `Private/Safety/Test-AdmanTargetAllowed.ps1` step (b) (line ~52) to skip the RID-deny check when `objectSid` is absent (OUs/containers have none) — robustness regardless of (a)/(b).
3. Re-run both integration files on the lab to confirm green.
4. Flip UAT test 2 `issue` → `pass` once green. Then test 3 (DPAPI cross-machine re-prompt, CONF-04) — needs a second machine/user; can be marked blocked/skipped. Then close phase 00 UAT (status complete) and advance.
</remaining_work>

<decisions_made>
- Issue #2 → **Option A** (tests call `Initialize-Adman` end-to-end) over module-scope state injection. Rationale: true end-to-end, also exercises the init path; avoids re-deriving Config/DenyRids/ProtectedGroupDns/DomainSid by hand.
- Issue #3 → **recommend option (a)** (test targets child users). Rationale: OU-expansion is not a phase-00 requirement; the gate deliberately resolves the given identity as-is.
</decisions_made>

<blockers>
- Issue #3 needs the (a)/(b) product decision before coding. Workaround: proceed with (a).
- Test 3 (DPAPI) needs a second machine/user. Workaround: mark blocked/skipped.
- Lab + `D:\adman` are reachable ONLY from the user's interactive `runas /netonly` PS7 session; the agent CANNOT reach `D:` or the lab DC. All lab runs are manual.
</blockers>

## Required Reading (in order)
1. `.planning/phases/00-foundation-safety-harness/00-UAT.md` — authoritative UAT state; Gaps section has all three issues with root causes and resolutions.
2. `tests/Safety.WhatIf.Integration.Tests.ps1` — the file to fix for issue #3 (targets OU DN at the gated WhatIf call).
3. `Private/Safety/Test-AdmanTargetAllowed.ps1` — step (b) line ~52 crashes on null objectSid for OU targets; step (c)/(d) enforce scope + protected membership.
4. `Private/Safety/Resolve-AdmanTarget.ps1` — binds `-Identity` only; resolves the given identity as-is, does NOT enumerate OU children (the crux of issue #3).
5. `tests/Safety.GateOrder.Tests.ps1` — the correct patterns: module-scope invocation + `Set-AdmanSafetyState` config injection.

## Critical Anti-Patterns (do NOT repeat)
- **Never call a non-exported (Private) function directly in a test** → invoke via `& (Get-Module adman) { param($t) ... } $args`, passing test-scope values in as params (`$script:` test vars are NOT visible inside module scope).
- **Import-Module is side-effect-free by design** — tests that exercise the gate MUST run `Initialize-Adman` (or inject `$script:Config` + `$script:DenyRids` + `$script:ProtectedGroupDns` + `$script:DomainSid`) first, or every `$script:Config.*` access throws under `Set-StrictMode -Version Latest`.
- **Do NOT touch production code for test-setup bugs** — issues #1/#2 were test-file defects; the gate itself is proven by the unit suite (138 green).

## Infrastructure State
- Repo (git-tracked): `C:\Users\nhdinh\dev\adman` (master). All commits here.
- Test run checkout: `D:\adman` — NOT reachable from the agent; `git pull` there after each C: commit.
- Lab DC: `lab-dc01.lab.local` (192.168.10.10), domain NC `DC=lab,DC=local`, functional level Windows2016.
- Lab auth: `runas /netonly /user:LAB\Administrator powershell`; verify with `Get-ADRootDSE -Server lab-dc01.lab.local`.
- Required env for integration tests: `ADMAN_TEST_OU` (user uses `OU=Adman-test,DC=lab,DC=local`) and `ADMAN_TEST_DC` (`lab-dc01.lab.local`).
- PS7 path fix: prepend `C:\Users\nhdinh\OneDrive\Documents\WindowsPowerShell\Modules` to `$env:PSModulePath` (Pester 6 lives there).
- `node` is NOT on PATH for gsd-tools; use `"/c/Program Files/nodejs/node.exe" "$HOME/.claude/gsd-core/bin/gsd-tools.cjs"`.

<context>
UAT is 1 pass (test 1) + 1 partial (test 2: SAFE-06 proven, SAFE-01/10 blocked on issue #3) + 1 pending (test 3 DPAPI). The two quick tasks are committed; only the issue-#3 fix and final lab validation remain before phase 00 UAT can close. Worktree isolation is broken on this host — run any executor sequentially on master, no isolation.
</context>

<next_action>
Start with `/gsd-verify-work 0` to resume the UAT session. Confirm issue #3 option (a), then `/gsd-quick` to apply the WhatIf test fix (target child user fixtures + optional objectSid-absent hardening), `git pull` on D:, re-run the two integration files on the lab, flip test 2 to pass, then handle test 3 and close phase 00.
</next_action>
