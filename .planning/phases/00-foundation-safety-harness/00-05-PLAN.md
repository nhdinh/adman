---
phase: 00-foundation-safety-harness
plan: 05
type: execute
wave: 5
depends_on:
  - 00-04
files_modified:
  - Private/Audit/Write-AdmanAudit.ps1
  - Private/Audit/AdmanAuditIO.ps1
  - Private/Audit/Find-AdmanAuditOrphans.ps1
  - Private/Foundation/Get-AdmanRecoveryPosture.ps1
  - tests/Audit.Schema.Tests.ps1
  - tests/Audit.FailClosed.Tests.ps1
  - tests/Audit.OrphanSweep.Tests.ps1
  - tests/Safety.WhatIf.Integration.Tests.ps1
  - tests/Safety.Protected.Integration.Tests.ps1
  - tests/RecoveryPosture.Tests.ps1
autonomous: true
requirements:
  - SAFE-03
  - SAFE-04
  - SAFE-08
  - SAFE-09
user_setup: []

must_haves:
  truths:
    - "Write-AdmanAudit is the ONLY audit writer and is synchronous: via small private seams it takes mutex Global\adman-audit, opens .store/audit/audit-YYYYMMDD.jsonl (Append/Write/Read-share), writes a JSON-lines record (ConvertTo-Json -Compress -Depth 5), and Flush(\$true) to durable media before releasing the mutex (SAFE-03); the seams (New-AdmanAuditMutex/Open-AdmanAuditStream/Write-AdmanEventLog) are the mockable surface over the .NET mutex/file/eventlog calls."
    - "Audit is fail-closed by write-ahead reservation: a PENDING record is durably flushed BEFORE the mutation, and if that PENDING write fails for ANY reason (path missing + auto-create failed, ACL denial, ERROR_DISK_FULL 112, sharing violation, unreachable path) the function THROWS before AD is touched — that throw IS the refusal (SAFE-04, T-00-04)."
    - "An OUTCOME-write failure (after a successful mutation) does NOT roll back AD; it escalates to the Windows Event Log (best-effort, source registered by Initialize-Adman), a loud UI warning, and \$script:AuditDegraded=\$true, because AD object-state rollback is unreliable (SAFE-04, T-00-17)."
    - "The audit record schema contains exactly the D-03 field set (tsUtc, who, userSid, what, scope, target(s) with DN+SID+objectClass, count, whatIf, result, reason, correlationId, host, psEdition, moduleVersion) and contains NO secret field (no password/secret/credential/key/token) — proven by a static test (SAFE-03/CONF-05, T-00-05)."
    - "Audit integrity is verifiable: a startup/ondemand orphan sweep correlates PENDING↔OUTCOME by correlationId and surfaces PENDING records with no matching OUTCOME (the detection seam for an OUTCOME-write gap), never silently dropping it (D-03)."
    - "Recovery posture (Recycle Bin / Forest Functional Level / tombstone) is reported read-only as a warning and does NOT gate operations, because the tool ships no hard-delete verb (SAFE-09); this feeds the MENU-05 probe and Phase-1 RPT-07 rather than enforcing a block."
    - "Phase exit gate: the full mocked Unit suite is green, Invoke-ScriptAnalyzer is clean (incl. PSUseShouldProcessForStateChangingFunctions), and the 00-01 SAFE-08 AST guard + custom PSSA rule pass against Public/ (no exported function calls AD write cmdlets) (SAFE-08/09 exit gate)."
  artifacts:
    - "Private/Audit/Write-AdmanAudit.ps1 (the ONLY audit writer; synchronous write-ahead JSONL; throws on PENDING failure, escalates on OUTCOME failure)"
    - "Private/Audit/Find-AdmanAuditOrphans.ps1 (PENDING↔OUTCOME correlation sweep by correlationId)"
    - "Private/Foundation/Get-AdmanRecoveryPosture.ps1 (read-only Recycle Bin / FFL / tombstone; warning-only)"
    - "tests/Audit.Schema.Tests.ps1, tests/Audit.FailClosed.Tests.ps1, tests/Audit.OrphanSweep.Tests.ps1, tests/RecoveryPosture.Tests.ps1 (Unit, mocked)"
    - "tests/Safety.WhatIf.Integration.Tests.ps1, tests/Safety.Protected.Integration.Tests.ps1 (lab-only, -Tag Integration; never run in the Unit suite)"
  key_links:
    - "Write-AdmanAudit's PENDING-throw contract is EXACTLY what Invoke-AdmanMutation (00-04) relies on for SAFE-04; changing the throw condition or ordering silently breaks fail-closed (the 00-04 gate Test 4 + this plan's FailClosed test pin both ends)."
    - "Test-AdmanAuditWritable (00-03) and Write-AdmanAudit (here) MUST use the same AuditDir + open-append+Flush(true) contract so the startup admission matches the runtime writer; the probe writes ZERO bytes because Find-AdmanAuditOrphans parses every line as strict JSONL (a non-JSON marker would break orphan parsing)."
    - "The audit schema's no-secret guarantee is the enforcement point for CONF-05 across the whole tool; the schema test (Audit.Schema.Tests.ps1) is the single place that bans secret field names."
    - "The integration tests are gated -Tag Integration and run ONLY against a disposable lab OU; the Unit suite (default Pester filter) must exclude them so the default run never touches a domain."
---

<objective>
Implement the fail-closed, append-only audit writer (the only writer, synchronous, write-ahead), the audit-integrity orphan sweep, the read-only recovery-posture reporter, and then close the phase exit gate: full mocked Unit suite green + ScriptAnalyzer clean + the SAFE-08/09 AST guard proven against Public/ (SAFE-03/04, with SAFE-08/09 as the phase exit gate).

Purpose: SAFE-04 ("refuse the destructive action if the audit record cannot be written") is only real if the audit write is a write-ahead reservation that throws before AD is touched; this plan builds that writer and proves the throw ordering, then runs the consolidated phase proof. It is the last Phase-0 plan and produces the green-suite + lint-clean evidence the roadmap's Phase-0 success criteria require.

Output: Write-AdmanAudit + integrity + recovery-posture helpers, their Unit tests, two lab-only integration test files, and a recorded green exit-gate run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-foundation-safety-harness/00-CONTEXT.md
@.planning/phases/00-foundation-safety-harness/00-RESEARCH.md
@.planning/phases/00-foundation-safety-harness/00-PATTERNS.md
@.planning/phases/00-foundation-safety-harness/00-VALIDATION.md
@.planning/phases/00-foundation-safety-harness/00-01-PLAN.md
@.planning/phases/00-foundation-safety-harness/00-03-PLAN.md
@.planning/phases/00-foundation-safety-harness/00-04-PLAN.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write-AdmanAudit — synchronous write-ahead JSONL, fail-closed (SAFE-03/04)</name>
  <files>Private/Audit/Write-AdmanAudit.ps1, tests/Audit.Schema.Tests.ps1, tests/Audit.FailClosed.Tests.ps1</files>
  <behavior>
    - Test 1 (schema shape, SAFE-03): a written record, parsed back from the .jsonl, contains keys tsUtc, who, userSid, what, scope, target, targets, count, whatIf, result, reason, correlationId, host, psEdition, moduleVersion; who = "$env:USERDOMAIN\$env:USERNAME"; userSid = current WindowsIdentity User.Value; result ∈ {PENDING, Success, Failure, Refused, Cancelled}.
    - Test 2 (no secrets, SAFE-03/CONF-05): the record contains NO key matching /pass(word)?|secret|credential|key|token/i and NO value equal to a supplied password; the function source contains no parameter named Password/Secret/Credential.
    - Test 3 (PENDING throw = refusal, SAFE-04): when the PENDING write cannot be completed (mock the Open-AdmanAuditStream seam to throw), Write-AdmanAudit -Result 'PENDING' THROWS a terminating error whose message contains 'AUDIT FAIL-CLOSED' and the verb; and (wired through the 00-04 gate) the AD write wrapper is never invoked.
    - Test 4 (OUTCOME failure escalates, no rollback, SAFE-04): when the OUTCOME (Result='Success'/'Failure') write throws, the function does NOT throw to the caller; it calls Write-EventLog (mocked) and/or Write-Warning and sets $script:AuditDegraded=$true; it never calls an AD write/remove to "roll back".
    - Test 5 (durability + ordering, SAFE-04): the record is flushed with Flush($true) (assert via the Open-AdmanAuditStream seam mock that the returned stream's Flush was called with $true) and the named mutex is acquired before and released after (assert the New-AdmanAuditMutex seam's WaitOne/ReleaseMutex were called); the daily-rotated filename matches 'audit-yyyyMMdd.jsonl'.
    - Test 6 (concurrency, SAFE-04): two concurrent writers serialize (the New-AdmanAuditMutex seam is used; assert its WaitOne was called) and both records land (append, read-share allows readers).
  </behavior>
  <read_first>
    - .planning/phases/00-foundation-safety-harness/00-PATTERNS.md (§Private/Audit/Write-AdmanAudit.ps1 L189–L234 body L194–L231 + sequencing rules L234; Shared: fail-closed audit ordering L474–L476)
    - .planning/phases/00-foundation-safety-harness/00-RESEARCH.md (Pattern 4 L371–L415 body L376–L412; event-source note L415/A2; Code Examples L601–L605; §Don't Hand-Roll L526)
    - .planning/phases/00-foundation-safety-harness/00-CONTEXT.md (D-03 L37–L42 schema L42)
    - Private/Safety/Invoke-AdmanMutation.ps1 (00-04 — the PENDING/OUTCOME call sites the writer must satisfy), Public/Initialize-Adman.ps1 (00-03 — event-source registration + AuditDir)
  </read_first>
  <action>
    Implement Private/Audit/Write-AdmanAudit.ps1 per PATTERNS L194–L231 / RESEARCH L376–L412. param([string]$CorrelationId,[string]$Verb,$Targets,[Parameter(Mandatory)][ValidateSet('PENDING','Success','Failure','Refused','Cancelled')][string]$Result,[string]$Reason,[switch]$WhatIf).
      - Acquire the mutex via the seam: `$mutex = New-AdmanAuditMutex` (the seam wraps the Global\adman-audit named mutex); `[void]$mutex.WaitOne()`; guarantee `ReleaseMutex()`+`Dispose()` in a finally.
      - `$path = Join-Path $script:Config.AuditDir ("audit-{0:yyyyMMdd}.jsonl" -f (Get-Date))`; ensure the dir exists (`New-Item -ItemType Directory -Force -ErrorAction Stop`).
      - Normalize targets: `$targetObjs = @($Targets)`; `$targetDns = $targetObjs | ForEach-Object { $_.DistinguishedName }`; `$targetDetail = $targetObjs | ForEach-Object { @{ dn=$_.DistinguishedName; sid=($_.objectSid.Value); objectClass=($_.objectClass -join ',') } }`.
      - Build `$rec = [ordered]@{ tsUtc=(Get-Date).ToUniversalTime().ToString('o'); who="$env:USERDOMAIN\$env:USERNAME"; userSid=([System.Security.Principal.WindowsIdentity]::GetCurrent().User.Value); what=$Verb; scope=($script:Config.ManagedOUs -join '|'); target=($targetDns -join '|'); targets=$targetDetail; count=$targetObjs.Count; whatIf=[bool]$WhatIf; result=$Result; reason=$Reason; correlationId=$CorrelationId; host=$env:COMPUTERNAME; psEdition=$PSEdition; moduleVersion=(Get-Module adman).Version.ToString() } | ConvertTo-Json -Compress -Depth 5`. NO secret fields (schema is fixed; do not add password/secret/credential/key/token).
      - Open via the seam: `$fs = Open-AdmanAuditStream -Path $path` (the seam returns a stream opened Append/Write/Read-share); write UTF8 bytes of `$rec + "`n"`; `$fs.Flush($true)` (flush to disk, not just OS cache); dispose in finally.
      - catch block: if (`$Result -eq 'PENDING'`) { throw "AUDIT FAIL-CLOSED: cannot write audit record ($($_.Exception.Message)); refusing $Verb." } (this throw is the SAFE-04 refusal and happens before the gate's AD write). Else (OUTCOME) { best-effort `Write-AdmanEventLog -EventId 9001 -EntryType Error -Message "AUDIT OUTCOME WRITE FAILED cid=$CorrelationId verb=$Verb (mutation already applied)"` (the seam wraps Write-EventLog in try/catch and degrades to Write-Warning when the source is unregistered); `Write-Warning "AUDIT OUTCOME WRITE FAILED for cid=$CorrelationId — see Event Log."`; `$script:AuditDegraded = $true` }. NEVER attempt an AD rollback on OUTCOME failure (D-03).
    The writer is the ONLY audit sink (D-01): do NOT route any audit record through Write-PSFMessage/PSFramework logging (async breaks fail-closed). Diagnostics elsewhere may use Write-PSFMessage; audit uses only this function.
    Define the private seams in Private/Audit/AdmanAuditIO.ps1 (non-exported): `New-AdmanAuditMutex` (creates/returns the Global\adman-audit named mutex), `Open-AdmanAuditStream` (opens the given path Append/Write/Read-share and returns the stream), `Write-AdmanEventLog` (wraps Write-EventLog in try/catch, degrading to Write-Warning when the source is unregistered). Write-AdmanAudit calls ONLY these seams for its mutex/file/eventlog operations.
    Tests mock the private wrapper seams (New-AdmanAuditMutex, Open-AdmanAuditStream, Write-AdmanEventLog) — NOT raw .NET static methods/constructors, which Pester cannot mock cleanly — so the throw/flush/ordering behavior is proven without touching the real filesystem for the fail-closed cases (use TestDrive: for the happy-path schema test).
  </action>
  <acceptance_criteria>
    - `Select-String -Path Private/Audit/Write-AdmanAudit.ps1 -Pattern "Global\\adman-audit"` >=1 (named mutex); `Flush\(\$true\)|Flush\(\$true\)` >=1 (durable flush); `Append` + `FileShare.Read|Read` present (append + read-share).
    - PENDING throw present and ordered: `Select-String -Path Private/Audit/Write-AdmanAudit.ps1 -Pattern "AUDIT FAIL-CLOSED"` >=1 and the throw is inside the `if ($Result -eq 'PENDING')` branch.
    - No secret fields: `Select-String -Path Private/Audit/Write-AdmanAudit.ps1 -Pattern 'password|secret|credential|apiKey|privateKey' -SimpleMatch` returns 0; and tests/Audit.Schema.Tests.ps1 asserts the parsed record's key set equals the D-03 set with no secret-named key.
    - No PSFramework audit routing: `Select-String -Path Private/Audit/Write-AdmanAudit.ps1 -Pattern 'Write-PSFMessage|PSFramework'` returns 0 (audit never via PSFramework).
    - No AD rollback on OUTCOME failure: `Select-String -Path Private/Audit/Write-AdmanAudit.ps1 -Pattern 'Set-AD|Remove-AD|Disable-AD|Enable-AD|Move-ADObject'` returns 0.
    - Wrapper seams exist and are the mocked surface: Private/Audit/AdmanAuditIO.ps1 defines New-AdmanAuditMutex, Open-AdmanAuditStream, Write-AdmanEventLog; tests/Audit.FailClosed.Tests.ps1 mocks those seams (`Select-String -Path tests/Audit.FailClosed.Tests.ps1 -Pattern 'Mock\s+(New-AdmanAuditMutex|Open-AdmanAuditStream|Write-AdmanEventLog)'` >=1) and does NOT mock raw .NET statics (`Select-String -Path tests/Audit.FailClosed.Tests.ps1 -Pattern '\[System\.IO\.File\]::Open|\[System\.Threading\.Mutex\]::new' returns 0).
    - `Invoke-Pester -Path tests/Audit.Schema.Tests.ps1,tests/Audit.FailClosed.Tests.ps1 -Output Detailed` exits 0; PENDING-throw ⇒ refusal (and, via the 00-04 gate test, no AD write) and OUTCOME-failure ⇒ escalation + $script:AuditDegraded, no rollback, all green.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "Invoke-Pester -Path tests/Audit.Schema.Tests.ps1,tests/Audit.FailClosed.Tests.ps1 -Output Minimal"</automated>
  </verify>
  <done>The only audit writer durably flushes a no-secret JSON-lines record under a named mutex, throws before AD on any PENDING-write failure (the refusal), escalates (not rolls back) on OUTCOME failure, and is proven by mocked FileStream/Mutex tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audit-integrity orphan sweep + read-only recovery-posture reporter (SAFE-03/04 integrity; RPT-07 feed)</name>
  <files>Private/Audit/Find-AdmanAuditOrphans.ps1, Private/Foundation/Get-AdmanRecoveryPosture.ps1, tests/Audit.OrphanSweep.Tests.ps1, tests/RecoveryPosture.Tests.ps1</files>
  <behavior>
    - Test 1 (orphan detection, SAFE-04): given a .jsonl with a PENDING record whose correlationId has no later OUTCOME record, Find-AdmanAuditOrphans returns that correlationId as an orphan; a PENDING with a matching OUTCOME is NOT flagged.
    - Test 2 (no silent drop, D-03): orphans are surfaced (returned + Write-PSFMessage Warning) so monitoring can detect an OUTCOME-write gap; the function never deletes or rewrites records.
    - Test 3 (recovery posture read-only, RPT-07 feed): Get-AdmanRecoveryPosture returns {RecycleBinEnabled, ForestFunctionalLevel, TombstoneLifetime} via Get-ADOptionalFeature (Name -like 'Recycle Bin*') + (Get-ADForest).ForestMode + (Get-ADObject configuration partition tombstoneLifetime) — all mocked; it performs NO write and returns a warning flag when Recycle Bin is disabled.
    - Test 4 (posture does not gate, SAFE-09): Get-AdmanRecoveryPosture never throws to block an operation when Recycle Bin is off (it reports; the tool ships no hard-delete verb so there is nothing to gate).
  </behavior>
  <read_first>
    - .planning/phases/00-foundation-safety-harness/00-CONTEXT.md (D-03 L37–L42 — OUTCOME-gap detection via correlationId pairing + startup orphan sweep)
    - .planning/phases/00-foundation-safety-harness/00-RESEARCH.md (Pattern 4 L406–L410 OUTCOME escalation; Pattern 7 probe table L486 Recycle-Bin posture)
    - Private/Audit/Write-AdmanAudit.ps1 (Task 1 — record schema correlationId/result), Public/Test-AdmanCapability.ps1 (00-03 — RecycleBinEnabled flag already surfaced; do not duplicate the gate, feed the report)
  </read_first>
  <action>
    Private/Audit/Find-AdmanAuditOrphans.ps1: param([string]$AuditDir = $script:Config.AuditDir, [int]$LookbackDays = 7). Read the last N days of audit-*.jsonl; for each line parse JSON; group by correlationId; a record is an ORPHAN when it has result='PENDING' and the same correlationId has NO subsequent record with result ∈ {Success,Failure,Refused,Cancelled} in the scanned window. Return the list of orphan correlationIds (with tsUtc/what/target) and emit `Write-PSFMessage -Level Warning` summarizing the count. Read-only: do NOT modify/delete/rewrite any audit line. (This is the detection seam for an OUTCOME-write gap from Task 1; it surfaces, it does not "fix" — D-03.)
    Private/Foundation/Get-AdmanRecoveryPosture.ps1: read-only. Return [pscustomobject]@{ RecycleBinEnabled = [bool](Get-ADOptionalFeature -Filter "Name -like 'Recycle Bin*'" | Where-Object { $_.EnabledScopes.Count -gt 0 }); ForestFunctionalLevel = (Get-ADForest).ForestMode.ToString(); TombstoneLifetime = (Get-ADObject -Identity "CN=Directory Service,CN=Windows NT,CN=Services,CN=Configuration,$rootDN" -Properties tombstoneLifetime).tombstoneLifetime }. Pin -Server; catch errors into a $null field + Write-PSFMessage Warning; NEVER throw to block (warning-only — SAFE-09 ships no hard delete). Feeds the 00-03 probe's RecycleBinEnabled flag and Phase-1 RPT-07; do not re-implement the probe flag here (consume or leave the 00-03 flag as-is and note this helper is the report-grade source).
    All AD calls mocked in Unit tests via tests/Mocks/ActiveDirectory.psm1 (00-01); no live domain.
  </action>
  <acceptance_criteria>
    - Find-AdmanAuditOrphans correlates by correlationId: `Select-String -Path Private/Audit/Find-AdmanAuditOrphans.ps1 -Pattern 'correlationId'` >=1 and `PENDING` + an OUTCOME-set check present; `Select-String -Pattern 'Remove-Item|Set-Content|Out-File'` returns 0 (read-only; never rewrites audit).
    - Get-AdmanRecoveryPosture is read-only and non-blocking: `Select-String -Path Private/Foundation/Get-AdmanRecoveryPosture.ps1 -Pattern 'throw '` returns 0 (no terminating throw); references Get-ADOptionalFeature + ForestMode + tombstoneLifetime.
    - `Invoke-Pester -Path tests/Audit.OrphanSweep.Tests.ps1,tests/RecoveryPosture.Tests.ps1 -Output Detailed` exits 0; orphan positive (flagged) + negative (paired, not flagged) fixtures green; posture returns the three fields and does not throw when Recycle Bin is off.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "Invoke-Pester -Path tests/Audit.OrphanSweep.Tests.ps1,tests/RecoveryPosture.Tests.ps1 -Output Minimal"</automated>
  </verify>
  <done>PENDING↔OUTCOME orphan sweep surfaces audit gaps read-only; recovery-posture reporter returns Recycle-Bin/FFL/tombstone as a warning and never gates operations.</done>
</task>

<task type="auto">
  <name>Task 3: Phase exit gate — lab-only integration tests + full Unit suite + lint + SAFE-08/09 proof</name>
  <files>tests/Safety.WhatIf.Integration.Tests.ps1, tests/Safety.Protected.Integration.Tests.ps1</files>
  <read_first>
    - .planning/phases/00-foundation-safety-harness/00-VALIDATION.md (Sampling Rate L30–L36; Manual-Only Verifications L58–L66; Validation Sign-Off L71–L77)
    - .planning/phases/00-foundation-safety-harness/00-RESEARCH.md (§Validation Architecture L679–L728; Test Map L694–L712)
    - .planning/phases/00-foundation-safety-harness/00-PATTERNS.md (§tests/Safety.WhatIf.Integration.Tests.ps1 L450; §tests/Safety.Protected.Tests.ps1 L452)
    - tests/Safety.Gate.Tests.ps1 (00-01 AST guard), rules/AdmanSafetyRules.psm1 (00-01 custom rule), Private/Safety/Invoke-AdmanMutation.ps1 (00-04), Private/Audit/Write-AdmanAudit.ps1 (Task 1)
  </read_first>
  <action>
    Add two LAB-ONLY integration test files, both tagged -Tag 'Integration' so the default Unit run (Pester Filter.Tag='Unit' from 00-01) EXCLUDES them and never touches a domain:
      - tests/Safety.WhatIf.Integration.Tests.ps1 (covers SAFE-01/10 end-to-end): against a disposable lab test OU only, run a gated -WhatIf and assert (a) AD is unchanged after the run, (b) the audit record's target list equals the resolved list, (c) the operator-shown count equals $resolved.Count. Skip/Set-ItSkipped when no lab OU is configured (detect via an env var ADMAN_TEST_OU; if unset, mark the test Skipped with a clear message — never auto-run destructive).
      - tests/Safety.Protected.Integration.Tests.ps1 (covers SAFE-06): lab fixtures for a nested Domain-Admins member, a gMSA, and a renamed RID-500 — assert the gate refuses each and logs Refused with the precise reason; assert adminCount is not consulted. Skipped unless ADMAN_TEST_OU is set.
    Both files MUST carry -Tag 'Integration' on every Describe/It and MUST NOT be collected by the default Unit filter (verify the 00-01 PesterConfiguration default tag excludes 'Integration' or includes only 'Unit').
    Then run and RECORD the phase exit gate (write results to the SUMMARY): 
      (1) `Invoke-Pester -Path tests -Output Normal -TagFilter Unit` => exit 0 (all Unit tests green; no domain touch).
      (2) `Invoke-ScriptAnalyzer -Path . -Recurse -Settings PSScriptAnalyzerSettings.psd1` => exit 0 (PSUseShouldProcessForStateChangingFunctions + custom rule clean).
      (3) The SAFE-08 proof: tests/Safety.Gate.Tests.ps1 passes (no Public/*.ps1 calls a banned AD write cmdlet) AND the custom rule rules/AdmanSafetyRules.psm1 reports zero diagnostics under Public/. Phase 0 ships NO Public write verbs (those arrive Phase 2), so the guard passes trivially now and is re-proven in Phase 2 against the new verbs — note this forward-re-proof in the SUMMARY.
      (4) Confirm the SAFE-09 invariant via the guard: the hard-delete verb appears nowhere under Public/ (enforced by the 00-01 banned list / Get-AdmanBannedWriteVerbs).
    Do NOT run Integration tests in this task unless ADMAN_TEST_OU is explicitly provided by the operator (lab-only); record them as "pending lab" otherwise.
  </action>
  <acceptance_criteria>
    - Both integration files carry the Integration tag: `Select-String -Path tests/Safety.WhatIf.Integration.Tests.ps1,tests/Safety.Protected.Integration.Tests.ps1 -Pattern "-Tag 'Integration'|-Tag @\('Integration'\)"` >=1 in EACH; each references ADMAN_TEST_OU and marks Skipped when unset.
    - The default Unit filter excludes Integration: running `Invoke-Pester -Path tests -Output Normal -TagFilter Unit` collects 0 tests from the two Integration files (prove by the run summary counts) and exits 0.
    - Full Unit suite green: `Invoke-Pester -Path tests -TagFilter Unit -Output Minimal` exits 0 (Module.Manifest, Harness, Safety.Gate, Config.*, Credential.*, Foundation.Capability, Initialize.Adman, Safety.Scope/DenyList/Protected/PreviewEqualsExecute/Confirm/NoHardDelete/GateOrder, Audit.Schema/FailClosed/OrphanSweep, RecoveryPosture).
    - Lint clean: `Invoke-ScriptAnalyzer -Path . -Recurse -Settings PSScriptAnalyzerSettings.psd1` exits 0 (the Phase-0 module is clean under the ShouldProcess rule + custom SAFE-08 rule).
    - SAFE-08/09 proof recorded: tests/Safety.Gate.Tests.ps1 passes against Public/ and the SUMMARY notes the guard is re-proven in Phase 2 when write verbs land.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "Invoke-Pester -Path tests -TagFilter Unit -Output Minimal; Invoke-ScriptAnalyzer -Path . -Recurse -Settings PSScriptAnalyzerSettings.psd1"</automated>
    <human-check>
      End-of-phase human verification (workflow.human_verify_mode=end-of-phase): (a) approve the PSFramework/Pester/PSScriptAnalyzer install (00-01 user_setup / T-00-SC) if not yet done; (b) optionally run the -Tag Integration tests against a disposable lab OU by setting ADMAN_TEST_OU to confirm SAFE-01/06/10 end-to-end -WhatIf and protected-account refusal (manual-only per VALIDATION L58–L66); (c) confirm DPAPI cross-machine re-prompt on a second machine/user (CONF-04). No per-task checkpoint:human-verify is used; these are the phase-level human checks.
    </human-check>
  </verify>
  <done>Lab-only integration tests exist but are excluded from the default Unit run; the full mocked Unit suite is green, lint is clean, and the SAFE-08/09 guard is proven against Public/ with a forward-re-proof note for Phase 2.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase (plan 00-05) produces

Functions (new, non-exported/private): `Write-AdmanAudit` (the ONLY audit writer), `Find-AdmanAuditOrphans`, `Get-AdmanRecoveryPosture`, plus the audit-IO seams `New-AdmanAuditMutex`, `Open-AdmanAuditStream`, `Write-AdmanEventLog` (in Private/Audit/AdmanAuditIO.ps1; mockable wrappers over the .NET mutex/file/eventlog calls).
Functions (exported): none this plan.
Files: `Private/Audit/Write-AdmanAudit.ps1`, `Private/Audit/AdmanAuditIO.ps1`, `Private/Audit/Find-AdmanAuditOrphans.ps1`, `Private/Foundation/Get-AdmanRecoveryPosture.ps1`, `tests/Audit.Schema.Tests.ps1`, `tests/Audit.FailClosed.Tests.ps1`, `tests/Audit.OrphanSweep.Tests.ps1`, `tests/RecoveryPosture.Tests.ps1`, `tests/Safety.WhatIf.Integration.Tests.ps1` (lab-only), `tests/Safety.Protected.Integration.Tests.ps1` (lab-only).
Runtime artifacts (gitignored): `.store/audit/audit-YYYYMMDD.jsonl` (append-only JSON-lines; created on first mutation).
Audit record schema (D-03, no secrets): `tsUtc, who, userSid, what, scope, target, targets[{dn,sid,objectClass}], count, whatIf, result, reason, correlationId, host, psEdition, moduleVersion`.
Phase exit evidence (recorded in 00-05-SUMMARY.md): full Unit suite green + ScriptAnalyzer clean + SAFE-08/09 guard passing against Public/ (forward-re-proof in Phase 2 noted).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutation → audit record | The audit write is the fail-closed control; if it can be skipped or fail-open, an action runs unaudited. |
| audit file on disk → monitoring | OUTCOME-write gaps and tampering must be detectable (orphan sweep) and never silently dropped. |
| secret-bearing call → log | Any password/reset value reaching the audit record is an information-disclosure crossing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-04 | Repudiation | Audit fail-open / skipped write lets a mutation run unaudited | high | mitigate | Write-ahead PENDING reservation flushed with Flush($true) under Global\adman-audit; ANY PENDING-write failure THROWS before AD is touched (Task 1); 00-04 gate Test 4 proves PENDING-before-write. Verified by: PENDING-throw ⇒ refusal test; source has AUDIT FAIL-CLOSED in the PENDING branch + Flush($true) + named mutex. (SAFE-04) |
| T-00-05 | Information Disclosure | Password/reset value/secret written into the audit record | high | mitigate | Fixed D-03 schema with no secret fields; static test bans secret-named keys (Task 1). Verified by: Audit.Schema.Tests.ps1 asserts key set equals D-03 with zero /password|secret|credential|key|token/ keys; writer source grep = 0. (SAFE-03/CONF-05) |
| T-00-17 | Repudiation | OUTCOME-write failure masked or a fake AD rollback compounds damage | medium | mitigate | OUTCOME failure escalates to Event Log + Write-Warning + $script:AuditDegraded; never rolls back AD (Task 1); orphan sweep detects PENDING-without-OUTCOME (Task 2). Verified by: OUTCOME-failure test (no throw, escalation set, zero AD cmdlets in writer) + orphan positive fixture flagged. (SAFE-04, D-03) |
| T-00-01 | Tampering | An exported/Public verb calls AD write cmdlets directly (gate bypass) — phase exit proof | high | mitigate | 00-01 AST guard (tests/Safety.Gate.Tests.ps1) + custom PSSA rule (rules/AdmanSafetyRules.psm1) run against Public/ in the exit gate (Task 3); re-proven in Phase 2 when write verbs land. Verified by: both guards pass against Public/; SUMMARY records the forward-re-proof. (SAFE-08) |
| T-00-16 | Tampering | A hard-delete verb appears under Public/ | high | mitigate | Get-AdmanBannedWriteVerbs (00-01) includes the hard-delete verb; the AST guard + custom rule ban it from Public/ (Task 3 exit proof). Verified by: guard asserts the hard-delete verb is absent under Public/. (SAFE-09) |
| T-00-18 | Tampering | Lab/integration test runs against a production domain by default | medium | mitigate | Integration tests gated -Tag 'Integration', excluded from the default Unit filter, and Skipped unless ADMAN_TEST_OU is set (Task 3). Verified by: default Unit run collects 0 Integration tests; each Integration file references ADMAN_TEST_OU + Skipped-when-unset. (project constraint) |
</threat_model>

<verification>
- `powershell -NoProfile -Command "Invoke-Pester -Path tests -TagFilter Unit -Output Minimal"` exits 0 (full mocked Unit suite green; no domain touch).
- `powershell -NoProfile -Command "Invoke-ScriptAnalyzer -Path . -Recurse -Settings PSScriptAnalyzerSettings.psd1"` exits 0 (ShouldProcess + custom SAFE-08 rule clean).
- Source assertions: writer has AUDIT FAIL-CLOSED in the PENDING branch + Flush($true) + Global\adman-audit + zero secret fields + zero AD cmdlets + zero Write-PSFMessage; orphan sweep is read-only; integration tests are -Tag 'Integration' + ADMAN_TEST_OU-gated.
- Phase exit gate recorded in 00-05-SUMMARY.md: Unit green, lint clean, SAFE-08/09 guard passing against Public/, Integration tests pending-lab.
</verification>

<success_criteria>
- SAFE-04 is structurally enforced: a failed audit pre-write throws before AD is touched; OUTCOME failures escalate without rollback; gaps are detectable via the orphan sweep.
- SAFE-03 holds: every action (incl. dry-runs) appends a no-secret, fully-fielded JSON-lines record under a named mutex with durable flush.
- The phase exit gate is green: full mocked Unit suite passes, ScriptAnalyzer is clean, and the SAFE-08/09 AST guard proves no exported function calls AD write cmdlets directly.
- Integration tests exist for SAFE-01/06/10 but are lab-only and excluded from the default run (no production-domain risk).
- Recovery posture is reported as a warning and never gates (consistent with SAFE-09 no-hard-delete).
</success_criteria>

<output>
Create `.planning/phases/00-foundation-safety-harness/00-05-SUMMARY.md` when done (record the green exit-gate run and the pending-lab Integration tests).
</output>
