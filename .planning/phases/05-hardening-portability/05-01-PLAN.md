---
phase: 05-hardening-portability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/USAGE.md
  - docs/RECOVERY-RUNBOOK.md
  - tests/Help.Coverage.Tests.ps1
  - Public/Initialize-Adman.ps1
  - Public/Start-Adman.ps1
  - Public/Test-AdmanCapability.ps1
  - Public/Config/*.ps1
  - Public/Find-AdmanUser.ps1
  - Public/Find-AdmanComputer.ps1
  - Public/Get-AdmanStaleReport.ps1
  - Public/Get-AdmanAccountStateReport.ps1
  - Public/Get-AdmanRecoveryPostureReport.ps1
  - Public/Format-AdmanReport.ps1
  - Public/Export-AdmanReportCsv.ps1
  - Public/Export-AdmanReportHtml.ps1
  - Public/Get-AdmanInventoryReport.ps1
  - Public/New-AdmanUser.ps1
  - Public/Disable-AdmanUser.ps1
  - Public/Enable-AdmanUser.ps1
  - Public/Set-AdmanUserPassword.ps1
  - Public/Unlock-AdmanUser.ps1
  - Public/Move-AdmanUser.ps1
  - Public/Disable-AdmanComputer.ps1
  - Public/Enable-AdmanComputer.ps1
  - Public/Move-AdmanComputer.ps1
  - Public/Reset-AdmanComputerAccount.ps1
  - Public/New-AdmanLocalUser.ps1
  - Public/Set-AdmanLocalUser.ps1
  - Public/Remove-AdmanLocalUser.ps1
  - Public/Add-AdmanLocalGroupMember.ps1
  - Public/Remove-AdmanLocalGroupMember.ps1
  - Public/Add-AdmanGroupMember.ps1
  - Public/Remove-AdmanGroupMember.ps1
  - Public/Invoke-AdmanBulkAction.ps1
  - Public/Start-AdmanUserOnboarding.ps1
  - Public/Start-AdmanUserOffboarding.ps1
  - Public/Restore-AdmanQuarantinedUser.ps1
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
user_setup: []
must_haves:
  truths:
    - README.md explains install prerequisites (RSAT + PSFramework 1.14.457), first-run config wizard, safe-usage summary, and a "What works today" section reflecting Phases 0-4 shipped state (D-01)
    - docs/USAGE.md lists every menu entry from Get-AdmanMenuDefinition (label, required inputs, B/Q behavior) and every exported function from adman.psd1 FunctionsToExport with at least one example (D-01)
    - docs/RECOVERY-RUNBOOK.md documents quarantine restore via Restore-AdmanQuarantinedUser, AD Recycle Bin restore cmdlets, and authoritative restore warning/escalation (D-07)
    - Every function in adman.psd1 FunctionsToExport has a comment-based help block containing .SYNOPSIS, .DESCRIPTION, .PARAMETER for each declared parameter, and .EXAMPLE (D-02, DOC-03)
    - tests/Help.Coverage.Tests.ps1 fails the build when any public function lacks required help sections (D-02, DOC-03)
  artifacts:
    - README.md
    - docs/USAGE.md
    - docs/RECOVERY-RUNBOOK.md
    - tests/Help.Coverage.Tests.ps1
  key_links:
    - adman.psd1 FunctionsToExport -> Help.Coverage.Tests.ps1 test target list
    - Get-AdmanMenuDefinition -> docs/USAGE.md menu section
    - docs/REMOTE-OPS.md -> README.md and docs/USAGE.md references
  prohibitions:
    - statement: Documentation must never include example passwords, real credential values, or live OU paths from the deployed config
      status: flagged-unverified
      verification: manual
    - statement: Help text must not claim a state-changing function lacks -WhatIf or bypasses the gate
      status: flagged-unverified
      verification: manual
    - statement: No new public function may be added without completing its comment-based help block
      status: flagged-unverified
      verification: manual
---

<objective>
Complete the Phase 5 documentation and inline-help deliverables. Refresh the README to reflect the shipped Phases 0-4 state, author a standalone usage guide and recovery runbook, and enforce comment-based help on every exported function through a Pester contract test.

Purpose: DOC-01/02/03 are the remaining v1 requirements. The tool is functionally complete from Phases 0-4; this plan makes it operable by a mixed-skill team and closes the loop on help coverage.
Output: README.md, docs/USAGE.md, docs/RECOVERY-RUNBOOK.md, tests/Help.Coverage.Tests.ps1, and complete comment-based help on all 38 exported functions.
</objective>

<execution_context>
@C:/Users/nhdinh/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-hardening-portability/05-CONTEXT.md
@.planning/phases/05-hardening-portability/05-RESEARCH.md
@.planning/phases/05-hardening-portability/05-PATTERNS.md
@.planning/phases/05-hardening-portability/05-VALIDATION.md
@.claude/CLAUDE.md
@adman.psd1
@Private/Menu/Get-AdmanMenuDefinition.ps1
@docs/REMOTE-OPS.md
@README.md
</context>

<assumption_delta_decision>
  <noun>parameterized function</noun>
  <decision>no-change</decision>
  <rationale>The assumption-delta detector fired on the word "parameterized" in DOC-02 ("usage guide covers every menu action and parameterized function with examples"). This refers to exported PowerShell cmdlets that accept parameters, not to a change from a derived/chosen value to a modeled identity. No entity or identity model changes in this phase; the usage guide simply documents existing Public verbs and their parameters. Therefore no promotion or add-alside is required.</rationale>
</assumption_delta_decision>

<assumptions>
The following edge probes from the spec-less probe fallback remain unresolved and are carried as explicit planning assumptions:

- DOC-02 / adjacency: When a menu label exactly matches a function name prefix, the usage guide separates the menu description from the function example without merging them.
- DOC-02 / empty: The usage guide still contains a meaningful entry for menu actions that have an empty PromptSpec (read-only reports and recovery posture).
- DOC-02 / ordering: Exported functions in docs/USAGE.md follow the order of adman.psd1 FunctionsToExport; menu actions follow the order returned by Get-AdmanMenuDefinition.
- DOC-03 / adjacency: A public function with zero declared parameters still requires .SYNOPSIS, .DESCRIPTION, and .EXAMPLE, but skips the .PARAMETER count assertion.
- DOC-03 / empty: Functions with no declared parameters (e.g., Start-Adman) are not penalized for missing .PARAMETER blocks.
- DOC-03 / ordering: Help parameters in .PARAMETER blocks are matched against the AST-declared parameter order; mismatched order is allowed but every declared parameter must be documented.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the help-coverage contract test (Wave 0 scaffold)</name>
  <files>tests/Help.Coverage.Tests.ps1</files>
  <read_first>
    - adman.psd1 (FunctionsToExport list)
    - tests/Module.Manifest.Tests.ps1 (manifest import + PSFramework stub pattern)
    - tests/Menu.Tests.ps1 (AST parameter discovery pattern)
    - 05-PATTERNS.md (Help.Coverage.Tests.ps1 pattern)
  </read_first>
  <behavior>
    - RED: Test imports adman.psd1, iterates ExportedFunctions.Keys, and asserts each public function has non-empty/whitespace .Synopsis, non-empty/whitespace .Description, at least one .Example, and a .Parameter entry for every declared AST parameter (excluding WhatIf/Confirm/common params). Functions with no declared parameters skip the parameter assertion. The test runs under Pester 6 and is tagged Unit. It will fail until Task 2 adds missing help.
    - GREEN: After Task 2, the test passes with zero failures.
  </behavior>
  <action>
    Create tests/Help.Coverage.Tests.ps1 per D-02. In BeforeDiscovery, set up the PSFramework 1.14.457 stub in $TestDrive\Modules, prepend it to $env:PSModulePath, import adman.psd1 with -Force, and capture $script:Commands = (Get-Module adman).ExportedFunctions.Keys | Sort-Object. Define $script:CommonParams = @('WhatIf','Confirm','Verbose','Debug','ErrorAction','WarningAction','InformationAction','ErrorVariable','WarningVariable','InformationVariable','OutVariable','OutBuffer','PipelineVariable'). Use Describe 'adman public help coverage' -Tag 'Unit' with It blocks using -ForEach $script:Commands. For each command: assert (Get-Help $_ -Full).Synopsis is not null/whitespace; assert .Description.Text is not null/whitespace; assert .Examples.Example.Count -ge 1; assert the count of .Parameters.Parameter entries (excluding CommonParams) equals the count of AST-declared parameters (excluding CommonParams), or skip the count assertion when the AST param block is absent. Do not hard-code the 38 function names; derive from the manifest.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Help.Coverage.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    tests/Help.Coverage.Tests.ps1 exists, imports adman.psd1, derives the command list from FunctionsToExport, and produces failing tests for exported functions missing .DESCRIPTION or .EXAMPLE. The Pester run exits non-zero (expected RED state).
  </done>
  <acceptance_criteria>
    - tests/Help.Coverage.Tests.ps1 exists under tests/
    - The test file uses BeforeDiscovery and imports adman.psd1
    - The test references (Get-Module adman).ExportedFunctions.Keys, not a hard-coded list
    - Running Invoke-Pester -Path tests/Help.Coverage.Tests.ps1 -Tag Unit shows at least one failure for missing .DESCRIPTION or .EXAMPLE before Task 2
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add complete comment-based help to every exported public function</name>
  <files>Public/**/*.ps1</files>
  <read_first>
    - tests/Help.Coverage.Tests.ps1
    - adman.psd1 (FunctionsToExport list)
    - Public/Find-AdmanUser.ps1 (complete help block pattern)
    - Public/Disable-AdmanUser.ps1 (existing partial help pattern)
    - 05-PATTERNS.md (comment-based help pattern)
  </read_first>
  <action>
    For each of the 38 function names in adman.psd1 FunctionsToExport, open the corresponding Public/*.ps1 file and ensure the comment-based help block above the function declaration contains .SYNOPSIS, .DESCRIPTION, .PARAMETER for every declared parameter, and .EXAMPLE. Expand existing .SYNOPSIS-only blocks; add new blocks where absent. Parameter help names must exactly match declared parameter names. Examples must show realistic parameter values for the common parameter set and must not contain passwords or live OU paths. Keep all function logic, parameter types, and validation unchanged. Do not add help to non-exported Private functions.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Help.Coverage.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    Help.Coverage.Tests.ps1 exits with zero failures. (Get-Help Find-AdmanUser -Full).Description is non-empty, and (Get-Help Disable-AdmanUser -Full).Examples.Example.Count is at least 1.
  </done>
  <acceptance_criteria>
    - Invoke-Pester -Path tests/Help.Coverage.Tests.ps1 -Tag Unit exits with 0 failures
    - (Get-Help Find-AdmanUser -Full).Description.Text is not null or whitespace
    - (Get-Help Disable-AdmanUser -Full).Examples.Example.Count -ge 1
    - No Public/*.ps1 file referenced in FunctionsToExport is skipped
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Refresh README.md and create docs/USAGE.md and docs/RECOVERY-RUNBOOK.md</name>
  <files>README.md, docs/USAGE.md, docs/RECOVERY-RUNBOOK.md</files>
  <read_first>
    - README.md (current Phase 0-only content)
    - docs/REMOTE-OPS.md (keep and reference)
    - Private/Menu/Get-AdmanMenuDefinition.ps1 (source of truth for menu entries)
    - adman.psd1 (FunctionsToExport source of truth)
    - 05-PATTERNS.md (README and docs patterns)
  </read_first>
  <action>
    Rewrite README.md per D-01: keep the safety-guarantees section, update "What works today" to list the shipped menu action categories (search/report, user/computer/local/group lifecycle, remote ops, bulk/workflows) and exported function groups, add or refresh sections ## Installation (RSAT + PSFramework 1.14.457), ## First run (Initialize-Adman / Start-Adman), ## Safe usage (-WhatIf, confirmation, managed-OU scoping, deny-list, protected accounts), and ## Project layout. Create docs/USAGE.md by reading Get-AdmanMenuDefinition to enumerate every non-separator menu entry with its Label, Verb, PromptSpec fields (Name, Prompt, Required, Type, Choices), and note that B/Q navigate back/quit. Add an ## Exported functions section with one example per function in adman.psd1 FunctionsToExport, organized by category. Reference docs/REMOTE-OPS.md for remote-computer operations. Create docs/RECOVERY-RUNBOOK.md per D-07 with sections: ## Restore from quarantine (Restore-AdmanQuarantinedUser), ## Restore from AD Recycle Bin (Get-ADObject -IncludeDeletedObjects | Restore-ADObject), and ## Authoritative restore warning (when to escalate, never run an authoritative restore without a change control). Use contoso.local placeholders for all DNs and example values.
  </action>
  <verify>
    <automated>Test-Path docs/USAGE.md; Test-Path docs/RECOVERY-RUNBOOK.md; Select-String -Path README.md -Pattern '## Installation|## First run|## Safe usage|## What works today'</automated>
  </verify>
  <done>
    README.md reflects Phases 0-4 shipped state and contains the required sections. docs/USAGE.md covers every menu action and exported function. docs/RECOVERY-RUNBOOK.md covers quarantine restore, Recycle Bin restore, and authoritative-restore escalation.
  </done>
  <acceptance_criteria>
    - README.md contains markdown headings ## Installation, ## First run, ## Safe usage, ## What works today
    - docs/USAGE.md contains a section or table row for every non-separator Label returned by (Get-AdmanMenuDefinition).Label
    - docs/USAGE.md contains an ## Exported functions section with one code example per function in adman.psd1 FunctionsToExport
    - docs/RECOVERY-RUNBOOK.md contains markdown headings ## Restore from quarantine, ## Restore from AD Recycle Bin, ## Authoritative restore warning
    - No example in any doc file contains a plaintext password or a live OU path from config/adman.defaults.json
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> operator | Static docs must not give unsafe examples or expose internal bypass details. |
| Contributor -> Public/*.ps1 help blocks | Help text is part of the safety contract; inaccurate help can mislead reviewers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-01 | Information Disclosure | README.md / docs/USAGE.md examples | high | mitigate | Scrub all examples of passwords, credential values, and live OU paths; use contoso.local placeholders. |
| T-05-01-02 | Tampering | Public/*.ps1 comment-based help | medium | mitigate | Help.Coverage.Tests.ps1 enforces presence; PSScriptAnalyzer enforces SupportsShouldProcess accuracy; manual review note in verify step compares safety claims to actual gate behavior. |
| T-05-01-03 | Information Disclosure | docs/RECOVERY-RUNBOOK.md | low | accept | Runbook is internal documentation; access control is repository/file-share permissions, not code. |
| T-05-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no packages. |
</threat_model>

<verification>
- Invoke-Pester -Path tests/Help.Coverage.Tests.ps1 -Tag Unit passes.
- Invoke-ScriptAnalyzer -Path Public -Settings ./PSScriptAnalyzerSettings.psd1 -Recurse is clean.
- README.md, docs/USAGE.md, and docs/RECOVERY-RUNBOOK.md exist and contain the required sections.
- docs/USAGE.md menu section matches (Get-AdmanMenuDefinition).Label set; exported-function examples match adman.psd1 FunctionsToExport.
</verification>

<success_criteria>
- Every exported function has SYNOPSIS, DESCRIPTION, PARAMETER, and EXAMPLE help blocks.
- tests/Help.Coverage.Tests.ps1 passes and will fail the build if help regresses.
- README, USAGE, and RECOVERY-RUNBOOK are complete, accurate, and contain no unsafe examples.
</success_criteria>

<output>
Create `.planning/phases/05-hardening-portability/05-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- README.md (refreshed)
- docs/USAGE.md (new)
- docs/RECOVERY-RUNBOOK.md (new)
- tests/Help.Coverage.Tests.ps1 (new)
- Complete comment-based help blocks on all 38 Public/*.ps1 functions exported in adman.psd1 FunctionsToExport
