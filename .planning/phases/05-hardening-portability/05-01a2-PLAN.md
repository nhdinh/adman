---
phase: 05-hardening-portability
plan: 01a2
type: execute
wave: 2
depends_on:
  - 05-01a1
files_modified:
  - Public/New-AdmanUser.ps1
  - Public/Disable-AdmanUser.ps1
  - Public/Enable-AdmanUser.ps1
  - Public/Set-AdmanUserPassword.ps1
  - Public/Unlock-AdmanUser.ps1
  - Public/Move-AdmanUser.ps1
  - Public/Disable-AdmanComputer.ps1
  - Public/Enable-AdmanComputer.ps1
  - Public/Move-AdmanComputer.ps1
  - Public/Reset-AdmanComputerAccount.ps1
autonomous: true
requirements:
  - DOC-03
user_setup: []
must_haves:
  truths:
    - All AD user lifecycle functions (New-AdmanUser, Disable-AdmanUser, Enable-AdmanUser, Set-AdmanUserPassword, Unlock-AdmanUser, Move-AdmanUser) and AD computer lifecycle functions (Disable-AdmanComputer, Enable-AdmanComputer, Move-AdmanComputer, Reset-AdmanComputerAccount) have complete comment-based help blocks containing .SYNOPSIS, .DESCRIPTION, .PARAMETER for each declared parameter, and .EXAMPLE (D-02, DOC-03)
    - Help text for state-changing functions in this category accurately describes -WhatIf/confirm behavior and does not claim the function bypasses the mutation gate
  artifacts:
    - Complete comment-based help blocks on the AD user/computer lifecycle Public/*.ps1 functions exported in adman.psd1 FunctionsToExport
  key_links:
    - tests/Help.Coverage.Tests.ps1 (from 05-01a1) -> these Public/*.ps1 help block completeness
  prohibitions:
    - statement: Help text must not claim a state-changing function lacks -WhatIf or bypasses the gate
      status: flagged-unverified
      verification: manual
    - statement: No new public function may be added without completing its comment-based help block
      status: flagged-unverified
      verification: manual
---

<objective>
Add complete comment-based help to the AD user and computer lifecycle exported functions. This is the second slice of the DOC-03 inline-help enforcement work.

Purpose: DOC-03 requires every public command/parameter to have inline help enforced by a test gate. This plan completes help for the AD lifecycle category; the test scaffold was created by 05-01a1 and the remaining categories are handled by 05-01a3.
Output: Complete comment-based help on the AD user/computer lifecycle Public/*.ps1 functions.
</objective>

<execution_context>
@C:/Users/nhdinh/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-hardening-portability/05-CONTEXT.md
@.planning/phases/05-hardening-portability/05-RESEARCH.md
@.planning/phases/05-hardening-portability/05-PATTERNS.md
@.planning/phases/05-hardening-portability/05-VALIDATION.md
@.claude/CLAUDE.md
@adman.psd1
</context>

<assumptions>
The following edge probes from the spec-less probe fallback remain unresolved and are carried as explicit planning assumptions:

- DOC-03 / adjacency: A public function with zero declared parameters still requires .SYNOPSIS, .DESCRIPTION, and .EXAMPLE, but skips the .PARAMETER count assertion.
- DOC-03 / empty: Functions with no declared parameters are not penalized for missing .PARAMETER blocks.
- DOC-03 / ordering: Help parameters in .PARAMETER blocks are matched against the AST-declared parameter order; mismatched order is allowed but every declared parameter must be documented.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 2b: Add comment-based help to AD user and computer lifecycle functions</name>
  <files>Public/New-AdmanUser.ps1, Public/Disable-AdmanUser.ps1, Public/Enable-AdmanUser.ps1, Public/Set-AdmanUserPassword.ps1, Public/Unlock-AdmanUser.ps1, Public/Move-AdmanUser.ps1, Public/Disable-AdmanComputer.ps1, Public/Enable-AdmanComputer.ps1, Public/Move-AdmanComputer.ps1, Public/Reset-AdmanComputerAccount.ps1</files>
  <read_first>
    - tests/Help.Coverage.Tests.ps1
    - adman.psd1 (FunctionsToExport list)
    - Public/Disable-AdmanUser.ps1 (existing partial help pattern)
    - Public/Move-AdmanUser.ps1 (state-changing function with -WhatIf pattern)
    - 05-PATTERNS.md (comment-based help pattern)
  </read_first>
  <action>
    For each function in this category, open the corresponding Public/*.ps1 file and ensure the comment-based help block contains .SYNOPSIS, .DESCRIPTION, .PARAMETER for every declared parameter, and .EXAMPLE. For state-changing functions, the help text must accurately describe the -WhatIf/confirm behavior and must not claim the function bypasses the mutation gate. Examples must use contoso.local placeholders for DNs and must not contain passwords. Keep all function logic and parameter validation unchanged.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Help.Coverage.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    Help.Coverage.Tests.ps1 exits with zero failures for the AD user/computer lifecycle category. (Get-Help Disable-AdmanUser -Full).Examples.Example.Count is at least 1, and (Get-Help New-AdmanUser -Full).Description is non-empty.
  </done>
  <acceptance_criteria>
    - Invoke-Pester -Path tests/Help.Coverage.Tests.ps1 -Tag Unit exits with 0 failures
    - (Get-Help Disable-AdmanUser -Full).Examples.Example.Count -ge 1
    - (Get-Help New-AdmanUser -Full).Description.Text is not null or whitespace
    - No help text for a state-changing function in this category claims it lacks -WhatIf or bypasses the gate
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contributor -> Public/*.ps1 help blocks | Help text is part of the safety contract; inaccurate help can mislead reviewers. |
| Test -> module manifest | Help.Coverage.Tests.ps1 derives the public surface from FunctionsToExport. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01a2-01 | Tampering | Public/*.ps1 comment-based help | medium | mitigate | Help.Coverage.Tests.ps1 enforces presence; PSScriptAnalyzer enforces SupportsShouldProcess accuracy; manual review compares safety claims to actual gate behavior. |
| T-05-01a2-02 | Information Disclosure | Public/*.ps1 help examples | medium | mitigate | Examples use contoso.local placeholders and contain no passwords or live OU paths. |
| T-05-01a2-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no packages. |
</threat_model>

<verification>
- Invoke-Pester -Path tests/Help.Coverage.Tests.ps1 -Tag Unit passes.
- Invoke-ScriptAnalyzer -Path Public -Settings ./PSScriptAnalyzerSettings.psd1 -Recurse is clean.
- Every AD lifecycle function returns non-empty .Synopsis, .Description, at least one .Example, and .PARAMETER coverage for each declared parameter.
</verification>

<success_criteria>
- The AD user/computer lifecycle exported functions have SYNOPSIS, DESCRIPTION, PARAMETER, and EXAMPLE help blocks.
- tests/Help.Coverage.Tests.ps1 passes for this function category and will fail the build if help regresses.
- Help text for state-changing functions in this category accurately describes -WhatIf/confirm behavior and does not claim bypass of the mutation gate.
</success_criteria>

<output>
Create `.planning/phases/05-hardening-portability/05-01a2-SUMMARY.md` when done.
</output>

## Artifacts this plan produces

- Complete comment-based help blocks on the AD user/computer lifecycle Public/*.ps1 functions exported in adman.psd1 FunctionsToExport
