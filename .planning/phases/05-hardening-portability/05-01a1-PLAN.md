---
phase: 05-hardening-portability
plan: 01a1
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Help.Coverage.Tests.ps1
  - Public/Config/*.ps1
  - Public/Initialize-Adman.ps1
  - Public/Start-Adman.ps1
  - Public/Test-AdmanCapability.ps1
  - Public/Find-AdmanUser.ps1
  - Public/Find-AdmanComputer.ps1
  - Public/Get-AdmanStaleReport.ps1
  - Public/Get-AdmanAccountStateReport.ps1
  - Public/Get-AdmanRecoveryPostureReport.ps1
  - Public/Format-AdmanReport.ps1
  - Public/Export-AdmanReportCsv.ps1
  - Public/Export-AdmanReportHtml.ps1
  - Public/Get-AdmanInventoryReport.ps1
autonomous: true
requirements:
  - DOC-03
user_setup: []
must_haves:
  truths:
    - tests/Help.Coverage.Tests.ps1 exists with a category-scoped mode so 05-01a1/05-01a2/05-01a3 can enforce help incrementally per slice; the unscoped run is the final gate after 05-01a3 and is expected to show failures until then; parameter coverage compares declared and help parameter name sets (not just counts) (D-02, DOC-03)
    - Public/Config/*.ps1, Public/Initialize-Adman.ps1, Public/Start-Adman.ps1, Public/Test-AdmanCapability.ps1, Public/Find-AdmanUser.ps1, Public/Find-AdmanComputer.ps1, Public/Format-AdmanReport.ps1, Public/Export-AdmanReportCsv.ps1, Public/Export-AdmanReportHtml.ps1, Public/Get-AdmanInventoryReport.ps1, Public/Get-AdmanStaleReport.ps1, Public/Get-AdmanAccountStateReport.ps1, and Public/Get-AdmanRecoveryPostureReport.ps1 have complete comment-based help blocks containing .SYNOPSIS, .DESCRIPTION, .PARAMETER for each declared parameter, and .EXAMPLE (D-02, DOC-03)
    - State-changing function help text in this category accurately describes -WhatIf/confirm behavior and does not claim the function bypasses the mutation gate
  artifacts:
    - tests/Help.Coverage.Tests.ps1
    - Complete comment-based help blocks on the config/startup/read/report Public/*.ps1 functions exported in adman.psd1 FunctionsToExport
  key_links:
    - adman.psd1 FunctionsToExport -> Help.Coverage.Tests.ps1 test target list
    - Help.Coverage.Tests.ps1 -> Public/*.ps1 help block completeness
  prohibitions:
    - statement: Help text must not claim a state-changing function lacks -WhatIf or bypasses the gate
      status: flagged-unverified
      verification: manual
    - statement: No new public function may be added without completing its comment-based help block
      status: flagged-unverified
      verification: manual
---

<objective>
Create the help-coverage contract test and add complete comment-based help to the config, startup, capability-probe, and read/report exported functions. This is the first slice of the DOC-03 inline-help enforcement work; the remaining public function categories are handled by 05-01a2 and 05-01a3.

Purpose: DOC-03 requires every public command/parameter to have inline help enforced by a test gate. This plan establishes the test scaffold and completes help for the config/startup/read/report category, leaving AD lifecycle and local/group/bulk/workflow help to the sibling plans.
Output: tests/Help.Coverage.Tests.ps1 and complete comment-based help on the config/startup/read/report Public/*.ps1 functions.
</objective>

<execution_context>
@C:/Users/nhdinh/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-hardening-portability/05-CONTEXT.md
@.planning/phases/05-hardening-portability/05-RESEARCH.md
@.planning/phases/05-hardening-portability/05-PATTERNS.md
@.planning/phases/05-hardening-portability/05-VALIDATION.md
@.claude/CLAUDE.md
@adman.psd1
</context>

<assumption_delta_decision>
  <noun>parameterized function</noun>
  <decision>no-change</decision>
  <rationale>The assumption-delta detector fired on the word "parameterized" in DOC-02 ("usage guide covers every menu action and parameterized function with examples"). This refers to exported PowerShell cmdlets that accept parameters, not to a change from a derived/chosen value to a modeled identity. No entity or identity model changes in this phase; the usage guide simply documents existing Public verbs and their parameters. Therefore no promotion or add-alside is required.</rationale>
</assumption_delta_decision>

<assumptions>
The following edge probes from the spec-less probe fallback remain unresolved and are carried as explicit planning assumptions:

- DOC-03 / adjacency: A public function with zero declared parameters still requires .SYNOPSIS, .DESCRIPTION, and .EXAMPLE, but skips the .PARAMETER count assertion.
- DOC-03 / empty: Functions with no declared parameters (e.g., Start-Adman) are not penalized for missing .PARAMETER blocks.
- DOC-03 / ordering: Help parameters in .PARAMETER blocks are matched against the AST-declared parameter order; mismatched order is allowed but every declared parameter must be documented.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the help-coverage contract test (Wave 0 scaffold)</name>
  <files>tests/Help.Coverage.Tests.ps1</files>
  <read_first>
    - adman.psd1 (FunctionsToExport list)
    - tests/Module.Manifest.Tests.ps1 (manifest import + PSFramework stub pattern)
    - tests/Menu.Tests.ps1 (AST parameter discovery pattern)
    - 05-PATTERNS.md (Help.Coverage.Tests.ps1 pattern)
  </read_first>
  <behavior>
    - RED: Test imports adman.psd1, iterates ExportedFunctions.Keys (or an optional $FunctionName override), and asserts each public function has non-empty/whitespace .Synopsis, non-empty/whitespace .Description, at least one .Example, and a .Parameter entry for every declared AST parameter by exact name (excluding WhatIf/Confirm/common params). Functions with no declared parameters skip the parameter-name check. The test runs under Pester 6 and is tagged Unit. It will fail until Tasks 2a-2c add missing help.
    - GREEN: After Tasks 2a-2c, an unscoped run passes with zero failures.
    - The test accepts an optional [string[]]$FunctionName parameter at script scope so 05-01a1/05-01a2/05-01a3 can enforce help incrementally per slice. When $FunctionName is omitted or empty, the test covers all exported functions and is the final gate after 05-01a3.
  </behavior>
  <action>
    Create tests/Help.Coverage.Tests.ps1 per D-02. In BeforeDiscovery, set up the PSFramework 1.14.457 stub in $TestDrive\Modules, prepend it to $env:PSModulePath, import adman.psd1 with -Force, and capture $script:Commands = (Get-Module adman).ExportedFunctions.Keys | Sort-Object. Define $script:CommonParams = @('WhatIf','Confirm','Verbose','Debug','ErrorAction','WarningAction','InformationAction','ErrorVariable','WarningVariable','InformationVariable','OutVariable','OutBuffer','PipelineVariable'). Use Describe 'adman public help coverage' -Tag 'Unit' with It blocks using -ForEach $script:Commands. For each command: assert (Get-Help $_ -Full).Synopsis is not null/whitespace; assert .Description.Text is not null/whitespace; assert .Examples.Example.Count -ge 1 and that the first example's code/remark text is not null/whitespace; collect the set of declared parameter names from (Get-Command $_ -ErrorAction SilentlyContinue).ScriptBlock.Ast.Body.ParamBlock.Parameters.Name.VariablePath.UserPath excluding CommonParams, and the set of help-parameter names from (Get-Help $_ -Full).Parameters.Parameter.Name excluding CommonParams; assert the two sorted name sets are equal (or skip the name-set check when the AST param block is absent). This catches documented-but-removed parameters and missing parameter help even when counts coincide. Do not hard-code the function names; derive from the manifest.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Help.Coverage.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    tests/Help.Coverage.Tests.ps1 exists, imports adman.psd1, derives the command list from FunctionsToExport, and produces failing tests for exported functions missing .DESCRIPTION or .EXAMPLE or with help blocks placed above `Set-StrictMode` rather than inside/adjacent to the function. The Pester run exits non-zero (expected RED state).
  </done>
  <acceptance_criteria>
    - tests/Help.Coverage.Tests.ps1 exists under tests/
    - The test file uses BeforeDiscovery and imports adman.psd1
    - The test references (Get-Module adman).ExportedFunctions.Keys, not a hard-coded list
    - The test accepts an optional [string[]]$FunctionName parameter at script scope
    - Running Invoke-Pester with a New-PesterContainer containing the 05-01a1 function list exits with 0 failures
    - Running the unscoped test shows expected failures for functions covered by 05-01a2/05-01a3 before those plans run
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2a: Add comment-based help to config, startup, and read/report functions</name>
  <files>Public/Config/*.ps1, Public/Initialize-Adman.ps1, Public/Start-Adman.ps1, Public/Test-AdmanCapability.ps1, Public/Find-AdmanUser.ps1, Public/Find-AdmanComputer.ps1, Public/Get-AdmanStaleReport.ps1, Public/Get-AdmanAccountStateReport.ps1, Public/Get-AdmanRecoveryPostureReport.ps1, Public/Format-AdmanReport.ps1, Public/Export-AdmanReportCsv.ps1, Public/Export-AdmanReportHtml.ps1, Public/Get-AdmanInventoryReport.ps1</files>
  <read_first>
    - tests/Help.Coverage.Tests.ps1
    - adman.psd1 (FunctionsToExport list)
    - Public/Find-AdmanUser.ps1 (complete help block pattern)
    - Public/Disable-AdmanUser.ps1 (existing partial help pattern)
    - 05-PATTERNS.md (comment-based help pattern)
  </read_first>
  <action>
    Addresses Cycle 4 review concern: the global help-coverage test must not be expected to pass before 05-01a2/05-01a3 complete. The test created in Task 1 supports a category-scoped FunctionName parameter; this task runs it scoped to the 01a1 function subset only.

    For each function in this category, open the corresponding Public/*.ps1 file and ensure the comment-based help block is placed inside the function body or immediately before the `function` keyword (PowerShell associates the help block with the function only when it is inside the function or directly adjacent to the function declaration). Help blocks placed above `Set-StrictMode` or other script-level statements are not returned by `Get-Help` and must be moved. The help block must contain .SYNOPSIS, .DESCRIPTION, .PARAMETER for every declared parameter, and .EXAMPLE. Expand existing .SYNOPSIS-only blocks; add new blocks where absent. Parameter help names must exactly match declared parameter names. Examples must show realistic parameter values for the common parameter set and must not contain passwords or live OU paths. Keep all function logic, parameter types, and validation unchanged. Do not add help to non-exported Private functions.
  </action>
  <verify>
    <automated>Invoke-Pester -Container (New-PesterContainer -Path tests/Help.Coverage.Tests.ps1 -Data @{ FunctionName = @('Initialize-Adman','Start-Adman','Test-AdmanCapability','Get-AdmanConfig','Set-AdmanConfig','Export-AdmanConfig','Import-AdmanConfig','Find-AdmanUser','Find-AdmanComputer','Get-AdmanStaleReport','Get-AdmanAccountStateReport','Get-AdmanRecoveryPostureReport','Format-AdmanReport','Export-AdmanReportCsv','Export-AdmanReportHtml','Get-AdmanInventoryReport') })</automated>
  </verify>
  <done>
    Help.Coverage.Tests.ps1 exits with zero failures for the config/startup/read/report category when invoked with the 05-01a1 function list; the unscoped run may still fail for functions covered by 05-01a2/05-01a3. (Get-Help Find-AdmanUser -Full).Description is non-empty, (Get-Help Format-AdmanReport -Full).Examples.Example.Count is at least 1, the declared and help parameter name sets match for every function in this category, and the help block is located inside/adjacent to the function rather than above `Set-StrictMode`.
  </done>
  <acceptance_criteria>
    - Invoke-Pester -Path tests/Help.Coverage.Tests.ps1 -Tag Unit exits with 0 failures
    - (Get-Help Find-AdmanUser -Full).Description.Text is not null or whitespace
    - (Get-Help Format-AdmanReport -Full).Examples.Example.Count -ge 1
    - Each function in this category has .PARAMETER help for every declared non-common parameter by exact name match
    - Help blocks are placed inside each function or immediately adjacent to the `function` keyword, not above `Set-StrictMode`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contributor -> Public/*.ps1 help blocks | Help text is part of the safety contract; inaccurate help can mislead reviewers. |
| Test -> module manifest | Help.Coverage.Tests.ps1 derives the public surface from FunctionsToExport. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01a1-01 | Tampering | Public/*.ps1 comment-based help | medium | mitigate | Help.Coverage.Tests.ps1 enforces presence; PSScriptAnalyzer enforces SupportsShouldProcess accuracy; manual review compares safety claims to actual gate behavior. |
| T-05-01a1-02 | Information Disclosure | Public/*.ps1 help examples | medium | mitigate | Examples use contoso.local placeholders and contain no passwords or live OU paths. |
| T-05-01a1-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no packages. |
</threat_model>

<verification>
- Invoke-Pester with the 05-01a1 function list passed to tests/Help.Coverage.Tests.ps1 passes.
- Invoke-ScriptAnalyzer -Path Public -Settings ./PSScriptAnalyzerSettings.psd1 -Recurse is clean.
- Every function in this category returns non-empty .Synopsis, .Description, at least one .Example, and .PARAMETER coverage for each declared parameter.
</verification>

<success_criteria>
- The config/startup/read/report exported functions have SYNOPSIS, DESCRIPTION, PARAMETER, and EXAMPLE help blocks.
- tests/Help.Coverage.Tests.ps1 passes for this function category and will fail the build if help regresses.
- Help text for state-changing functions in this category accurately describes -WhatIf/confirm behavior and does not claim bypass of the mutation gate.
</success_criteria>

<output>
Create `.planning/phases/05-hardening-portability/05-01a1-SUMMARY.md` when done.
</output>

## Artifacts this plan produces

- tests/Help.Coverage.Tests.ps1 (new)
- Complete comment-based help blocks on the config/startup/read/report Public/*.ps1 functions exported in adman.psd1 FunctionsToExport
