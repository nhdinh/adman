---
phase: 5
cycle: 6
reviewers: [codex]
reviewed_at: 2026-07-22T10:04:00+07:00
plans_reviewed:
  - 05-01a1-PLAN.md
  - 05-01a2-PLAN.md
  - 05-01a3-PLAN.md
  - 05-01b-PLAN.md
  - 05-02-PLAN.md
  - 05-03-PLAN.md
---

# Cross-AI Plan Review — Phase 5

## Codex Review

### Summary

Phase 5 is directionally strong: it targets the real remaining operational risks, uses existing sources of truth (`FunctionsToExport`, `Get-AdmanMenuDefinition`, audit seams), and keeps new AD behavior out of scope. The main risks are execution details: comment-based help must be validated through `Get-Help`, not text scanning; CI/signing needs a real import path under `AllSigned`; and audit hash-chain work needs careful backward compatibility with existing audit JSONL and restore lookup.

### 05-01a1 — Help Scaffold + Config/Startup/Read/Report Help

#### Strengths

- Uses the manifest export list as the public-command source of truth: `adman.psd1:53`.
- Correctly targets current gaps: several config/startup/reporting commands have no `.EXAMPLE` blocks in source, including `Public/Initialize-Adman.ps1`, `Public/Start-Adman.ps1`, and config verbs.
- The optional `-FunctionName` filter is a good incremental migration tool.

#### Concerns

- **HIGH**: The plan must validate with `Get-Help`, not only regex/AST text. Existing comment blocks are file-level and separated from the function by `Set-StrictMode`, e.g. `Public/Disable-AdmanUser.ps1:2` has help at lines 2-21, `Set-StrictMode` at line 23, and the function begins at line 25. In practice, `Get-Help Disable-AdmanUser -Full` returns autogenerated syntax with empty description/parameter text.
- **MEDIUM**: "Declared AST parameter" must mean parameters on the exported function only. `Public/Config/Set-AdmanConfig.ps1:19` has helper `Set-AdmanConfigValue` parameters at lines 19-24, while exported `Set-AdmanConfig` starts at line 43. A file-wide `ParameterAst` scan would produce false failures like `Object`.

#### Suggestions

- Make `tests/Help.Coverage.Tests.ps1` import the module and assert `Get-Help <name> -Full` has real `Description.Text`, parameter descriptions, and examples.
- Move help blocks inside each exported function or immediately adjacent to the function definition in the form PowerShell actually binds.

#### Risk Assessment

**MEDIUM**. The scope is straightforward, but the current source layout makes a naive help test easy to pass while `Get-Help` remains unusable.

### 05-01a2 — AD User/Computer Lifecycle Help

#### Strengths

- Correct slice boundary: these are all exported in `adman.psd1:53`.
- Lifecycle descriptions already document safety behavior well, e.g. `Public/Disable-AdmanUser.ps1:7` routes through `Invoke-AdmanMutation`, managed scope checks, confirmation, and audit.

#### Concerns

- **MEDIUM**: The scoped assertion requiring exact terms `WhatIf`, `confirm`, and `audit` may be brittle. Existing text often says "confirms" or relies on examples/code for `-WhatIf`, e.g. `Public/Disable-AdmanUser.ps1:10` and line 20.
- **LOW**: Examples using `contoso.local` are safe, but password-reset examples must stay free of literal passwords; `Public/New-AdmanUser.ps1:52` already uses `Read-Host -AsSecureString`, which is the right pattern.

#### Suggestions

- Standardize one short safety paragraph per destructive command containing exact words: `WhatIf`, `confirm`, and `audit`.
- Keep examples realistic but non-secret; use generated/password-prompt flows only.

#### Risk Assessment

**LOW-MEDIUM**. Mostly documentation, but test brittleness could slow the slice.

### 05-01a3 — Local/Group/Bulk/Workflow Help

#### Strengths

- Covers the highest-blast-radius public functions, including bulk and offboarding/restore.
- Existing bulk help already documents cap and typed confirmation behavior at `Public/Invoke-AdmanBulkAction.ps1:7`.

#### Concerns

- **MEDIUM**: This slice depends on 05-03 because `Restore-AdmanQuarantinedUser` help is supposed to describe archive search, but current restore state only scans top-level `audit-*.jsonl` files in `$script:Config.AuditDir` at `Private/Workflow/Get-AdmanOffboardingState.ps1:45`. The dependency is correct, but execution order must enforce it.
- **LOW**: Current restore help has no `.PARAMETER` entries for `Identity` or `Force`, despite actual parameters at `Public/Restore-AdmanQuarantinedUser.ps1:36`.

#### Suggestions

- Implement 05-03 before updating restore help, or phrase archive behavior as "when available" only in a temporary branch.
- Add workflow-specific examples for `-WhatIf` and normal execution.

#### Risk Assessment

**MEDIUM** due to dependency ordering.

### 05-01b — README, USAGE, RECOVERY-RUNBOOK

#### Strengths

- Correctly uses `Get-AdmanMenuDefinition` as the menu source of truth; labels, verbs, prompts, and fixed params are centralized at `Private/Menu/Get-AdmanMenuDefinition.ps1:105`.
- README refresh is clearly needed: current README still says only Phase 0 works at `README.md:5` and "No destructive verbs are exported yet" at line 80, which is now stale.

#### Concerns

- **MEDIUM**: The docs coverage test must handle repeated menu verbs. `Set-AdmanLocalUser` appears three times for reset/enable/disable, enforced by `tests/Menu.Tests.ps1:854`. A function-only docs section will not prove every menu action is covered.
- **LOW**: `docs/REMOTE-OPS.md` exists, but `docs/USAGE.md` and `docs/RECOVERY-RUNBOOK.md` do not currently exist, so this is real net-new documentation.

#### Suggestions

- In `docs/USAGE.md`, separate "Menu actions" from "Exported functions" so repeated labels are covered independently from function references.
- Make the docs test check menu labels under the menu section and function names under the exported-functions section.

#### Risk Assessment

**LOW-MEDIUM**. Scope is broad but mostly mechanical if generated from current sources.

### 05-02 — Dual-Edition + Signing

#### Strengths

- Correctly defers `CompatiblePSEditions = @('Desktop','Core')` until CI proves it; current manifest is Desktop-only at `adman.psd1:18`.
- Removing the stale Pester comment is valid: `tests/PesterConfiguration.psd1:8` still says 5.1 should use the quick run.
- Signing `.psd1/.psm1/.ps1` matches the loader, which dot-sources recursive private/public scripts at `adman.psm1:39`.

#### Concerns

- **MEDIUM**: The AllSigned smoke test should import the signed module after signing all dot-sourced files. Since the loader dot-sources every `Private/**/*.ps1` and `Public/**/*.ps1`, any missed script breaks import under `AllSigned`.
- **LOW**: The plan excludes `tests`, `.github`, and `.githooks` from signing. That is fine only because it reverts execution policy before Pester; keep that ordering explicit.

#### Suggestions

- Add a CI assertion that every loaded module script has a valid signature, not just that `Set-AuthenticodeSignature` returned valid once.
- Fix code coverage paths while here: `tests/PesterConfiguration.psd1:21` uses `Public/*.ps1`, `Private/*.ps1`, which misses nested files such as `Private/Audit/Write-AdmanAudit.ps1`.

#### Risk Assessment

**MEDIUM**. Main risk is CI/environment friction rather than design.

### 05-03 — Audit Hardening + Commit Guard

#### Strengths

- Builds on the existing synchronous fail-closed writer rather than replacing it. Current writer uses `Global\adman-audit` via the seam at `Private/Audit/AdmanAuditIO.ps1:26` and appends/flushed JSONL at `Private/Audit/Write-AdmanAudit.ps1:168`.
- Event-log seam already exists and is tested through mocks; current outcome failure calls `Write-AdmanEventLog` at `Private/Audit/Write-AdmanAudit.ps1:207`.
- `.store/` is already ignored at `.gitignore:1`, so the hook/CI scan is defense in depth.

#### Concerns

- **HIGH**: Hash integration must preserve fail-closed semantics. If reading/parsing the previous audit line fails during a PENDING write, the code should refuse the mutation. Current catch block already treats PENDING write failures as refusal at `Private/Audit/Write-AdmanAudit.ps1:188`; the new previous-hash read must stay inside that protected path.
- **MEDIUM**: Rotation changes restore behavior. Current restore lookup only searches active audit files in the audit dir at `Private/Workflow/Get-AdmanOffboardingState.ps1:45`, so archive search and tests are required before retention is enabled.
- **MEDIUM**: Existing schema tests assert an exact D-03 key set at `tests/Audit.Schema.Tests.ps1:55`. Updating to `hash`/`prevHash` is necessary but will ripple through any tests assuming exact fields.

#### Suggestions

- Define legacy behavior explicitly: first post-upgrade record should use `prevHash` from the raw bytes of the previous legacy line, or omit it only when the file is empty.
- Add corruption tests for malformed prior JSONL, missing `hash`, and modified middle records.
- Keep `hash`/`prevHash` out of any secret-name regex concerns and update all exact-key assertions in one slice.

#### Risk Assessment

**MEDIUM-HIGH**. This is the riskiest plan because it touches the safety-critical audit path and restore source of truth.

### Overall Risk

**MEDIUM**. The phase goals are achievable and well scoped, but success depends on tightening three mechanics: true `Get-Help` validation, enforced ordering between restore docs and audit rotation, and careful audit hash-chain failure behavior. The plans are stronger than average, but 05-03 deserves the most review and testing attention.

---

## Consensus Summary

Only Codex was invoked for this cycle. The review was source-grounded and cited concrete `path/to/file:line` evidence for each finding.

### Agreed Strengths

- Phase 5 scope is correctly bounded to documentation, dual-edition CI/signing, and audit hardening; no new AD capabilities are introduced.
- Plans correctly use existing sources of truth: `adman.psd1 FunctionsToExport`, `Get-AdmanMenuDefinition`, and the existing audit sink.
- The decision to delay `CompatiblePSEditions = @('Desktop','Core')` until CI proves it is sound.
- README refresh is badly needed and correctly scoped.
- Audit hardening correctly preserves the existing fail-closed writer and extends it with hash chain, rotation, and archive-aware restore.

### Agreed Concerns

- **HIGH — 05-01a1**: Existing comment-based help blocks are placed above `Set-StrictMode` and do not bind to functions; `Get-Help` returns empty descriptions. The plan must enforce correct placement and `Get-Help`-based validation.
- **HIGH — 05-03**: Audit hash-chain integration must preserve fail-closed semantics during PENDING writes; failures to read/parse the previous audit record should refuse the mutation.
- **MEDIUM — 05-01a2**: The `.DESCRIPTION` keyword assertion (`WhatIf`, `confirm`, `audit`) for state-changing functions may be brittle.
- **MEDIUM — 05-01a3**: Restore help depends on archive-search behavior implemented in `05-03`; execution order must be enforced.
- **MEDIUM — 05-01b**: The docs coverage test must cover repeated menu labels independently of exported function names.
- **MEDIUM — 05-02**: AllSigned smoke testing must ensure every dot-sourced module script is signed; a single signature check is insufficient.
- **MEDIUM — 05-03**: Existing audit schema tests assert an exact key set and must be updated in lockstep with the new `hash`/`prevHash` fields.
- **LOW — 05-02**: `tests/PesterConfiguration.psd1` code coverage paths miss nested `Private/**/*.ps1` files.

### Divergent Views

None — only Codex provided a review in this cycle.

### Verification coverage

Reviewer had read-only sandbox access to `C:/Users/nhdinh/dev/adman` and cited specific file/line evidence.

## Current HIGH Concerns

- **05-01a1**: Existing comment-based help blocks are placed above `Set-StrictMode` and do not bind to functions; `Get-Help` returns empty descriptions. The plan must enforce correct placement and `Get-Help`-based validation.
- **05-03**: Audit hash-chain integration must preserve fail-closed semantics during PENDING writes; failures to read/parse the previous audit record should refuse the mutation.

## Current Actionable Non-HIGH Concerns

- **05-01a2 MEDIUM**: The exact-term assertion (`WhatIf`, `confirm`, `audit`) for state-changing function descriptions may be brittle. Decide whether to keep the exact-word requirement or relax it and document the rationale in the plan.
- **05-02 MEDIUM**: Add a CI assertion that every loaded module script (not just one sample file) has a valid Authenticode signature before the AllSigned smoke import.
- **05-02 LOW**: Fix `tests/PesterConfiguration.psd1` code coverage paths to include nested `Private/**/*.ps1` and `Public/**/*.ps1` files (e.g., `Private/Audit/Write-AdmanAudit.ps1`).

---

*Review generated by `/gsd-review --phase 5 --codex`*
