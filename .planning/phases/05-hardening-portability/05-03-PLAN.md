---
phase: 05-hardening-portability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - config/adman.schema.json
  - config/adman.defaults.json
  - Private/Audit/Write-AdmanAudit.ps1
  - Private/Audit/Rotation.ps1
  - tests/Audit.Integrity.Tests.ps1
  - tests/Audit.EventLog.Tests.ps1
  - .githooks/pre-commit
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
user_setup: []
must_haves:
  truths:
    - config/adman.schema.json and config/adman.defaults.json include audit.retentionDays with default 90 and minimum 1 (D-05)
    - Write-AdmanAudit.ps1 records prevHash = SHA-256 of the previous record's JSON bytes for each audit record after the first of the day, without altering the synchronous fail-closed PENDING write path (D-05)
    - Private/Audit/Rotation.ps1 contains Get-AdmanAuditIntegrity (verifies the per-day hash chain and reports the first broken link) and Invoke-AdmanAuditRotation (archives files older than audit.retentionDays to .store/audit/archive/YYYYMM/) (D-05)
    - tests/Audit.Integrity.Tests.ps1 proves the chain is valid for unmodified records and invalid after tampering (D-05)
    - tests/Audit.EventLog.Tests.ps1 proves Write-AdmanAudit escalates to Write-AdmanEventLog with EventId 9001 and EntryType Error on OUTCOME-write failure (D-05)
    - .githooks/pre-commit refuses any commit containing staged .store/ paths (D-08)
  artifacts:
    - config/adman.schema.json (audit.retentionDays)
    - config/adman.defaults.json (audit.retentionDays)
    - Private/Audit/Rotation.ps1
    - tests/Audit.Integrity.Tests.ps1
    - tests/Audit.EventLog.Tests.ps1
    - .githooks/pre-commit
  key_links:
    - Write-AdmanAudit -> Private/Audit/Rotation.ps1 hash helper
    - audit.retentionDays config -> Invoke-AdmanAuditRotation
    - .githooks/pre-commit -> README.md hook-installation instruction
  prohibitions:
    - statement: Write-AdmanAudit must never silently skip an audit write; PENDING failures must throw and OUTCOME failures must escalate
      status: flagged-unverified
      verification: manual
    - statement: Audit hash chain must never be billed as tamper-proof; it is tamper-evident only
      status: flagged-unverified
      verification: manual
    - statement: .store/ must never be committed to source control
      status: flagged-unverified
      verification: manual
---

<objective>
Harden the audit subsystem and source-control guardrails: add a per-record SHA-256 hash chain with integrity verifier and rotation, prove event-log escalation on OUTCOME-write failure, and block accidental .store/ commits with a pre-commit hook.

Purpose: The safety model depends on a trustworthy audit log and a clean boundary between runtime secrets/config and source control. This plan implements tamper-evidence, rotation, and defense-in-depth commit protection.
Output: audit.retentionDays config, hash-chain helpers and rotation in Private/Audit/Rotation.ps1, integrated prevHash in Write-AdmanAudit, integrity/event-log tests, and .githooks/pre-commit.
</objective>

<execution_context>
@C:/Users/nhdinh/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-hardening-portability/05-CONTEXT.md
@.planning/phases/05-hardening-portability/05-RESEARCH.md
@.planning/phases/05-hardening-portability/05-PATTERNS.md
@.planning/phases/05-hardening-portability/05-VALIDATION.md
@.claude/CLAUDE.md
@Private/Audit/Write-AdmanAudit.ps1
@Private/Audit/AdmanAuditIO.ps1
@config/adman.schema.json
@config/adman.defaults.json
@tests/Audit.OrphanSweep.Tests.ps1
@tests/Audit.FailClosed.Tests.ps1
@.gitignore
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audit.retentionDays config and hash-chain helper seam</name>
  <files>config/adman.schema.json, config/adman.defaults.json, Private/Audit/Rotation.ps1</files>
  <read_first>
    - config/adman.schema.json
    - config/adman.defaults.json
    - Private/Audit/Write-AdmanAudit.ps1
    - Private/Audit/AdmanAuditIO.ps1
    - 05-PATTERNS.md (config additive defaults and Rotation.ps1 patterns)
  </read_first>
  <action>
    Add an "audit" object to config/adman.schema.json as a sibling to "safety" with required ["retentionDays"] and a retentionDays integer property (description: "Number of days to retain audit JSONL files before rotation", default: 90, minimum: 1). Add "audit": { "retentionDays": 90 } to config/adman.defaults.json. Create Private/Audit/Rotation.ps1 with comment-based help and two non-exported functions: Get-AdmanAuditPreviousHash accepts a [string]$Path and returns the stored hash of the last line of that file, or 64 zero characters if the file does not exist or the last line is empty/whitespace. Get-AdmanAuditIntegrity accepts [string]$Path and verifies each line: parse as JSON; if the line is not the first line, assert $record.prevHash equals the SHA-256 of the previous record's canonical JSON bytes (computed by excluding prevHash and hash from the object, converting to JSON -Compress -Depth 5, UTF8-encoding, and hashing); return a PSCustomObject with Valid, Lines, BrokenAtLine, Reason. Do not modify Write-AdmanAudit.ps1 in this task.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Config.Load.Tests.ps1 -Tag Unit; Test-Path Private/Audit/Rotation.ps1; Select-String -Path Private/Audit/Rotation.ps1 -Pattern 'Get-AdmanAuditPreviousHash|Get-AdmanAuditIntegrity|SHA256'</automated>
  </verify>
  <done>
    config schema and defaults include audit.retentionDays, and Private/Audit/Rotation.ps1 exposes the hash-chain helpers with correct signatures.
  </done>
  <acceptance_criteria>
    - config/adman.schema.json contains an "audit" object with "retentionDays" (type integer, default 90, minimum 1)
    - config/adman.defaults.json contains "audit": { "retentionDays": 90 }
    - Private/Audit/Rotation.ps1 contains function Get-AdmanAuditPreviousHash
    - Private/Audit/Rotation.ps1 contains function Get-AdmanAuditIntegrity
    - Config load tests (tests/Config.Load.Tests.ps1 -Tag Unit) still pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Integrate prevHash into Write-AdmanAudit and add rotation + tests</name>
  <files>Private/Audit/Write-AdmanAudit.ps1, Private/Audit/Rotation.ps1, tests/Audit.Integrity.Tests.ps1, tests/Audit.EventLog.Tests.ps1</files>
  <read_first>
    - Private/Audit/Write-AdmanAudit.ps1
    - Private/Audit/Rotation.ps1
    - Private/Audit/AdmanAuditIO.ps1
    - tests/Audit.OrphanSweep.Tests.ps1 (audit record writer helper pattern)
    - tests/Audit.FailClosed.Tests.ps1 (mock seam pattern for OUTCOME failure)
    - 05-PATTERNS.md (hash-chain and test patterns)
  </read_first>
  <action>
    Modify Write-AdmanAudit.ps1 per D-05. After building the ordered record hashtable and before ConvertTo-Json, call Get-AdmanAuditPreviousHash for the current day's audit file path. Compute the canonical record by creating a copy of the ordered hashtable excluding prevHash and hash, converting it to JSON with -Compress -Depth 5, UTF8-encoding, and computing SHA-256 bytes with [System.Security.Cryptography.SHA256]::Create().ComputeHash; store the resulting hex string as $rec['hash']. Then add $rec['prevHash'] = $previousHash. Serialize the full record (including hash and prevHash) to JSON and write it as before. Keep the existing PENDING fail-closed throw and OUTCOME event-log escalation logic unchanged. Add Invoke-AdmanAuditRotation to Private/Audit/Rotation.ps1: accept optional [string]$AuditDir = $script:Config.AuditDir and [int]$RetentionDays = $script:Config.audit.retentionDays; enumerate files matching audit-*.jsonl in $AuditDir; for each file whose name date is older than $RetentionDays from today, create the archive folder $AuditDir\archive\YYYYMM and Move-Item the file there; write a marker file archive-YYYYMM.marker in the archive folder with the rotation timestamp. Create tests/Audit.Integrity.Tests.ps1: in a $TestDrive audit directory write three records using the same hash logic, call Get-AdmanAuditIntegrity, assert Valid=$true and Lines=3; then mutate the middle line and assert Valid=$false with BrokenAtLine=3. Create tests/Audit.EventLog.Tests.ps1: mock Write-AdmanEventLog -ModuleName adman, mock Open-AdmanAuditStream -ModuleName adman to throw on the second (OUTCOME) call while returning a stream on the first (PENDING) call, call Write-AdmanAudit -Result 'Success', and assert the mock was invoked with EventId 9001 and EntryType Error.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Audit.Integrity.Tests.ps1 -Tag Unit; Invoke-Pester -Path tests/Audit.EventLog.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    Write-AdmanAudit records hash and prevHash, the integrity test detects tampering, the event-log test proves OUTCOME escalation, and rotation archives old files.
  </done>
  <acceptance_criteria>
    - Invoke-Pester -Path tests/Audit.Integrity.Tests.ps1 -Tag Unit exits with 0 failures
    - Invoke-Pester -Path tests/Audit.EventLog.Tests.ps1 -Tag Unit exits with 0 failures
    - Write-AdmanAudit.ps1 contains a call to Get-AdmanAuditPreviousHash
    - Write-AdmanAudit.ps1 contains SHA256 computation and adds 'hash' and 'prevHash' keys to the record
    - Private/Audit/Rotation.ps1 contains Invoke-AdmanAuditRotation
    - The PENDING fail-closed throw and OUTCOME event-log escalation in Write-AdmanAudit.ps1 remain unchanged
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create .githooks/pre-commit hook and document installation</name>
  <files>.githooks/pre-commit, README.md</files>
  <read_first>
    - .gitignore
    - README.md
    - 05-RESEARCH.md (Pattern 4, lines 406-421)
    - 05-PATTERNS.md (.githooks/pre-commit pattern)
  </read_first>
  <action>
    Create .githooks/pre-commit per D-08 as a POSIX sh script. Set BLOCKED='\.store/'. Use git rev-parse --verify HEAD to determine AGAINST (HEAD if it succeeds, otherwise the empty-tree SHA 4b825dc642cb6eb9a060e54bf8d69288fbee4904). Run git diff --cached --name-only "$AGAINST" and grep for ^$BLOCKED; if any match, echo "ERROR: Committing .store/ files is not allowed." to stderr and exit 1; otherwise exit 0. On Windows, ensure the file is committed with the executable bit (git update-index --chmod=+x .githooks/pre-commit). Update README.md to add a one-line instruction under ## Install or ## Contributing: "Enable the commit guard with `git config core.hooksPath .githooks`". Do not modify .gitignore.
  </action>
  <verify>
    <automated>Test-Path .githooks/pre-commit; Select-String -Path .githooks/pre-commit -Pattern '\\.store/'; Select-String -Path README.md -Pattern 'git config core\.hooksPath \.githooks'</automated>
  </verify>
  <done>
    The pre-commit hook exists, blocks staged .store/ paths, and README documents how to enable it.
  </done>
  <acceptance_criteria>
    - .githooks/pre-commit exists and is executable in git's index
    - The hook blocks staged files whose paths start with .store/
    - README.md contains the exact command `git config core.hooksPath .githooks`
    - .gitignore still contains the `.store/` line
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit writer -> filesystem | Hash chain detects tampering after the record is written. |
| Audit writer -> Event Log | Escalation path when the local write fails after a mutation. |
| Developer workstation -> Git | Pre-commit hook blocks accidental secret/config commits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-01 | Tampering | Audit JSONL files | high | mitigate | Implement SHA-256 per-record hash chain with prevHash; provide Get-AdmanAuditIntegrity verifier; document tamper-evident (not tamper-proof) semantics. |
| T-05-03-02 | Repudiation | Write-AdmanAudit OUTCOME path | high | mitigate | Preserve existing fail-closed PENDING throw and event-log escalation on OUTCOME failure; prove with Audit.EventLog.Tests.ps1. |
| T-05-03-03 | Information Disclosure | Audit archive folder | medium | mitigate | Archive folder lives under .store/audit and inherits the same filesystem permissions; access control is OS ACLs, not code. |
| T-05-03-04 | Tampering | .store/ commit guard bypass | medium | mitigate | Pre-commit hook plus CI checkout scan from Plan 05-02; CI scan fails the build if .store/ is present. |
| T-05-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no packages. |
</threat_model>

<verification>
- Invoke-Pester -Path tests/Audit.Integrity.Tests.ps1 -Tag Unit passes.
- Invoke-Pester -Path tests/Audit.EventLog.Tests.ps1 -Tag Unit passes.
- Invoke-Pester -Path tests/Config.Load.Tests.ps1 -Tag Unit passes.
- .githooks/pre-commit exists, blocks staged .store/ paths, and README documents `git config core.hooksPath .githooks`.
- PSScriptAnalyzer is clean on Private/Audit/Rotation.ps1.
</verification>

<success_criteria>
- Audit records carry a verifiable SHA-256 hash chain.
- Audit rotation archives files older than audit.retentionDays.
- OUTCOME-write failures are proven to escalate to the Windows Event Log.
- .store/ cannot be committed accidentally.
</success_criteria>

<output>
Create `.planning/phases/05-hardening-portability/05-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- config/adman.schema.json with audit.retentionDays (modified)
- config/adman.defaults.json with audit.retentionDays (modified)
- Private/Audit/Rotation.ps1 (new) with Get-AdmanAuditPreviousHash, Get-AdmanAuditIntegrity, Invoke-AdmanAuditRotation
- Private/Audit/Write-AdmanAudit.ps1 with hash/prevHash integration (modified)
- tests/Audit.Integrity.Tests.ps1 (new)
- tests/Audit.EventLog.Tests.ps1 (new)
- .githooks/pre-commit (new)
- README.md with hook-installation instruction (modified)
