---
phase: 00-foundation-safety-harness
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Safety.WhatIf.Integration.Tests.ps1
  - Private/Safety/Test-AdmanTargetAllowed.ps1
autonomous: true
gap_closure: true
requirements:
  - SAFE-01
  - SAFE-10
user_setup: []

must_haves:
  truths:
    - "The WhatIf integration test targets NON-PROTECTED child USER fixtures under the lab OU (not the OU DN), so Resolve-AdmanTarget resolves each user as-is (each has an objectSid) and the gate returns Succeeded == the number of targeted users — matching the gate's resolve-identity-as-is semantics (option (a), user decision 2026-07-14)."
    - "After a gated -WhatIf run against those user fixtures, AD is UNCHANGED (no user was actually disabled), the audit record's target list equals the resolved list, and the operator-shown count ($result.Succeeded) equals the resolved count (SAFE-01/10)."
    - "Test-AdmanTargetAllowed step (b) no longer throws under StrictMode when a resolved target has no objectSid (non-security-principals such as OUs/containers): the RID-deny check is skipped for such targets, while remaining fully enforced for real security principals (users/groups/computers/gMSA) — fail-closed semantics for RID-500/builtin-admin denial are NOT weakened."
    - "Both lab integration files re-run green against the lab OU (ADMAN_TEST_OU + ADMAN_TEST_DC set): Safety.WhatIf.Integration.Tests.ps1 proves SAFE-01/10 end-to-end and Safety.Protected.Integration.Tests.ps1 still proves SAFE-06 (nested-admin refused) — the Protected test is untouched and must not regress."
  artifacts:
    - "tests/Safety.WhatIf.Integration.Tests.ps1 (gated -WhatIf It block targets provisioned non-protected user fixtures; assertions match gate semantics)"
    - "Private/Safety/Test-AdmanTargetAllowed.ps1 (step (b) guards the objectSid cast so a null/absent objectSid skips the RID-deny check instead of throwing)"
  key_links:
    - "The WhatIf test's Succeeded assertion MUST equal the number of NON-PROTECTED users it targets; if it accidentally targets a protected/deny-listed fixture (e.g. lab-nested-admin or a RID-500), the gate returns Denied and the assertion fails for the WRONG reason — the fixtures must be dedicated non-protected users (lab-whatif-*), provisioned idempotently in the gated path."
    - "The step-(b) hardening is the ONLY production-code change in this plan; it must preserve the exact deny behavior for principals — a user whose objectSid RID is in $script:DenyRids is still refused. The guard only skips the check when objectSid is absent/null, it never allows a principal that would otherwise be denied."
    - "The WhatIf test still initializes via Initialize-AdmanLab (gap #2 fix) and invokes the gate via module scope (gap #1 fix); this plan changes ONLY the target set and the assertions, not the init or invocation pattern."
---

<objective>
Close UAT gap #3 (the only unfixed gap): fix the Safety.WhatIf.Integration.Tests.ps1 crash + semantic mismatch by (1) retargeting the test at non-protected child USER fixtures under the lab OU so the gate's resolve-identity-as-is semantics produce a truthful Succeeded count, and (2) hardening Test-AdmanTargetAllowed step (b) to skip the RID-deny check when a resolved target has no objectSid (non-security-principals), without weakening RID denial for real principals.

Purpose: The WhatIf test crashed at Test-AdmanTargetAllowed.ps1:52 because it targeted the OU DN directly; Resolve-AdmanTarget resolved the OU object (no objectSid), and the StrictMode cast `.Value` threw. Even past the crash, the test asserted Succeeded == OU child-object count, but the gate resolves a given identity as-is and does NOT enumerate OU children. Per the user's 2026-07-14 decision (option (a)), the test targets child user fixtures and the gate keeps its as-is semantics; step (b) is hardened for robustness so a non-principal target can never crash the gate again.

Output: Two targeted edits (one test file, one production safety file), a re-run of both lab integration files green, and the full Unit suite still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundation-safety-harness/00-UAT.md
@.planning/phases/00-foundation-safety-harness/.continue-here.md
@tests/Safety.WhatIf.Integration.Tests.ps1
@tests/Safety.Protected.Integration.Tests.ps1
@Private/Safety/Test-AdmanTargetAllowed.ps1
@Private/Safety/Resolve-AdmanTarget.ps1
@Private/Safety/Invoke-AdmanMutation.ps1
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget the WhatIf integration test at non-protected child user fixtures (option a)</name>
  <files>tests/Safety.WhatIf.Integration.Tests.ps1</files>
  <read_first>
    - tests/Safety.WhatIf.Integration.Tests.ps1 (the file under edit — current gated It block targets @($script:TestOu) at the Invoke-AdmanMutation call and asserts Succeeded == @($before).Count where $before is the OU's OneLevel child objects)
    - tests/Safety.Protected.Integration.Tests.ps1 (the PASSING sibling — reuse its fixture-discovery pattern: Get-ADUser -SearchBase $script:TestOu ... -ErrorAction SilentlyContinue + Set-ItResult -Skipped when a fixture is absent)
    - Private/Safety/Invoke-AdmanMutation.ps1 (result shape: Succeeded = allowed.Count, Denied = resolved.Count - allowed.Count, Targets = allowed DNs, WhatIf, CorrelationId; a protected/deny-listed target lands in Denied, NOT Succeeded)
    - Private/Safety/Resolve-AdmanTarget.ps1 (binds -Identity only; resolves each given identity as-is; does NOT enumerate OU children)
  </read_first>
  <action>
    In tests/Safety.WhatIf.Integration.Tests.ps1, change ONLY the gated It block 'a gated -WhatIf leaves AD unchanged, audit target == resolved, shown count == resolved.Count' (and add fixture provisioning in the same gated path). Do NOT touch Initialize-AdmanLab, the skip gate, or the module-scope invocation pattern.

    1. Provision dedicated NON-PROTECTED user fixtures idempotently in the gated path (after Initialize-AdmanLab, before the dry-run). Create two users under $script:TestOu with sAMAccountName 'lab-whatif-1' and 'lab-whatif-2' if they do not already exist:
       - For each name: `$u = Get-ADUser -SearchBase $script:TestOu -SearchScope Subtree -LDAPFilter "(sAMAccountName=$name)" -Server $script:TestDc -ErrorAction SilentlyContinue`; if `-not $u`, create it with `New-ADUser -Name $name -SamAccountName $name -Path $script:TestOu -Server $script:TestDc -Enabled $true -AccountPassword (ConvertTo-SecureString 'Lab!Passw0rd1' -AsPlainText -Force) -ErrorAction Stop`.
       - These fixtures are deliberately NON-protected: they are not members of Domain Admins (or any protected group), not gMSA, and not RID 500/501/502 — so the gate ALLOWS them and they land in Succeeded. Do NOT reuse lab-nested-admin / gMSA / RID-500 fixtures (those are protected and would produce Denied, failing the Succeeded assertion for the wrong reason).
       - If fixture creation fails (insufficient lab rights), Set-ItResult -Inconclusive with a clear message rather than erroring.

    2. Resolve the fixture DNs into an array `$targets` (the two lab-whatif-* DistinguishedNames). If fewer than the expected fixtures are present after provisioning, Set-ItResult -Inconclusive -Because 'lab-whatif-* fixtures could not be provisioned under ADMAN_TEST_OU.' and return.

    3. Snapshot AD state for the TARGETED USERS before the dry-run (not the whole OU): capture each fixture's Enabled state via `Get-ADUser -Identity <dn> -Server $script:TestDc -Properties Enabled` into a `$before` map keyed by DN.

    4. Run the gated -WhatIf against the USER fixtures (not the OU DN):
       `$result = & (Get-Module adman) { param($t) Invoke-AdmanMutation -Verb 'Disable-ADAccount' -Targets $t -WhatIf -Confirm:$false } $targets`
       (pass $targets as the param; the existing module-scope pattern from gap #1 is preserved).

    5. Assertions — match gate semantics exactly:
       - (a) AD UNCHANGED: re-read each fixture's Enabled state; assert every targeted user is STILL Enabled (a -WhatIf dry-run must not disable them, SAFE-01). Do NOT assert on OU child-object count.
       - (c) operator-shown count == resolved count: `$result.WhatIf | Should -BeTrue`; `$result.Succeeded | Should -Be $targets.Count -Because 'each non-protected user fixture resolves as-is and is allowed (SAFE-10)'`; `$result.Denied | Should -Be 0`.
       - (b) audit target list == resolved list: read the audit .jsonl for $result.CorrelationId (existing logic); assert the PENDING/OUTCOME records carry whatIf=$true AND that the union of the audit records' target DNs equals $targets (the resolved set). Keep the existing correlationId-filtered read; add a comparison of the recorded target DN set to $targets.

    6. Update the It-block's synopsis comment to state it targets non-protected child USER fixtures (resolve-identity-as-is), not the OU DN.

    Keep every -Tag 'Integration' marker and the ADMAN_TEST_OU/ADMAN_TEST_DC skip gate intact. Do NOT modify Initialize-AdmanLab. Do NOT touch production code in this task.
  </action>
  <acceptance_criteria>
    - The gated It block no longer passes the OU DN to Invoke-AdmanMutation: `Select-String -Path tests/Safety.WhatIf.Integration.Tests.ps1 -Pattern 'Invoke-AdmanMutation' -Context 0,3` shows the targets argument is the fixture array ($targets), and `Select-String -Path tests/Safety.WhatIf.Integration.Tests.ps1 -Pattern '@\(\$script:TestOu\)'` returns 0 in the gated call (the OU DN is no longer the mutation target).
    - Non-protected fixtures are provisioned: `Select-String -Path tests/Safety.WhatIf.Integration.Tests.ps1 -Pattern "lab-whatif-1|lab-whatif-2"` >=1 and a `New-ADUser` provisioning call is present.
    - The Succeeded assertion is against the fixture count, not OU child count: `Select-String -Path tests/Safety.WhatIf.Integration.Tests.ps1 -Pattern '\$result\.Succeeded \| Should -Be \$targets\.Count'` >=1, and the old `@($before).Count` OU-child-count assertion is gone.
    - The AD-unchanged assertion checks the targeted users' Enabled state (not OU child count): a `Get-ADUser ... -Properties Enabled` re-read with a `Should -BeTrue`/`Should -Be \$true` on Enabled is present.
    - Init + invocation patterns preserved: `Select-String -Path tests/Safety.WhatIf.Integration.Tests.ps1 -Pattern 'Initialize-AdmanLab'` >=1 and `Select-String -Pattern '& \(Get-Module adman\)'` >=1; all `-Tag 'Integration'` markers still present (>=2).
    - File parses: `powershell -NoProfile -Command "[void][System.Management.Automation.Language.Parser]::ParseFile('tests/Safety.WhatIf.Integration.Tests.ps1',[ref]$null,[ref]$null)"` exits 0 with no parse errors.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "$e=$null;$t=$null;[void][System.Management.Automation.Language.Parser]::ParseFile('tests/Safety.WhatIf.Integration.Tests.ps1',[ref]$t,[ref]$e); if($e){$e|ForEach-Object{$_.Message};exit 1}; 'PARSE OK'"</automated>
  </verify>
  <done>The WhatIf test provisions two non-protected lab-whatif-* users, targets them (not the OU DN), asserts AD-unchanged via their Enabled state, and asserts Succeeded == fixture count with Denied == 0 and audit targets == resolved set; init + module-scope invocation unchanged; file parses clean.</done>
</task>

<task type="auto">
  <name>Task 2: Harden Test-AdmanTargetAllowed step (b) to skip RID-deny when objectSid is absent</name>
  <files>Private/Safety/Test-AdmanTargetAllowed.ps1</files>
  <read_first>
    - Private/Safety/Test-AdmanTargetAllowed.ps1 (step (b) at line ~52: `$rid = ([System.Security.Principal.SecurityIdentifier]$Object.objectSid).Value.Split('-')[-1]` throws under Set-StrictMode -Version Latest when $Object.objectSid is null — non-security-principals like OUs/containers have no objectSid)
    - tests/Safety.DenyList.Tests.ps1 (the Unit test that pins RID-deny behavior for principals — must still pass; the hardening must NOT weaken denial for users/groups/computers)
    - tests/Safety.Protected.Tests.ps1 (Unit test pinning protected/deny refusal for real principals — must still pass)
  </read_first>
  <action>
    In Private/Safety/Test-AdmanTargetAllowed.ps1, harden step (b) ONLY. This is a robustness fix for non-security-principal targets (OUs/containers) and must NOT change behavior for real security principals.

    Replace the unconditional cast at step (b) with a guard that skips the RID-deny check when the resolved object has no objectSid:
      - Read the raw sid: `$sid = $Object.objectSid` (use `.PSObject.Properties['objectSid']` value semantics already present via the -Properties fetch in Resolve-AdmanTarget; under StrictMode read it defensively: `$sid = if ($Object.PSObject.Properties['objectSid']) { $Object.objectSid } else { $null }`).
      - Only compute and check the RID when a sid is present: `if ($null -ne $sid) { $rid = ([System.Security.Principal.SecurityIdentifier]$sid).Value.Split('-')[-1]; if ($rid -in $script:DenyRids) { $reasons.Add("deny-listed RID $rid") } }`.
      - When objectSid is absent/null (OU/container/other non-principal), add NO deny reason and continue — the target is still subject to step (c) managed-OU scope and step (d) protected-membership, so it is NOT silently allowed; it simply cannot match a RID deny (it has no RID).

    CRITICAL — do not weaken principal denial:
      - For any object WITH an objectSid (users, groups, computers, gMSA), the RID-deny check runs EXACTLY as before. A renamed RID-500 user is still refused. The Unit tests Safety.DenyList.Tests.ps1 and Safety.Protected.Tests.ps1 must remain green with zero changes.
      - Do NOT add an early return; keep the accumulate-reasons pattern. Do NOT change steps (a), (c), or (d).
      - Update the step-(b) comment to note the guard: non-security-principals (OU/container) have no objectSid, so the RID check is skipped for them (robustness; they are still scope- and membership-checked).

    Keep Set-StrictMode -Version Latest. No new parameters. No change to the return shape.
  </action>
  <acceptance_criteria>
    - The objectSid cast is guarded: `Select-String -Path Private/Safety/Test-AdmanTargetAllowed.ps1 -Pattern 'if \(\$null -ne \$sid\)|-ne \$null' ` >=1 AND the cast `([System.Security.Principal.SecurityIdentifier]` appears INSIDE that null-guard (no unguarded `.Value` on a possibly-null sid).
    - No unguarded direct cast remains at step (b): the line `$rid = ([System.Security.Principal.SecurityIdentifier]$Object.objectSid).Value` (unguarded) is replaced; `Select-String -Path Private/Safety/Test-AdmanTargetAllowed.ps1 -Pattern '\$Object\.objectSid\)\.Value'` returns 0 (the cast no longer dereferences .Value directly off $Object.objectSid without a null check).
    - Principal denial preserved: the `if ($rid -in $script:DenyRids)` check and `$reasons.Add("deny-listed RID $rid")` are still present and still run for any object with a sid.
    - No early return introduced: `Select-String -Path Private/Safety/Test-AdmanTargetAllowed.ps1 -Pattern 'return' ` count is unchanged (still only the final `return @{ Allowed ... }`).
    - Unit suite still green (principal denial + protected refusal unregressed): `Invoke-Pester -Path tests/Safety.DenyList.Tests.ps1,tests/Safety.Protected.Tests.ps1,tests/Safety.Scope.Tests.ps1 -Output Minimal` exits 0.
    - File parses: `powershell -NoProfile -Command "[void][System.Management.Automation.Language.Parser]::ParseFile('Private/Safety/Test-AdmanTargetAllowed.ps1',[ref]$null,[ref]$null)"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "Invoke-Pester -Path tests/Safety.DenyList.Tests.ps1,tests/Safety.Protected.Tests.ps1,tests/Safety.Scope.Tests.ps1 -Output Minimal"</automated>
  </verify>
  <done>Step (b) skips the RID-deny check only when objectSid is absent (non-principal targets), never throws under StrictMode on an OU/container, and still refuses any principal whose objectSid RID is deny-listed; DenyList/Protected/Scope Unit tests stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify — full Unit suite green + both lab integration files re-run green</name>
  <files>tests/Safety.WhatIf.Integration.Tests.ps1, Private/Safety/Test-AdmanTargetAllowed.ps1</files>
  <read_first>
    - .planning/phases/00-foundation-safety-harness/.continue-here.md (Infrastructure State: lab reachable ONLY from the operator's interactive runas /netonly PS7 session; the agent CANNOT reach D:\adman or the lab DC — all lab runs are MANUAL; PS7 needs the WindowsPowerShell Modules path prepended; required env ADMAN_TEST_OU + ADMAN_TEST_DC)
    - tests/PesterConfiguration.psd1 (default Filter.Tag='Unit' — the Integration files are excluded from the Unit run)
  </read_first>
  <action>
    Automated (agent runs on C:\Users\nhdinh\dev\adman):
      1. Full mocked Unit suite: `Invoke-Pester -Path tests -TagFilter Unit -Output Minimal` => exit 0 (all Unit tests green; Integration files excluded by the tag filter; the step-(b) hardening must not regress any Unit test).
      2. Lint: `Invoke-ScriptAnalyzer -Path Private/Safety/Test-AdmanTargetAllowed.ps1 -Settings PSScriptAnalyzerSettings.psd1` => 0 findings on the edited production file.
      3. Confirm the Integration files are still excluded from the default Unit run (collection unbroken): the Unit run summary shows 0 tests collected from the two Integration files.

    Manual (operator runs on the lab — the agent CANNOT reach the lab; record as human_judgment):
      4. From the operator's interactive `runas /netonly /user:LAB\Administrator` PS7 session on D:\adman (after `git pull`), with `$env:ADMAN_TEST_OU='OU=Adman-test,DC=lab,DC=local'` and `$env:ADMAN_TEST_DC='lab-dc01.lab.local'` set and the WindowsPowerShell Modules path prepended to $env:PSModulePath:
         - `Invoke-Pester -Path tests/Safety.WhatIf.Integration.Tests.ps1 -TagFilter Integration -Output Detailed` => the gated -WhatIf It block PASSES (AD unchanged; Succeeded == 2; Denied == 0; audit targets == resolved).
         - `Invoke-Pester -Path tests/Safety.Protected.Integration.Tests.ps1 -TagFilter Integration -Output Detailed` => still PASSES (nested-admin refused + Refused audit record; gMSA/RID-500 may be Inconclusive if fixtures absent — acceptable, not a failure).
      5. On a green WhatIf run, flip UAT test 2 from `issue` to `pass` in 00-UAT.md and record the gap-#3 resolution as FIXED.

    Do NOT attempt to run the Integration-tagged tests from the agent (no lab reachability). The agent runs steps 1-3; steps 4-5 are the operator's manual lab validation.
  </action>
  <acceptance_criteria>
    - `powershell -NoProfile -Command "Invoke-Pester -Path tests -TagFilter Unit -Output Minimal"` exits 0 (full Unit suite green; Integration files excluded).
    - `powershell -NoProfile -Command "Invoke-ScriptAnalyzer -Path Private/Safety/Test-AdmanTargetAllowed.ps1 -Settings PSScriptAnalyzerSettings.psd1"` reports 0 findings.
    - The default Unit run collects 0 tests from the two Integration files (tag filter intact).
    - (Manual/recorded) Both lab integration files pass against the lab OU; UAT test 2 flipped to pass with gap #3 marked FIXED.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "Invoke-Pester -Path tests -TagFilter Unit -Output Minimal; Invoke-ScriptAnalyzer -Path Private/Safety/Test-AdmanTargetAllowed.ps1 -Settings PSScriptAnalyzerSettings.psd1"</automated>
    <human-check>
      Lab validation (manual-only; agent cannot reach the lab DC): from the operator's runas /netonly PS7 session on D:\adman with ADMAN_TEST_OU + ADMAN_TEST_DC set, run both -Tag Integration files and confirm the WhatIf file passes (AD unchanged, Succeeded == fixture count, Denied == 0, audit targets == resolved) and the Protected file still passes. Then flip UAT test 2 to pass and mark gap #3 FIXED in 00-UAT.md.
    </human-check>
  </verify>
  <done>Full Unit suite green and lint clean on the edited file; both lab integration files re-run green on the lab (WhatIf proves SAFE-01/10, Protected still proves SAFE-06); UAT test 2 flipped to pass with gap #3 recorded FIXED.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan (00-06, gap closure) produces

Modified files (2):
- `tests/Safety.WhatIf.Integration.Tests.ps1` — gated -WhatIf It block retargeted at two provisioned non-protected `lab-whatif-*` user fixtures (resolve-identity-as-is); assertions match gate semantics (AD-unchanged via fixture Enabled state; Succeeded == fixture count; Denied == 0; audit targets == resolved set). Init (Initialize-AdmanLab) + module-scope invocation unchanged.
- `Private/Safety/Test-AdmanTargetAllowed.ps1` — step (b) hardened: the objectSid→RID cast is null-guarded so a non-security-principal target (OU/container) skips the RID-deny check instead of throwing under StrictMode; principal RID denial unchanged.

No new exported functions. No new files. No config/schema changes.

Runtime artifacts (lab only, gitignored): two `lab-whatif-*` user fixtures provisioned under the lab OU (idempotent create-if-absent in the gated test path).

Verification evidence (recorded in 00-06-SUMMARY.md): full Unit suite green (138+ passed / 0 failed), lint clean on the edited file, both lab integration files green on the lab, UAT test 2 flipped to pass with gap #3 FIXED.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → mutation gate | The WhatIf test now provisions and targets real lab users; a mis-provisioned (protected) fixture would produce a false Denied and mask the SAFE-01/10 proof. |
| objectSid-absent target → RID-deny check | The step-(b) hardening relaxes a StrictMode crash into a skip; if implemented wrong it could let a deny-listed principal through (weakening SAFE-05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-19 | Tampering | Step-(b) hardening accidentally skips RID-deny for a real principal (e.g. renamed RID-500), weakening SAFE-05 | high | mitigate | The null-guard skips ONLY when objectSid is absent/null; any object WITH a sid runs the exact prior `if ($rid -in $script:DenyRids)` check. Verified by: Safety.DenyList.Tests.ps1 + Safety.Protected.Tests.ps1 (renamed RID-500 refused) stay green with zero changes; acceptance criterion requires the deny check still present and the unguarded `.Value` deref gone. (SAFE-05/06) |
| T-00-20 | Repudiation | WhatIf test targets a protected fixture and reports a false pass/fail, so SAFE-01/10 is not genuinely proven | medium | mitigate | Test provisions DEDICATED non-protected `lab-whatif-*` users (not lab-nested-admin/gMSA/RID-500); asserts Denied == 0 so a protected fixture would fail loudly. Verified by: acceptance criteria require the lab-whatif-* provisioning + Denied==0 assertion; manual lab run confirms Succeeded == fixture count. (SAFE-01/10) |
| T-00-18 | Tampering | Integration test runs against a production domain by default | medium | mitigate | Unchanged: both files stay -Tag 'Integration', excluded from the default Unit filter, and Skipped unless ADMAN_TEST_OU + ADMAN_TEST_DC are set. Verified by: Task 3 confirms the Unit run collects 0 Integration tests. (project constraint) |
</threat_model>

<verification>
- `powershell -NoProfile -Command "Invoke-Pester -Path tests -TagFilter Unit -Output Minimal"` exits 0 (full mocked Unit suite green; Integration files excluded; step-(b) hardening unregressed).
- `powershell -NoProfile -Command "Invoke-ScriptAnalyzer -Path Private/Safety/Test-AdmanTargetAllowed.ps1 -Settings PSScriptAnalyzerSettings.psd1"` => 0 findings.
- Both edited files parse clean (Parser.ParseFile, no errors).
- Source assertions: WhatIf test no longer targets the OU DN (targets the lab-whatif-* fixture array); Succeeded asserted against `$targets.Count`; Denied asserted == 0; AD-unchanged asserted via fixture Enabled state. Test-AdmanTargetAllowed step (b) null-guards the objectSid cast; the deny check is preserved for principals; no early return added.
- (Manual, recorded) Both lab integration files pass against the lab OU; UAT test 2 flipped to pass; gap #3 marked FIXED in 00-UAT.md.
</verification>

<success_criteria>
- UAT gap #3 is closed: the WhatIf integration test runs green against the lab OU, proving SAFE-01 (AD unchanged after -WhatIf) and SAFE-10 (operator-shown count == resolved count; audit targets == resolved list) end-to-end.
- The gate's resolve-identity-as-is semantics are preserved (option (a)); no OU-expansion product change was made.
- Test-AdmanTargetAllowed no longer crashes on a non-security-principal target, and RID denial for real principals (renamed RID-500) is unweakened (DenyList/Protected Unit tests green).
- The Protected integration test still passes (SAFE-06) — no regression.
- The full Unit suite remains green and lint stays clean; UAT test 2 is flipped to pass.
</success_criteria>

<output>
Create `.planning/phases/00-foundation-safety-harness/00-06-SUMMARY.md` when done (record the green Unit run, the lint result, the manual lab re-run outcome for both integration files, and the UAT test-2 flip to pass with gap #3 FIXED).
</output>
