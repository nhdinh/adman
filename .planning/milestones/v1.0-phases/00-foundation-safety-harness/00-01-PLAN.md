---
phase: 00-foundation-safety-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - adman.psd1
  - adman.psm1
  - Public/Initialize-Adman.ps1
  - Public/Start-Adman.ps1
  - PSScriptAnalyzerSettings.psd1
  - rules/AdmanSafetyRules.psm1
  - tests/PesterConfiguration.psd1
  - tests/Mocks/ActiveDirectory.psm1
  - tests/Safety.Gate.Tests.ps1
  - .planning/phases/00-foundation-safety-harness/00-01-PSFramework-verified.md
  - .claude/CLAUDE.md
autonomous: true
requirements:
  - MENU-05
  - SAFE-08
user_setup:
  - service: powershell-gallery
    why: "PSFramework 1.14.457 (config+diagnostics backbone, D-01) is the second mandatory module after RSAT; Pester 6.0.0 and PSScriptAnalyzer 1.25.0 are dev dependencies. The package-legitimacy seam did NOT run in this environment, so the FIRST install of PSFramework requires your explicit approval (end-of-phase human check) after the automated re-verification task prints exact cmdlet signatures."
    env_vars: []
    dashboard_config:
      - task: "Approve and run: Install-PSResource -Name PSFramework -Version 1.14.457 -Scope CurrentUser (and Pester/PSScriptAnalyzer for dev)"
        location: "Elevated or CurrentUser PowerShell on the dev/admin workstation"

must_haves:
  truths:
    - "The module loads on Windows PowerShell 5.1 with `$ErrorActionPreference='Stop'` set module-wide and dot-sources Private/** then Public/** (SAFE-08 boundary foundation)."
    - "adman.psd1 declares an EXPLICIT FunctionsToExport list (never '*'); the gate name Invoke-AdmanMutation is ABSENT from that list (SAFE-08, T-00-01)."
    - "CompatiblePSEditions is @('Desktop') only until the Phase 5 CI matrix passes on 7.6 (honest dual-edition claim); PowerShellVersion='5.1'."
    - "RequiredModules lists PSFramework (EXACT-pinned 1.14.457 via RequiredVersion — ModuleVersion alone is only a minimum floor) and does NOT list ActiveDirectory (RSAT is a prerequisite, never a bundled dependency)."
    - "Invoke-ScriptAnalyzer runs with PSScriptAnalyzerSettings.psd1 and has PSUseShouldProcessForStateChangingFunctions ENABLED plus a documented PSAvoidUsingWriteHost suppression scoped only to the (Phase-1) TUI module (SAFE-01/08)."
    - "A custom PSScriptAnalyzer rule (rules/AdmanSafetyRules.psm1) and a Pester AST test (tests/Safety.Gate.Tests.ps1) exist that parse Public/**/*.ps1 and flag any CommandAst resolving to the banned AD write cmdlet set (SAFE-08 exit-gate scaffold)."
    - "No unit test touches a live domain: tests/Mocks/ActiveDirectory.psm1 mocks every Get-AD*/Set-AD*/Disable-AD*/Move-ADObject/New-AD* cmdlet (project constraint)."
    - "PSFramework 1.14.457 exact cmdlet signatures are re-verified at build time (Assumption A1) BEFORE pinning; the verified output is written to 00-01-PSFramework-verified.md (T-00-SC)."
    - "Project instructions (.claude/CLAUDE.md) are reconciled to D-01 so downstream agents read ONE consistent story: PSFramework 1.14.457 is adopted in Phase 0 for config + diagnostic/ops logging, and the audit writer stays hand-rolled/synchronous (the audit exception) — no stale 'defer PSFramework to a later milestone/v2' framing remains (Addresses review concern C2-M2)."
  artifacts:
    - "adman.psd1 (manifest: RootModule, PowerShellVersion='5.1', CompatiblePSEditions=@('Desktop'), RequiredModules=@(@{ModuleName='PSFramework'; RequiredVersion='1.14.457'}) exact pin, explicit FunctionsToExport, ModuleVersion)"
    - "adman.psm1 (loader: $ErrorActionPreference='Stop'; dot-source Private/**/*.ps1 then Public/**/*.ps1; Export-ModuleMember -Function $public; import side-effect-free)"
    - "Public/Initialize-Adman.ps1 (exported stub; real body lands in 00-03)"
    - "Public/Start-Adman.ps1 (exported stub for Phase 1 menu; calls Initialize-Adman)"
    - "PSScriptAnalyzerSettings.psd1 (rule enable list + CustomRulePath -> rules/AdmanSafetyRules.psm1 + scoped WriteHost suppression)"
    - "rules/AdmanSafetyRules.psm1 (custom rule: banned AD write cmdlets under Public/ with token-grep fallback)"
    - "tests/PesterConfiguration.psd1 (Pester 6: Run/Filter/CodeCoverage profiler-based + Output)"
    - "tests/Mocks/ActiveDirectory.psm1 (AD/CIM/remoting mocks)"
    - "tests/Safety.Gate.Tests.ps1 (AST guard scaffold)"
    - ".planning/phases/00-foundation-safety-harness/00-01-PSFramework-verified.md (build-time signature dump)"
  key_links:
    - "Manifest FunctionsToExport must equal the set of functions adman.psm1 passes to Export-ModuleMember (drift => SAFE-08 bypass)."
    - "PSScriptAnalyzerSettings.psd1 CustomRulePath must resolve to rules/AdmanSafetyRules.psm1 or the custom rule silently does not run."
    - "The AST test's $banned list and the custom rule's banned list must be identical (single source) or one will pass where the other fails."
---

<objective>
Establish the load-bearing module skeleton and the static-analysis harness that the entire safety spine hangs on, and re-verify the PSFramework 1.14.457 cmdlet surface before pinning it.

Purpose: SAFE-08 ("all writes through one non-exported gate") is only enforceable if (a) the manifest/export boundary is explicit and (b) an automated guard parses Public/ and rejects direct AD write calls. This plan builds that boundary and the Pester + PSScriptAnalyzer harness before any safety logic exists, so every later plan is tested and linted from its first commit. It also discharges Assumption A1 (PSFramework exact signatures) and surfaces the first-install approval (T-00-SC).

Output: A loadable adman module (stubs only), explicit export boundary, Pester 6 + PSScriptAnalyzer 1.25.0 harness with a custom SAFE-08 rule, AD/CIM mocks, and a build-time re-verification record for PSFramework.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/00-foundation-safety-harness/00-CONTEXT.md
@.planning/phases/00-foundation-safety-harness/00-RESEARCH.md
@.planning/phases/00-foundation-safety-harness/00-PATTERNS.md
@.planning/phases/00-foundation-safety-harness/00-VALIDATION.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build-time re-verification of PSFramework 1.14.457 cmdlet signatures (Assumption A1)</name>
  <files>.planning/phases/00-foundation-safety-harness/00-01-PSFramework-verified.md</files>
  <read_first>
    - .planning/phases/00-foundation-safety-harness/00-RESEARCH.md (§Standard Stack L89–L98; Assumptions Log A1 L634; Package Legitimacy Audit L139–L152; Environment Availability L659–L677)
    - .planning/phases/00-foundation-safety-harness/00-CONTEXT.md (D-01 L22–L26)
  </read_first>
  <action>
    Run (read-only; no install in this task) a Gallery + local-module probe and write the results to .planning/phases/00-foundation-safety-harness/00-01-PSFramework-verified.md so the pin in adman.psd1 is evidence-based. Per D-01 and RESEARCH A1.
    Concrete checks to perform and record:
    - Registry query (no mutation): `Find-PSResource -Name PSFramework -Version 1.14.457` (fallback `Find-Module -Name PSFramework -AllVersions`); record Name, Version, PublishedDate, Author, ProjectUri.
    - If the module is already installed locally, dump exact parameter sets for the safety-relevant cmdlets: `Get-Command Set-PSFConfig, Register-PSFConfigValidation, Export-PSFConfig, Import-PSFConfig, Write-PSFMessage | ForEach-Object { $_.Name; $_.Parameters.Keys }`. Confirm presence of `-Path` on Export/Import-PSFConfig and the parameter names used by 00-02.
    - If NOT installed, do NOT install here; record "not installed — install pending user approval (user_setup)" and the exact install command. The install itself is a human-approved action surfaced via this plan's user_setup + the end-of-phase human check (the package-legitimacy seam did not run in this environment; the first PSFramework install is never auto-approved, workflow.auto_advance ignored for this step).
    Record in the markdown: verdict ([OK]/[ASSUMED]/[SUS]), exact versions + publish dates, the confirmed-or-pending parameter names, and the install command. This artifact is the source the plan-review source-grounding pass uses to treat PSFramework symbols as verified, not drift.
  </action>
  <acceptance_criteria>
    - File .planning/phases/00-foundation-safety-harness/00-01-PSFramework-verified.md exists and contains a non-empty "Verdict" line plus a "Version" line reading exactly "1.14.457".
    - The file contains either (a) the literal parameter list for `Import-PSFConfig` showing a `-Path` parameter, or (b) the explicit string "install pending user approval" alongside the install command `Install-PSResource -Name PSFramework -Version 1.14.457 -Scope CurrentUser`.
    - No package was installed by this task: running `Get-Module -ListAvailable PSFramework` after the task returns the same result as before (the task is read-only).
    - The recorded PublishedDate matches the Gallery value for 1.14.457 (RESEARCH: 2026-07-02); any mismatch is flagged in the file as a discrepancy.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "Test-Path .planning/phases/00-foundation-safety-harness/00-01-PSFramework-verified.md"  # => True; and Select-String -Path .planning/phases/00-foundation-safety-harness/00-01-PSFramework-verified.md -Pattern '1.14.457' returns >=1 match</automated>
  </verify>
  <done>PSFramework 1.14.457 provenance + signatures captured to disk with an [OK] verdict or an explicit "pending approval" note; no install performed; the manifest pin in Task 2 cites this artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Manifest + root-module loader with explicit export boundary (SAFE-08)</name>
  <files>adman.psd1, adman.psm1, Public/Initialize-Adman.ps1, Public/Start-Adman.ps1, tests/Module.Manifest.Tests.ps1, .claude/CLAUDE.md</files>
  <behavior>
    - Test 1: `Import-Module ./adman.psd1 -Force` succeeds on Windows PowerShell 5.1 and sets `$ErrorActionPreference` to 'Stop' inside the module scope.
    - Test 2: `(Get-Command -Module adman).Name` equals EXACTLY the manifest FunctionsToExport list and does NOT contain 'Invoke-AdmanMutation' (gate stays non-exported) (SAFE-08, T-00-01).
    - Test 3: The manifest `FunctionsToExport` does not equal '*' and `RequiredModules` contains 'PSFramework' and does NOT contain 'ActiveDirectory'.
    - Test 4: Importing the module performs NO domain touch (mock `Get-ADDomain`/`Get-Module -ListAvailable ActiveDirectory`; assert neither is invoked during `Import-Module`) — import is side-effect-free.
  </behavior>
  <read_first>
    - .planning/phases/00-foundation-safety-harness/00-PATTERNS.md (§adman.psd1/adman.psm1 L374–L385; §Recommended Project Structure L192–L229)
    - .planning/phases/00-foundation-safety-harness/00-RESEARCH.md (§System Architecture Diagram L157–L188; §Standard Stack L89–L98)
    - .planning/phases/00-foundation-safety-harness/00-CONTEXT.md (D-01 L22–L26; Claude's Discretion L77–L79)
    - .planning/phases/00-foundation-safety-harness/00-01-PSFramework-verified.md (from Task 1 — pin evidence)
    - .claude/CLAUDE.md (What NOT to Use; PowerShell 5.1 subset)
  </read_first>
  <action>
    Create adman.psd1 (New-ModuleManifest shape) with these concrete fields: RootModule='adman.psm1'; ModuleVersion='0.1.0'; PowerShellVersion='5.1'; CompatiblePSEditions=@('Desktop'); RequiredModules=@(@{ModuleName='PSFramework'; RequiredVersion='1.14.457'}) — NOTE: use RequiredVersion (an EXACT pin), NOT ModuleVersion (which is only a minimum floor and would let a newer PSFramework satisfy the requirement); FunctionsToExport=@('Initialize-Adman','Start-Adman') for THIS plan (00-02 adds the *-AdmanConfig verbs and Test-AdmanCapability — those are appended to FunctionsToExport in 00-02, not now); CmdletsToExport=@(); VariablesToExport=@(); AliasesToExport=@(). Do NOT list ActiveDirectory anywhere (RSAT is a prereq). Do NOT use FunctionsToExport='*' (SAFE-08). Add PrivateData/PSData with a note that CompatiblePSEditions gains 'Core' only after the Phase 5 CI matrix passes.
    Create adman.psm1: set `$ErrorActionPreference = 'Stop'` at top; dot-source every `Private/**/*.ps1` first then every `Public/**/*.ps1` (sorted, recurse); collect `$public = Get-ChildItem $PSScriptRoot/Public -Filter *.ps1 -Recurse | ForEach-Object { $_.BaseName }` and call `Export-ModuleMember -Function $public` so the runtime export set matches the manifest (the manifest is the static boundary; the psm1 is the runtime boundary — keep them identical each plan). The loader MUST NOT call Initialize-Adman or any AD cmdlet at import (side-effect-free; init is invoked by Start-Adman/Initialize-Adman). Add a `-Server`-pinning helper slot: a module-scoped `$script:Config = @{}` hashtable (populated by 00-02) and a comment that all AD calls read `$script:Config.DC`.
    Public/Initialize-Adman.ps1 and Public/Start-Adman.ps1 are exported STUBS this plan: each declares `[CmdletBinding()]` and writes a single `Write-PSFMessage -Level Verbose` "not implemented until 00-03/Phase-1" line and returns; Start-Adman calls Initialize-Adman. Real bodies land in 00-03 (Initialize-Adman) and Phase 1 (Start-Adman menu).
    Pinning requirement: adman.psd1 RequiredModules PSFramework entry MUST use RequiredVersion equal to the verified version in 00-01-PSFramework-verified.md (1.14.457) — an exact pin, not a ModuleVersion floor.
    Project-instruction reconciliation (Addresses review concern C2-M2 / D-01): edit `.claude/CLAUDE.md` so it agrees with the adopted backbone. Make ONLY these two targeted row edits (keep the rest of the file byte-identical — do not rewrite CLAUDE.md broadly):
      (1) In the 'Built-in capabilities to prefer over new dependencies' table, the **Audit log** row currently defers a logging framework to a later milestone. Replace that row's mechanism+guidance cells with: mechanism = "hand-rolled, synchronous `Write-AdmanAudit` writing **JSON-lines** (`ConvertTo-Json -Compress -Depth 5` per entry, append, `Flush($true)`)"; guidance = "Structured, parseable, zero deps; **kept hand-rolled and synchronous on purpose** (Phase 0 / D-01) so fail-closed ('throw before AD if the record can't be written') is enforceable — PSFramework's durable logging is asynchronous and is NOT used for audit. PSFramework 1.14.457 IS adopted in Phase 0 for config + diagnostic/ops logging; only the audit sink is excluded from it. HIGH."
      (2) In the 'Alternatives Considered' table, the row that frames 'PSFramework logging/config' as a v2 upgrade is stale. Replace the whole row with: "| JSON-lines audit log (hand-rolled, synchronous) | **PSFramework** for the audit sink | PSFramework's durable logging is asynchronous (background runspace; first-record loss; process-exit-before-drain) and would break audit fail-closed, so the audit writer stays hand-rolled/synchronous (D-01). PSFramework 1.14.457 IS adopted in Phase 0 for config + diagnostic/ops logging; only the audit use of PSFramework is rejected. |"
    The two positive anchors that MUST be present verbatim after the edit (acceptance-grepped below): "PSFramework 1.14.457 IS adopted in Phase 0 for config + diagnostic/ops logging" and "stays hand-rolled/synchronous".
  </action>
  <acceptance_criteria>
    - `Test-ModuleManifest -Path ./adman.psd1` returns a valid manifest with no errors (run via `powershell -NoProfile -Command "Test-ModuleManifest ./adman.psd1 | Out-Null; $?"` => True).
    - `Select-String -Path adman.psd1 -Pattern "FunctionsToExport\s*=" shows an array literal that includes 'Initialize-Adman' and 'Start-Adman' and does NOT include the token '*' nor 'Invoke-AdmanMutation'.
    - `Select-String -Path adman.psd1 -Pattern "RequiredModules"` references PSFramework and version 1.14.457; `Select-String -Path adman.psd1 -Pattern "ActiveDirectory"` returns 0 matches. The pin is exact: `Select-String -Path adman.psd1 -Pattern "RequiredVersion\s*=\s*'1.14.457'"` returns >=1 and `Select-String -Path adman.psd1 -Pattern "ModuleVersion\s*=\s*'1.14.457'"` returns 0 (a ModuleVersion floor at 1.14.457 would not be a true pin).
    - `Select-String -Path adman.psd1 -Pattern "CompatiblePSEditions"` shows @('Desktop') and does NOT contain 'Core'.
    - `Select-String -Path adman.psm1 -Pattern "ErrorActionPreference\s*=\s*'Stop'"` returns >=1 match; `Select-String -Path adman.psm1 -Pattern "Export-ModuleMember -Function"` returns >=1 match; `Select-String -Path adman.psm1 -Pattern "Get-ADDomain|Get-ADUser|Set-AD"` returns 0 matches (no domain touch at import).
    - `Invoke-Pester -Path tests/Module.Manifest.Tests.ps1 -Output Detailed` exits 0 with all 4 behavior tests green.
    - Project instructions reconciled to D-01 (Addresses review concern C2-M2): `Select-String -Path .claude/CLAUDE.md -Pattern 'PSFramework 1.14.457 IS adopted in Phase 0 for config \+ diagnostic/ops logging'` >=1 AND `Select-String -Path .claude/CLAUDE.md -Pattern 'stays hand-rolled/synchronous'` >=1 (both the Audit log row and the Alternatives Considered row updated); only those two rows changed (`git diff -- .claude/CLAUDE.md` shows edits confined to the Audit log row and the Alternatives Considered PSFramework row).
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "Invoke-Pester -Path tests/Module.Manifest.Tests.ps1 -Output Minimal; if (-not $?) { exit 1 }"</automated>
  </verify>
  <done>Module loads on 5.1, exports exactly Initialize-Adman + Start-Adman (gate excluded), import is side-effect-free, PSFramework pinned to the verified version, RSAT absent from dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pester + PSScriptAnalyzer harness, custom SAFE-08 rule, AD mocks, AST guard scaffold</name>
  <files>PSScriptAnalyzerSettings.psd1, rules/AdmanSafetyRules.psm1, tests/PesterConfiguration.psd1, tests/Mocks/ActiveDirectory.psm1, tests/Safety.Gate.Tests.ps1, tests/Harness.Tests.ps1</files>
  <behavior>
    - Test 1 (lint): `Invoke-ScriptAnalyzer -Path . -Recurse -Settings PSScriptAnalyzerSettings.psd1` exits 0 against the (stub) module; `PSUseShouldProcessForStateChangingFunctions` is enabled in the settings file (SAFE-01).
    - Test 2 (custom rule): a positive-control fixture under tests/Fixtures/ that contains a direct `Set-ADUser` call — when the guard is pointed at that fixture path DIRECTLY (not via the repo-wide recurse lint) — is flagged by BOTH rules/AdmanSafetyRules.psm1 and tests/Safety.Gate.Tests.ps1; the same call inside a Private/ fixture is NOT flagged (guard scopes to Public/) (SAFE-08, T-00-01).
    - Test 3 (mocks): importing tests/Mocks/ActiveDirectory.psm1 supplies stub `Get-ADUser`/`Set-ADUser`/`Disable-ADAccount`/`Move-ADObject`/`Get-ADDomain` so a unit test runs with zero network (assert no real AD call — the mock returns a canned object).
    - Test 4 (token-grep fallback): a Public fixture using `& $cmd` dynamic invocation or `Invoke-Expression` of a banned name is still flagged (RESEARCH L304 caveat).
  </behavior>
  <read_first>
    - .planning/phases/00-foundation-safety-harness/00-PATTERNS.md (§tests/Safety.Gate.Tests.ps1 L389–L419 incl. caveat; §rules/AdmanSafetyRules.psm1 + PSScriptAnalyzerSettings.psd1 L423–L430; §tests/.../PesterConfiguration.psd1 L444–L454)
    - .planning/phases/00-foundation-safety-harness/00-RESEARCH.md (Pattern 1 guard L279–L304; Code Examples L593–L599; §Validation Architecture L679–L728; §Don't Hand-Roll L527)
    - .planning/phases/00-foundation-safety-harness/00-VALIDATION.md (Test Infrastructure L17–L26; Wave 0 Requirements L49–L54; Validation Sign-Off L71–L77)
    - .claude/CLAUDE.md (PSScriptAnalyzer rule set L76; documented WriteHost suppression)
  </read_first>
  <action>
    PSScriptAnalyzerSettings.psd1 (repo root): enable exactly these rules — PSUseShouldProcessForStateChangingFunctions, PSAvoidUsingPlainTextForPassword, PSUsePSCredentialType, PSAvoidGlobalVars, PSUseApprovedVerbs, PSAvoidUsingCmdletAliases, PSUseConsistentIndentation. Set CustomRulePath to 'rules/AdmanSafetyRules.psm1'. Add a DOCUMENTED suppression of PSAvoidUsingWriteHost scoped to the future TUI module path (forward-declared; the menu module arrives in Phase 1) — comment the reason inline ("menu legitimately paints the console"). Add a DOCUMENTED inline comment naming tests/Fixtures/** as intentional positive-control fixtures for the guard tests that are deliberately NOT lint-clean (they contain banned verbs by design); the repo-wide `Invoke-ScriptAnalyzer -Path . -Recurse` run stays green because the custom rule scopes to Public/ and therefore never inspects tests/Fixtures/**, and any future rule scoped to tests/ MUST exclude tests/Fixtures/** (this comment is the explicit fixture exclusion). The guard tests prove the positive control by pointing the custom rule + AST check DIRECTLY at the fixture path, not by relying on the recurse lint. Do not suppress any other rule globally.
    rules/AdmanSafetyRules.psm1: implement a PSScriptAnalyzer custom rule function that takes a ScriptBlockAst, recurses CommandAst nodes, and emits a DiagnosticRecord (Severity=Error) when a CommandAst under a `Public` path resolves to the banned AD write cmdlet set. Implement the RESEARCH L304 fallback: if `GetCommandName()` is null, inspect `CommandElements[0].Extent.Text`, and additionally scan the token stream for `&` and `Invoke-Expression`; resolve aliases via `Get-Command`. The banned list must be the SAME set used by tests/Safety.Gate.Tests.ps1 (define it once and reference it in both — see single-source note below). Banned set (AD write cmdlets, per RESEARCH L283–L286): Set-ADUser, Set-ADComputer, Set-ADObject, Set-ADAccountPassword, Disable-ADAccount, Enable-ADAccount, Unlock-ADAccount, Move-ADObject, New-ADUser, New-ADComputer, Add-ADGroupMember, Remove-ADGroupMember, Add-ADPrincipalGroupMembership, plus the hard-delete verb per SAFE-09 (lift the exact token from PATTERNS L285/L401 via read_first; this rule file and the AST test are the allowlisted home of that literal).
    tests/PesterConfiguration.psd1: Pester 6 config — Run.Path='tests'; Filter.Tag='Unit' default for quick runs; CodeCoverage.Enabled=$true (profiler-based, Pester 6 default) targeting Public/*.ps1 and Private/*.ps1; Output.Verbosity='Detailed'; TestResult.Enabled=$true (JUnit) for CI; Should.ErrorAction='Stop'. Quick run command `Invoke-Pester -Path tests -Output Normal -TagFilter Unit`; full `Invoke-Pester -Configuration (Import-PowerShellDataFile tests/PesterConfiguration.psd1)`.
    tests/Mocks/ActiveDirectory.psm1: export stub functions for Get-ADUser, Get-ADComputer, Get-ADObject, Get-ADGroup, Get-ADGroupMember, Get-ADDomain, Get-ADForest, Get-ADOrganizationalUnit, Get-ADOptionalFeature, Search-ADAccount, Set-ADUser, Set-ADComputer, Set-ADObject, Set-ADAccountPassword, Disable-ADAccount, Enable-ADAccount, Unlock-ADAccount, Move-ADObject, New-ADUser, New-ADComputer, Add-ADGroupMember, Remove-ADGroupMember, plus Get-CimInstance/New-CimSession and Invoke-Command/New-PSSession/Test-WSMan. Each stub returns a canned PSCustomObject with the properties the patterns read (objectSid, objectClass, DistinguishedName, memberOf, DomainSID, etc.). NO stub performs a network call. This guarantees unit tests never touch a live domain (project constraint).
    tests/Safety.Gate.Tests.ps1: implement the AST guard per PATTERNS L395–L416 — `Get-ChildItem Public -Filter *.ps1 -Recurse`, parse each with `[System.Management.Automation.Language.Parser]::ParseFile`, `FindAll({ CommandAst }, $true)`, assert zero hits against the banned set via `Should -BeNullOrEmpty`; include the L304 fallback (null GetCommandName -> CommandElements[0].Extent.Text; token grep for `&`/`Invoke-Expression`; alias resolution). Add a positive-control fixture under tests/Fixtures/ (NOT under Public/) that the test points the guard at DIRECTLY and asserts MUST be flagged (proves the guard fires), and a negative-control (Private/ wrapper) that MUST pass. Because the fixture lives under tests/Fixtures/ (outside Public/) and the guard scopes to Public/, the repo-wide recurse lint never trips on it (see the documented tests/Fixtures/** exclusion in PSScriptAnalyzerSettings.psd1).
    Single-source the banned set: define it in rules/AdmanSafetyRules.psm1 as an exported variable/function (e.g., Get-AdmanBannedWriteVerbs) and have tests/Safety.Gate.Tests.ps1 import it, so the two guards can never drift (key_link).
  </action>
  <acceptance_criteria>
    - `Select-String -Path PSScriptAnalyzerSettings.psd1 -Pattern "PSUseShouldProcessForStateChangingFunctions"` returns >=1 match and the rule is not in an ExcludeRules list.
    - `Select-String -Path PSScriptAnalyzerSettings.psd1 -Pattern "CustomRulePath"` points at rules/AdmanSafetyRules.psm1; the file exists at that path.
    - `Invoke-ScriptAnalyzer -Path . -Recurse -Settings PSScriptAnalyzerSettings.psd1` exits 0 (the current stub module is clean; the custom rule's Public-scope keeps the tests/Fixtures/** positive controls out of the recurse lint).
    - PSScriptAnalyzerSettings.psd1 documents the tests/Fixtures/** exclusion: `Select-String -Path PSScriptAnalyzerSettings.psd1 -Pattern 'tests/Fixtures'` returns >=1 (an inline comment naming the positive-control fixtures).
    - tests/Safety.Gate.Tests.ps1 parses Public/*.ps1 via `[System.Management.Automation.Language.Parser]::ParseFile` (source assertion: `Select-String -Path tests/Safety.Gate.Tests.ps1 -Pattern "Parser::ParseFile"` >=1) and asserts the banned list with `Should -BeNullOrEmpty` (`Select-String -Pattern "BeNullOrEmpty"` >=1).
    - The banned set is single-sourced: tests/Safety.Gate.Tests.ps1 calls `Get-AdmanBannedWriteVerbs` (or equivalent) rather than redeclaring the literal array (`Select-String -Path tests/Safety.Gate.Tests.ps1 -Pattern "Get-AdmanBannedWriteVerbs"` >=1).
    - `Invoke-Pester -Path tests/Safety.Gate.Tests.ps1,tests/Harness.Tests.ps1 -Output Detailed` exits 0; the positive-control fixture under tests/Fixtures/ (direct Set-ADUser) FAILS the guard as expected when the guard is pointed at it directly, and the negative-control (Private wrapper) passes.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "Invoke-ScriptAnalyzer -Path . -Recurse -Settings PSScriptAnalyzerSettings.psd1; Invoke-Pester -Path tests/Safety.Gate.Tests.ps1,tests/Harness.Tests.ps1 -Output Minimal"</automated>
  </verify>
  <done>Lint clean with ShouldProcess enforced; custom rule + AST guard share one banned list and fire on a positive control; AD/CIM/remoting mocks make unit tests domain-free; Pester 6 config with coverage ready.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase (plan 00-01) produces

Functions (new, exported): `Initialize-Adman` (stub, real body 00-03), `Start-Adman` (stub, real body Phase 1).
Functions (new, non-exported/private): `Get-AdmanBannedWriteVerbs` (single-source banned-verb data for the guards, in rules/).
Files: `adman.psd1`, `adman.psm1`, `Public/Initialize-Adman.ps1`, `Public/Start-Adman.ps1`, `PSScriptAnalyzerSettings.psd1`, `rules/AdmanSafetyRules.psm1`, `tests/PesterConfiguration.psd1`, `tests/Mocks/ActiveDirectory.psm1`, `tests/Safety.Gate.Tests.ps1`, `tests/Module.Manifest.Tests.ps1`, `tests/Harness.Tests.ps1`, `tests/Fixtures/` (positive/negative controls), `.planning/phases/00-foundation-safety-harness/00-01-PSFramework-verified.md`, `.claude/CLAUDE.md` (reconciled to D-01: PSFramework 1.14.457 adopted in Phase 0 for config + diagnostic/ops logging; audit writer stays hand-rolled/synchronous).
Config keys: none this plan (module-scoped `$script:Config = @{}` hashtable slot only).
Module boundary: explicit `FunctionsToExport` established; `Invoke-AdmanMutation` deliberately NOT exported (SAFE-08).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module import → session | Export boundary is the static SAFE-08 control; drift here is a bypass. |
| source tree (Public/) → AD | The AST guard + custom PSSA rule are the detective controls proving no Public verb calls AD writes directly. |
| Gallery → workstation | PSFramework install is a supply-chain crossing; seam did not run, so install is human-approved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-01 | Tampering | Public/ verb calling AD write cmdlets directly (gate bypass) | high | mitigate | Explicit FunctionsToExport excluding Invoke-AdmanMutation (Task 2) + AST guard tests/Safety.Gate.Tests.ps1 + custom PSSA rule rules/AdmanSafetyRules.psm1 sharing one banned list (Task 3); verified by acceptance criteria: manifest excludes '*'/Invoke-AdmanMutation, guard fires on positive control. (SAFE-08) |
| T-00-SC | Tampering | PSFramework supply-chain / wrong version pinned | high | mitigate | Build-time re-verification (Task 1) writes 00-01-PSFramework-verified.md before pin; manifest pins exact 1.14.457; first install is human-approved (user_setup + end-of-phase human check), never auto-approved. Verified by acceptance criteria: verified artifact present with version 1.14.457, no install performed by Task 1. |
| T-00-11 | Information Disclosure | Live domain touched by a unit test | medium | mitigate | tests/Mocks/ActiveDirectory.psm1 mocks every AD/CIM/remoting cmdlet; acceptance criteria assert import + guard tests run with zero network. (project constraint) |
| T-00-12 | Tampering | Lint rule disabled globally, weakening ShouldProcess enforcement | medium | mitigate | PSScriptAnalyzerSettings.psd1 enables PSUseShouldProcessForStateChangingFunctions with no global suppression except the documented, TUI-scoped WriteHost suppression (Task 3). (SAFE-01) |
</threat_model>

<verification>
- `powershell -NoProfile -Command "Test-ModuleManifest ./adman.psd1 | Out-Null; $?"` => True.
- `powershell -NoProfile -Command "Invoke-ScriptAnalyzer -Path . -Recurse -Settings PSScriptAnalyzerSettings.psd1"` exits 0.
- `powershell -NoProfile -Command "Invoke-Pester -Path tests -Output Minimal -TagFilter Unit"` exits 0 (Module.Manifest, Harness, Safety.Gate all green; the Safety.Gate positive-control fixture proves the guard fires).
- Manual end-of-phase human check (workflow.human_verify_mode=end-of-phase): approve the PSFramework/Pester/PSScriptAnalyzer install commands printed by Task 1 before they are run on the workstation (T-00-SC). No `checkpoint:human-verify` task is used; this is the phase-level human verification.
</verification>

<success_criteria>
- Module loads on Windows PowerShell 5.1 and exports exactly Initialize-Adman + Start-Adman; Invoke-AdmanMutation is not exported and not present yet.
- Lint is clean with PSUseShouldProcessForStateChangingFunctions enabled and a scoped, documented WriteHost suppression only.
- The SAFE-08 AST guard + custom PSSA rule exist, share one banned-verb source, and demonstrably fire on a positive-control fixture while passing a Private/ wrapper.
- PSFramework 1.14.457 is re-verified to disk before pinning; no package installed by automation.
- No unit test touches a live domain (mocks in place).
</success_criteria>

<output>
Create `.planning/phases/00-foundation-safety-harness/00-01-SUMMARY.md` when done.
</output>
