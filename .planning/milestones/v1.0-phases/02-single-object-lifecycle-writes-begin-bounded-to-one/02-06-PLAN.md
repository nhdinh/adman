---
phase: 02-single-object-lifecycle-writes-begin-bounded-to-one
plan: 06
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04", "02-05"]
files_modified:
  - Private/Menu/Get-AdmanMenuDefinition.ps1
  - Private/Menu/Read-AdmanActionParams.ps1
  - Public/Start-Adman.ps1
  - tests/Menu.Tests.ps1
  - tests/Start.Adman.Tests.ps1
  - tests/Module.Manifest.Tests.ps1
  - tests/Safety.Gate.Tests.ps1
  - tests/Safety.NoHardDelete.Tests.ps1
autonomous: true
requirements:
  - USER-02
  - USER-03
  - USER-04
  - USER-05
  - USER-06
  - LUSR-01
  - LUSR-02
  - COMP-02
  - COMP-03
  - COMP-04
  - GRP-01
  - GRP-02
  - GRP-03
user_setup: []
must_haves:
  truths:
    - The Start-Adman menu lists every new write verb with section grouping (Search / Reports / User writes / Computer writes / Local writes / Group membership)
    - Every menu write item routes to the same Public verb a senior calls directly (MENU-04)
    - The B/Q reserved-input contract is unchanged
    - Read-AdmanActionParams handles the polymorphic Type field (GeneratedPassword / SecureString) for password prompts (D-05)
    - adman.psd1 exports every new Public verb explicitly (never '*') — LANDED IN WAVE 2 PLANS (HIGH #2 review fix); this plan re-verifies the exports are present
    - The SAFE-08/09 AST guard passes against Public/ after all new verbs land
    - The full test suite is green
  artifacts:
    - Private/Menu/Get-AdmanMenuDefinition.ps1 (extended)
    - Private/Menu/Read-AdmanActionParams.ps1 (extended)
    - Public/Start-Adman.ps1 (extended: FixedParameters merge + separator skip)
  key_links:
    - Get-AdmanMenuDefinition gains ~15 new write entries with non-selectable section separator lines
    - Read-AdmanActionParams dispatches on PromptSpec Type='GeneratedPassword' to the D-05 Generate/Prompt sub-choice
    - PROMPTSPEC-PARAMETER NAME CONTRACT (HIGH #1 cycle-2 review fix): every PromptSpec Name exactly matches a parameter name on the target verb — 'AccountPassword' for New-AdmanUser, 'NewPassword' for Set-AdmanUserPassword, 'Password' for New-AdmanLocalUser and Set-AdmanLocalUser; the auto-generated "${name}Source" markers ('AccountPasswordSource', 'NewPasswordSource', 'PasswordSource') are declared as optional [ValidateSet('Generate','Prompt')] parameters on the corresponding verbs in Plans 02-02 and 02-04, so Start-Adman's `& $Verb @params` splat binds cleanly (MENU-04)
    - Menu entries gain an optional FixedParameters hashtable field (MEDIUM #6 review fix); Start-Adman merges FixedParameters with prompted params before dispatch (e.g., FixedParameters=@{Enable=$true} for the "Enable local user" entry)
    - adman.psd1 FunctionsToExport gains all 17 new Public verb names — LANDED IN WAVE 2 PLANS (HIGH #2 review fix); this plan re-verifies via tests/Module.Manifest.Tests.ps1
---

<objective>
Wire the Phase 2 write verbs into the Start-Adman menu, extend the prompt engine for D-05 password types, export the new verbs in the manifest, and re-prove the SAFE-08/09 AST guard against the expanded Public/ tree. This is the phase exit gate.

Purpose: MENU-04 requires every menu action to route to the same parameterized function a senior calls directly. The menu integration makes the Phase 2 verbs discoverable; the AST guard re-proof confirms no new verb bypasses either gate.
Output: Extended menu definition + prompt engine + manifest + menu tests. No new Public verbs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-CONTEXT.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-PATTERNS.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-01-SUMMARY.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-02-SUMMARY.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-03-SUMMARY.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-04-SUMMARY.md
@.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-05-SUMMARY.md
@Private/Menu/Get-AdmanMenuDefinition.ps1
@Private/Menu/Read-AdmanActionParams.ps1
@Public/Start-Adman.ps1
@adman.psd1
@tests/Menu.Tests.ps1
@tests/Module.Manifest.Tests.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Menu integration + D-05 prompt engine + FixedParameters dispatcher + manifest re-verification</name>
  <files>Private/Menu/Get-AdmanMenuDefinition.ps1, Private/Menu/Read-AdmanActionParams.ps1, Public/Start-Adman.ps1, tests/Menu.Tests.ps1, tests/Start.Adman.Tests.ps1, tests/Module.Manifest.Tests.ps1</files>
  <read_first>
    - Private/Menu/Get-AdmanMenuDefinition.ps1 (existing entry shape at lines 57-98 — gains the FixedParameters field)
    - Private/Menu/Read-AdmanActionParams.ps1 (the foreach loop at line 44 to extend with the Type dispatch)
    - Public/Start-Adman.ps1 (the menu rendering loop at lines 97-134 — separator skip + FixedParameters merge)
    - adman.psd1 (the FunctionsToExport array at line 53 — re-verify all 17 new verbs are present after Wave 2 plans)
    - tests/Menu.Tests.ps1 (existing menu contract tests)
    - tests/Module.Manifest.Tests.ps1 (existing manifest tests)
  </read_first>
  <behavior>
    - Test 1: Get-AdmanMenuDefinition returns entries for all Phase 2 write verbs (New-AdmanUser, Disable-AdmanUser, Enable-AdmanUser, Move-AdmanUser, Set-AdmanUserPassword, Unlock-AdmanUser, Disable-AdmanComputer, Enable-AdmanComputer, Move-AdmanComputer, Reset-AdmanComputerAccount, New-AdmanLocalUser, Set-AdmanLocalUser (THREE entries: Reset/Enable/Disable — one per parameter set), Remove-AdmanLocalUser, Add-AdmanLocalGroupMember, Remove-AdmanLocalGroupMember, Add-AdmanGroupMember, Remove-AdmanGroupMember)
    - Test 2: section separator entries have Verb=$null and are not selectable
    - Test 3: Read-AdmanActionParams dispatches on Type='GeneratedPassword' to a numeric sub-choice (Generate/Prompt); the Generate path calls New-AdmanRandomPassword; the Prompt path calls Read-Host -AsSecureString + Test-AdmanPasswordComplexity
    - Test 4: Read-AdmanActionParams preserves the B/Q reserved-input contract on the password sub-choice
    - Test 5: adman.psd1 FunctionsToExport contains all 17 new Public verb names explicitly (never '*') — re-verification after Wave 2 plans landed the exports (HIGH #2 review fix)
    - Test 6: the SAFE-08/09 AST guard passes against Public/ (no banned AD write or LocalAccounts cmdlet named directly)
    - Test 7 (MEDIUM #6 review fix — FixedParameters): the "Enable local user" menu entry carries FixedParameters=@{Enable=$true}; Start-Adman merges FixedParameters with the prompted params before dispatch, so & 'Set-AdmanLocalUser' @params receives Enable=$true WITHOUT a prompt; the "Disable local user" entry carries FixedParameters=@{Disable=$true}; separator entries carry no FixedParameters
    - Test 8 (MEDIUM #6 review fix — FixedParameters merge order): when a menu entry has both PromptSpec and FixedParameters, the dispatched params contain BOTH the prompted values AND the fixed values; FixedParameters keys do not collide with PromptSpec names (the menu definition is validated to prevent a fixed key from shadowing a prompted key)
    - Test 9 (HIGH #1 cycle-2 review fix — PromptSpec-parameter-name contract): for EVERY menu entry with a non-null Verb, the test introspects the target verb's declared parameter list (via (Get-Command <Verb>).Parameters.Keys) and asserts that EVERY key the menu path would splat — every PromptSpec Name, every FixedParameters key, AND every auto-generated "${PromptSpec.Name}Source" marker for Type='GeneratedPassword' fields — resolves to a declared parameter on that verb. Specifically: the New-AdmanUser entry's 'AccountPassword' + 'AccountPasswordSource' keys MUST be in New-AdmanUser's parameter list; the Set-AdmanUserPassword entry's 'NewPassword' + 'NewPasswordSource' keys MUST be in Set-AdmanUserPassword's parameter list; the New-AdmanLocalUser and Set-AdmanLocalUser entries' 'Password' + 'PasswordSource' keys MUST be in those verbs' parameter lists. This catches menu/verb drift at test time instead of at runtime (MENU-04 regression).
  </behavior>
  <action>
    Modify Private/Menu/Get-AdmanMenuDefinition.ps1: append new PSCustomObject entries following the existing shape (Label, Verb, PromptSpec, Properties). ADD an optional fifth field: FixedParameters (hashtable, default $null) — a declarative set of parameters the dispatcher injects WITHOUT prompting (MEDIUM #6 review fix). Insert non-selectable section separator entries (Label='--- User writes ---' etc., Verb=$null, PromptSpec=@(), Properties=@(), FixedParameters=$null) before each group: User writes (Create user -> New-AdmanUser with PromptSpec for Name/SamAccountName/UserPrincipalName/ParentOuDn + AccountPassword with Type='GeneratedPassword' and Choices=@('Generate (recommended)','Prompt') — the PromptSpec Name MUST be 'AccountPassword' to match New-AdmanUser's parameter name (HIGH #1 cycle-2 review fix); Disable user -> Disable-AdmanUser; Enable user -> Enable-AdmanUser; Reset user password -> Set-AdmanUserPassword with PromptSpec for Identity + NewPassword Type='GeneratedPassword' — the PromptSpec Name MUST be 'NewPassword' to match Set-AdmanUserPassword's parameter name (HIGH #1 cycle-2 review fix); Unlock user -> Unlock-AdmanUser; Move user -> Move-AdmanUser with TargetPath), Computer writes (Disable/Enable/Move computer, Reset computer account), Local writes (Create local user -> New-AdmanLocalUser with PromptSpec for Name + Password Type='GeneratedPassword' — the PromptSpec Name MUST be 'Password' to match New-AdmanLocalUser's parameter name (HIGH #1 cycle-2 review fix); THREE SEPARATE MENU ENTRIES for Set-AdmanLocalUser's three parameter sets — "Reset local user password" -> Set-AdmanLocalUser with PromptSpec for Name + Password Type='GeneratedPassword' (PromptSpec Name 'Password' matches the verb's parameter name; binds the 'Reset' parameter set), "Enable local user" -> Set-AdmanLocalUser with PromptSpec for Name only and FixedParameters=@{Enable=$true} (binds the 'Enable' parameter set), "Disable local user" -> Set-AdmanLocalUser with PromptSpec for Name only and FixedParameters=@{Disable=$true} (binds the 'Disable' parameter set); Remove local user -> Remove-AdmanLocalUser; Add local group member -> Add-AdmanLocalGroupMember; Remove local group member -> Remove-AdmanLocalGroupMember), Group membership (Add to group -> Add-AdmanGroupMember with Identity + GroupIdentity; Remove from group -> Remove-AdmanGroupMember). PROMPTSPEC-PARAMETER NAME CONTRACT (HIGH #1 cycle-2 review fix): every PromptSpec Name MUST exactly match a parameter name on the target verb — Start-Adman dispatches via `& $Verb @params`, so a PromptSpec Name that is not a declared parameter on the verb throws "parameter cannot be found" from the menu path while direct senior calls succeed. The password PromptSpec names are per-verb: 'AccountPassword' for New-AdmanUser, 'NewPassword' for Set-AdmanUserPassword, 'Password' for New-AdmanLocalUser and Set-AdmanLocalUser. The accompanying `${name}Source` markers ('AccountPasswordSource', 'NewPasswordSource', 'PasswordSource') are declared as optional [ValidateSet('Generate','Prompt')] parameters on the corresponding verbs in Plans 02-02 and 02-04, so the menu can safely set them and the splat will bind. SET-ADMANLOCALUSER MENU SHAPE (warning fix): the verb has three mutually-exclusive parameter sets (Reset/Enable/Disable); a single menu entry cannot bind them all without an ambiguous post-prompt. Three separate menu entries — one per parameter set — keep the menu shape declarative and let the PromptSpec for each entry carry exactly the fields that set needs. The FixedParameters field injects the -Enable / -Disable switch as a fixed parameter for the Enable/Disable entries (the operator picked the action by picking the menu item; no further prompt). VALIDATION: add a guard in Get-AdmanMenuDefinition (or a Pester test) asserting that no FixedParameters key collides with a PromptSpec Name on the same entry (a fixed key shadowing a prompted key would silently drop the operator's input). ADD A SECOND VALIDATION (HIGH #1 cycle-2 review fix): a Pester test that, for every menu entry with a non-null Verb, splats a synthetic PromptSpec-produced hashtable against the target verb's parameter list and asserts every key (PromptSpec Names + FixedParameters keys + the auto-generated "${name}Source" markers) resolves to a declared parameter on that verb — this catches menu/verb drift at test time instead of at runtime. Modify Private/Menu/Read-AdmanActionParams.ps1: inside the foreach ($field in $PromptSpec) loop, read the optional Type field (default 'Text'); when Type='GeneratedPassword', render the Choices array as a numeric sub-choice, dispatch n=1 to New-AdmanRandomPassword -Length $script:Config.security.passwordGeneration.length (store in $params[$name] and set $params["${name}Source"]='Generate'), dispatch n=2 to Read-Host -AsSecureString twice with equality check (transient BSTR zeroed in finally) + Test-AdmanPasswordComplexity (store in $params[$name] and set $params["${name}Source"]='Prompt'); preserve the B/Q reserved-input contract on the sub-choice. The $name here is the PromptSpec Name — which is per-verb ('AccountPassword', 'NewPassword', or 'Password') per the contract above; the "${name}Source" marker key is therefore 'AccountPasswordSource', 'NewPasswordSource', or 'PasswordSource' respectively, and each target verb declares the matching optional parameter (Plans 02-02 / 02-04). Modify Public/Start-Adman.ps1: TWO changes. (1) SEPARATOR SKIP: add a guard in the menu rendering loop to skip separator entries (Verb=$null) from the numbered list — render them as plain text lines (no number prefix, no selectability). The guard: when building the numbered menu, skip entries where Verb is $null; when rendering, emit separator labels as plain Write-Host lines without a number. (2) FIXEDPARAMETERS MERGE (MEDIUM #6 review fix): after Read-AdmanActionParams returns $params, merge the entry's FixedParameters hashtable into $params BEFORE dispatch — when $entry.FixedParameters is not $null, foreach ($key in $entry.FixedParameters.Keys) { $params[$key] = $entry.FixedParameters[$key] }; then dispatch via & $Verb @params. The merge happens AFTER prompting so fixed values are always present and never prompted for. MANIFEST RE-VERIFICATION (HIGH #2 review fix): the 17 new verb exports landed in Wave 2 plans (02-02 through 02-05); this plan does NOT modify adman.psd1. Instead, extend tests/Module.Manifest.Tests.ps1 with Test 5 asserting each of the 17 new verb names is present in FunctionsToExport (re-verification that Wave 2 plans landed the exports correctly). Extend tests/Menu.Tests.ps1 with Tests 1, 3, 4, 7, 8 above (menu definition contract: entry presence, GeneratedPassword Type dispatch, B/Q contract, FixedParameters presence on Enable/Disable local user entries, no FixedParameters/PromptSpec key collision) AND the new PromptSpec-parameter-name contract test (for every menu entry, every PromptSpec Name + FixedParameters key + "${name}Source" marker resolves to a declared parameter on the target verb — HIGH #1 cycle-2 review fix). Create tests/Start.Adman.Tests.ps1 for Test 2 (separator selectability) AND Tests 7/8 (FixedParameters merge): assert that Start-Adman renders separator entries as plain text lines WITHOUT a number prefix and that the numbered selection list contains ONLY entries with a non-null Verb (mock Write-Host and Read-Host; assert the separator label appears in the Write-Host stream but NOT in the numbered prompt); assert that when the operator selects the "Enable local user" entry, the dispatched params contain Enable=$true (mock Read-AdmanActionParams to return @{Name='testuser'}, mock & 'Set-AdmanLocalUser' and assert the received params contain Enable=$true).
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Menu.Tests.ps1, tests/Start.Adman.Tests.ps1, tests/Module.Manifest.Tests.ps1 -Tag Unit</automated>
  </verify>
  <done>
    All nine behavior tests pass. The menu lists all Phase 2 write verbs with section grouping. Read-AdmanActionParams handles the GeneratedPassword Type with the D-05 Generate/Prompt sub-choice. The FixedParameters field injects -Enable/-Disable without prompting (MEDIUM #6 review fix). Every PromptSpec Name + FixedParameters key + "${name}Source" marker resolves to a declared parameter on the target verb (HIGH #1 cycle-2 review fix — the menu-splat contract test passes for all menu entries). adman.psd1 exports all 17 new verbs explicitly (re-verified after Wave 2 plans landed them — HIGH #2 review fix). The AST guard passes against Public/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase exit gate — full suite green + AST guard re-proof + lint clean</name>
  <files>tests/Safety.Gate.Tests.ps1, tests/Safety.NoHardDelete.Tests.ps1</files>
  <read_first>
    - tests/Safety.Gate.Tests.ps1 (the AST guard — confirm it covers both AD write and LocalAccounts banned sets after Plan 02-01)
    - tests/Safety.NoHardDelete.Tests.ps1 (the no-hard-delete guard — confirm Remove-ADObject appears nowhere in Public/)
    - PSScriptAnalyzerSettings.psd1 (the lint config)
  </read_first>
  <behavior>
    - The full unit suite passes: Invoke-Pester -Path tests/ -Tag Unit
    - The AST guard passes: no Public/*.ps1 names a banned AD write cmdlet or a banned LocalAccounts cmdlet
    - The no-hard-delete guard passes: Remove-ADObject appears nowhere in Public/
    - PSScriptAnalyzer is clean: Invoke-ScriptAnalyzer -Path Public/, Private/ -Settings PSScriptAnalyzerSettings.psd1 returns zero Error-severity findings
  </behavior>
  <action>
    Run the full unit suite (Invoke-Pester -Path tests/ -Tag Unit) and confirm every test passes — this includes the Phase 0/1 tests (no regressions), the Plan 02-01 infrastructure tests, and the Plan 02-02..02-05 verb tests. Run the AST guard (tests/Safety.Gate.Tests.ps1) and confirm it covers both the AD write banned set and the LocalAccounts banned set (extended in Plan 02-01); confirm zero hits in Public/. Run the no-hard-delete guard (tests/Safety.NoHardDelete.Tests.ps1) and confirm Remove-ADObject appears nowhere in Public/. Run PSScriptAnalyzer against Public/ and Private/ with the repo settings file and confirm zero Error-severity findings. If any test fails, diagnose and fix the root cause (do NOT weaken a test to make it pass). If the AST guard flags a Public/ file, route the offending call through the appropriate gate. If PSScriptAnalyzer flags a ShouldProcess violation, add [CmdletBinding(SupportsShouldProcess, ConfirmImpact='High')] to the flagged function.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/ -Tag Unit; Invoke-ScriptAnalyzer -Path Public/, Private/ -Settings PSScriptAnalyzerSettings.psd1 -Severity Error</automated>
  </verify>
  <done>
    Full unit suite green (Phase 0/1 + Phase 2 tests, zero failures). AST guard passes against Public/ for both AD write and LocalAccounts banned sets. No-hard-delete guard passes (Remove-ADObject nowhere in Public/). PSScriptAnalyzer returns zero Error-severity findings against Public/ and Private/.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan modifies the following existing symbols (no new files):

**Modified Private functions:**
- `Get-AdmanMenuDefinition` — gains ~17 new write entries + section separator lines + the FixedParameters field (MEDIUM #6 review fix); password PromptSpec Names are per-verb to match the target verb's parameter name exactly ('AccountPassword' for New-AdmanUser, 'NewPassword' for Set-AdmanUserPassword, 'Password' for New-AdmanLocalUser and Set-AdmanLocalUser — HIGH #1 cycle-2 review fix)
- `Read-AdmanActionParams` — gains polymorphic Type dispatch (GeneratedPassword / SecureString); the auto-generated "${name}Source" marker key derives from the per-verb PromptSpec Name and binds to the matching optional parameter declared on the verb in Plans 02-02 / 02-04 (HIGH #1 cycle-2 review fix)

**Modified Public functions:**
- `Start-Adman` — gains separator-entry skip (Verb=$null rendered as plain text, not numbered) and FixedParameters merge into dispatched params (MEDIUM #6 review fix)

**Modified test files:**
- tests/Menu.Tests.ps1 — extended for write entries + separator handling + password Type dispatch + FixedParameters contract + the PromptSpec-parameter-name contract test (HIGH #1 cycle-2 review fix: every menu key resolves to a declared parameter on the target verb)
- tests/Start.Adman.Tests.ps1 — NEW: separator selectability + FixedParameters merge assertions
- tests/Module.Manifest.Tests.ps1 — extended for new export assertions (re-verification after Wave 2 plans landed the exports — HIGH #2 review fix)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> menu | The operator selects a numbered menu item and supplies prompted inputs |
| menu -> Public verb | The menu dispatches via & $Verb @params (MENU-04: same function a senior calls directly) |
| prompt engine -> SecureString | The GeneratedPassword Type produces a SecureString via CSPRNG or Read-Host -AsSecureString |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Information Disclosure | Password prompt in menu path | critical | mitigate | The GeneratedPassword Type produces a SecureString via New-AdmanRandomPassword (CSPRNG) or Read-Host -AsSecureString + Test-AdmanPasswordComplexity; the SecureString is stored in $params[$name] and passed into the Public verb's parameter; never echoed or logged (D-05) |
| T-02-04 | Repudiation | AST guard bypass | high | mitigate | The phase exit gate re-proves the SAFE-08/09 AST guard against the expanded Public/ tree for BOTH the AD write and LocalAccounts banned sets; any new verb bypassing either gate is caught before merge |
</threat_model>

<verification>
- `Invoke-Pester -Path tests/Menu.Tests.ps1, tests/Module.Manifest.Tests.ps1 -Tag Unit` — 6/6 green
- `Invoke-Pester -Path tests/ -Tag Unit` — full suite green (Phase 0/1 + Phase 2, zero failures)
- `Invoke-ScriptAnalyzer -Path Public/, Private/ -Settings PSScriptAnalyzerSettings.psd1 -Severity Error` — zero findings
</verification>

<success_criteria>
- The Start-Adman menu lists every Phase 2 write verb with section grouping
- Every menu write item routes to the same Public verb a senior calls directly (MENU-04)
- Read-AdmanActionParams handles the GeneratedPassword Type with the D-05 sub-choice
- The FixedParameters field injects -Enable/-Disable without prompting (MEDIUM #6 review fix)
- Every PromptSpec Name exactly matches a parameter name on the target verb, and every auto-generated "${name}Source" marker binds to a declared optional parameter on that verb (HIGH #1 cycle-2 review fix — the menu-splat contract test passes for all 17 menu entries)
- adman.psd1 exports all 17 new Public verbs explicitly (re-verified after Wave 2 plans landed them — HIGH #2 review fix)
- The SAFE-08/09 AST guard passes against Public/ for both banned sets
- The full unit suite is green (no regressions)
- PSScriptAnalyzer is clean (zero Error-severity findings)
</success_criteria>

<output>
Create `.planning/phases/02-single-object-lifecycle-writes-begin-bounded-to-one/02-06-SUMMARY.md` when done.
</output>
