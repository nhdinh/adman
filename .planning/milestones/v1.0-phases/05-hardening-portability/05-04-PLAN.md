---
phase: 05-hardening-portability
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Docs.Coverage.Tests.ps1
  - tests/Workflow.OffboardingState.Tests.ps1
  - Private/Config/Initialize-AdmanConfig.ps1
autonomous: true
gap_closure: true
gap_ids:
  - G-05-1
requirements:
  - DOC-02
  - DOC-03
user_setup: []
must_haves:
  truths:
    - tests/Docs.Coverage.Tests.ps1 excludes 'ProgressAction' from the common-parameter list, so the docs/USAGE.md coverage contract passes on both Windows PowerShell 5.1 and PowerShell 7.6 LTS.
    - tests/Workflow.OffboardingState.Tests.ps1 writes archived audit records with a valid SHA-256 self-hash, so Get-AdmanOffboardingState integrity verification succeeds.
    - Private/Config/Initialize-AdmanConfig.ps1 restricts Test-AdmanConfigValid line 107 to type-only validation and lets the existing CONF-02 fail-closed scope gate throw the 'FAIL-CLOSED: managed-OU scope' message for whitespace-only ManagedOUs.
    - All three previously failing Pester tests pass in the Phase 5 unit suite.
  artifacts:
    - tests/Docs.Coverage.Tests.ps1
    - tests/Workflow.OffboardingState.Tests.ps1
    - Private/Config/Initialize-AdmanConfig.ps1
  key_links:
    - tests/Docs.Coverage.Tests.ps1 $common array -> Get-Command parameter enumeration on PS7
    - tests/Workflow.OffboardingState.Tests.ps1 archive record -> Get-AdmanOffboardingState SHA-256 self-hash verification
    - Test-AdmanConfigValid ManagedOUs validation -> Initialize-AdmanConfig CONF-02 fail-closed scope gate
---

<objective>
Close Phase 5 UAT gap G-05-1 by fixing three independent test/validation regressions: a PS7 common-parameter mismatch in the docs coverage test, a placeholder audit self-hash in the offboarding-state test, and a message-drift caused by WR-06 moving whitespace-only ManagedOUs rejection ahead of the CONF-02 fail-closed scope gate.

Purpose: Restore a green Phase 5 unit-test suite on both Windows PowerShell 5.1 and PowerShell 7.6 LTS without weakening any safety invariant.
Output: Three patched files and passing Pester verification for the affected tests.
</objective>

<execution_context>
@C:/Users/nhdinh/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-hardening-portability/05-UAT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@tests/Docs.Coverage.Tests.ps1
@tests/Workflow.OffboardingState.Tests.ps1
@Private/Config/Initialize-AdmanConfig.ps1
@tests/Config.Load.Tests.ps1
</context>

<tasks>

<task type="auto">
  <name>Fix tests/Docs.Coverage.Tests.ps1 PS7 common-parameter list</name>
  <files>tests/Docs.Coverage.Tests.ps1</files>
  <action>
    At line 186, add 'ProgressAction' to the $common string array so the docs/USAGE.md parameter-coverage test ignores this PowerShell 7 common parameter. The current array lists Verbose, Debug, ErrorAction, WarningAction, InformationAction, ErrorVariable, WarningVariable, InformationVariable, OutVariable, OutBuffer, PipelineVariable, WhatIf, Confirm. Append 'ProgressAction' to that list and to the same list in any other occurrence within the file (check for duplicates). Do not change the list order or any other test logic. This preserves the contract that docs/USAGE.md must document every non-common parameter while adapting to PS7's expanded common-parameter set.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Docs.Coverage.Tests.ps1 -Tag Unit -Output Normal; $LASTEXITCODE -eq 0. Also run under PowerShell 7.6 LTS (or use the CI dual-edition matrix from 05-02-PLAN.md) to confirm the ProgressAction regression is fixed there.</automated>
  </verify>
  <done>
    Docs.Coverage.Tests.ps1 passes under PowerShell 7.6 LTS and no longer reports missing '-ProgressAction' documentation.
  </done>
</task>

<task type="auto">
  <name>Fix tests/Workflow.OffboardingState.Tests.ps1 archived audit self-hash</name>
  <files>tests/Workflow.OffboardingState.Tests.ps1</files>
  <action>
    In the archive setup (around the $rec ordered hashtable and file write), ensure the record contains a valid SHA-256 self-hash in the 'hash' field before it is written. The 'prevHash' field should remain as the genesis sentinel ('0' * 64), which is correct for the first link of the audit chain. The test currently builds the canonical JSON, computes a SHA-256 hash, assigns it to $rec['hash'], and then serializes the record again with the hash included. Verify this matches the production pattern used by Write-AdmanAudit / Invoke-AdmanAuditRotation: the canonical JSON used for hashing must include all fields EXCEPT the hash field, then the hash is appended and the record is re-serialized. If the current code already computes the hash before the hash field exists (which is correct) and preserves the prevHash sentinel, then the task is a verification pass; otherwise, remove any placeholder self-hash (e.g., a literal '0' * 64 mistakenly written into the 'hash' field) and ensure the written record contains the real self-hash.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Workflow.OffboardingState.Tests.ps1 -Tag Unit -Output Normal; $LASTEXITCODE -eq 0. Also run under PowerShell 7.6 LTS (or use the CI dual-edition matrix from 05-02-PLAN.md) to confirm cross-edition behavior.</automated>
  </verify>
  <done>
    Workflow.OffboardingState.Tests.ps1 passes and Get-AdmanOffboardingState successfully verifies the archived record's SHA-256 self-hash.
  </done>
</task>

<task type="auto">
  <name>Restore CONF-02 fail-closed message for whitespace-only ManagedOUs</name>
  <files>Private/Config/Initialize-AdmanConfig.ps1</files>
  <action>
    In function Test-AdmanConfigValid, around line 107, change the ManagedOUs element check from type-and-whitespace validation to type-only validation. Specifically, change the condition from:
      if (-not ($ou -is [string]) -or [string]::IsNullOrWhiteSpace($ou))
    to:
      if (-not ($ou -is [string]))
    and ensure the accompanying throw message is exactly: "Config validation failed: every 'ManagedOUs' entry must be a string." Do not reject whitespace-only strings in this validator. Leave the existing CONF-02 fail-closed scope gate in Initialize-AdmanConfig (lines 366-372) unchanged: it counts non-whitespace ManagedOUs and throws the required 'FAIL-CLOSED: managed-OU scope...' message when the count is zero. This restores the test expectation in tests/Config.Load.Tests.ps1 that whitespace-only ManagedOUs triggers the FAIL-CLOSED scope gate, not the structural validator.
  </action>
  <verify>
    <automated>Invoke-Pester -Path tests/Config.Load.Tests.ps1 -Tag Unit -Output Normal; $LASTEXITCODE -eq 0. Also run under PowerShell 7.6 LTS (or use the CI dual-edition matrix from 05-02-PLAN.md) to confirm cross-edition behavior.</automated>
  </verify>
  <done>
    Config.Load.Tests.ps1 passes, and the whitespace-only ManagedOUs test again receives the 'FAIL-CLOSED: managed-OU scope' message from the CONF-02 scope gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code -> production code | Test fixes must not relax production safety invariants. |
| Config validator -> scope gate | Whitespace-only ManagedOUs must still be rejected, only by the CONF-02 scope gate instead of the type validator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04-01 | Tampering | tests/Workflow.OffboardingState.Tests.ps1 archive record | low | mitigate | Compute the SHA-256 self-hash using the same canonicalization rules as production so integrity verification cannot be bypassed by a placeholder. |
| T-05-04-02 | Denial of Service | Private/Config/Initialize-AdmanConfig.ps1 | low | mitigate | Keep the CONF-02 scope gate intact so whitespace-only ManagedOUs still throws FAIL-CLOSED; only move the rejection point back to the scope gate. |
| T-05-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no packages. |
</threat_model>

<verification>
- Invoke-Pester -Path tests/Docs.Coverage.Tests.ps1 -Tag Unit passes on PowerShell 7.6 LTS.
- Invoke-Pester -Path tests/Workflow.OffboardingState.Tests.ps1 -Tag Unit passes.
- Invoke-Pester -Path tests/Config.Load.Tests.ps1 -Tag Unit passes.
- The full Phase 5 unit suite is green on both Windows PowerShell 5.1 and PowerShell 7.6 LTS.
</verification>

<success_criteria>
- G-05-1 is closed: all three root-cause fixes are committed.
- tests/Docs.Coverage.Tests.ps1, tests/Workflow.OffboardingState.Tests.ps1, and tests/Config.Load.Tests.ps1 pass.
- No production safety invariant is weakened; whitespace-only ManagedOUs is still rejected by the CONF-02 fail-closed scope gate.
</success_criteria>

<output>
Create `.planning/phases/05-hardening-portability/05-04-SUMMARY.md` when done.
</output>

## Atomic commit guidance

Commit each fix as a separate, atomic change so bisect/rollback is straightforward:

1. `fix(05-gap): include ProgressAction in docs coverage common-parameter exclusion (G-05-1)`
2. `fix(05-gap): compute valid SHA-256 self-hash in offboarding archive test setup (G-05-1)`
3. `fix(05-gap): restore CONF-02 fail-closed scope gate for whitespace-only ManagedOUs (G-05-1)`
