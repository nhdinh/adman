---
phase: quick-260714-n4w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - A new reader can tell in <30 seconds what adman is, why it exists, and what its core safety guarantee is.
    - A new reader can install prerequisites (RSAT/ActiveDirectory module, PSFramework 1.14.457) and import the module without guessing.
    - A new reader can see the current repo layout (Public/, Private/, config/, rules/, tests/) and understand what is implemented today (Phase 0 foundation/safety harness only).
    - A new reader understands `.store/` is gitignored and must never be committed.
    - A contributor can find the lint/test entry points (PSScriptAnalyzerSettings.psd1, tests/PesterConfiguration.psd1).
  artifacts:
    - README.md at the repo root (C:\Users\nhdinh\dev\adman\README.md)
  key_links:
    - README prereq section names ActiveDirectory (RSAT) and PSFramework 1.14.457 as install-time requirements, never bundled.
    - README "What works today" section reflects Phase 0 only (safety spine + config/credential + audit); does NOT claim Phase 1+ features (TUI menu, reports, lifecycle verbs, remoting, bulk, workflows) exist yet.
    - README safety section names the five guardrails (WhatIf/dry-run, confirm, managed-OU scope, deny-list/protected accounts, audit log) consistent with PROJECT.md Core Value.
---

<objective>
Produce a single root-level `README.md` for the `adman` project that accurately reflects what is in the repo today (Phase 0 foundation/safety harness complete; Phases 1–5 not started). Audience is mixed-skill IT admins (users) plus future contributors (devs). Tone is terse and function-first, matching the project's developer profile.

Purpose: Phase 5 (DOC-01) will eventually own the full README, but the repo currently has no entry-point documentation at all. A minimal, honest README now prevents a new contributor from misreading the codebase as feature-complete and gives admins a safe install/usage path for the foundation that already exists.

Output: `C:\Users\nhdinh\dev\adman\README.md` (single file).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@./adman.psd1
@./.gitignore
@./config/adman.defaults.json
@./config/adman.example.json
@./PSScriptAnalyzerSettings.psd1
@./tests/PesterConfiguration.psd1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft README.md content covering all required sections</name>
  <files>README.md</files>
  <action>
Create `C:\Users\nhdinh\dev\adman\README.md` with the following sections, in this order. Keep prose terse and function-first (project tone). Do NOT invent features that are not in the repo.

1. **Title + one-line pitch.** `# adman` followed by a single sentence: "Menu-driven (interactive TUI) PowerShell toolkit for safely administering users and computers in an on-prem Active Directory domain." Add a status line directly under it: `> Status: Phase 0 (foundation & safety harness) complete. Phases 1–5 (TUI, reports, lifecycle verbs, remoting, bulk/workflows, hardening) are not yet implemented.`

2. **What it is.** 2–3 sentences paraphrasing PROJECT.md "What This Is": consolidates AD object lifecycle, reporting/inventory, remote computer ops, and provisioning/offboarding behind one guided interface with safety guardrails. Note the name `adman` is a working placeholder.

3. **Why it exists (core value).** Quote or paraphrase PROJECT.md Core Value verbatim-ish: "Any admin on the team can perform common AD user/computer tasks correctly and safely — every destructive action is previewed (`-WhatIf`/dry-run), confirmed, scoped to a managed OU, blocked from protected accounts, and written to an audit log."

4. **Safety guarantees.** Bulleted list of the five guardrails (this is the load-bearing section):
   - `-WhatIf` / dry-run on every destructive action (preview ≡ execute, enforced by Pester + PSScriptAnalyzer).
   - Confirmation prompts scaled to blast radius (y/n single; typed token + count for bulk).
   - Managed-OU scoping — refuses any DN outside configured roots.
   - Deny-list + protected-account protection (recursive admin-group membership, gMSA/service accounts, well-known SIDs).
   - Structured append-only audit log (JSON-lines, fail-closed — refuses the action if the record cannot be written).
   Add one line: "Enforced by the non-exported `Invoke-AdmanMutation` gate; no exported function calls AD write cmdlets directly (SAFE-08)."

5. **What works today.** Honest inventory of Phase 0 only. List:
   - Module scaffold (`adman.psd1` / `adman.psm1`) with explicit `FunctionsToExport` (SAFE-08 boundary).
   - Non-secret config: `Initialize-AdmanConfig`, `Get/Set/Export/Import-AdmanConfig`, fail-closed on empty managed-OU or failed deny-list load.
   - Credential decision: pass-through by default; opt-in DPAPI-encrypted credential file written only on explicit "remember me"; re-prompts on restore failure.
   - Startup capability probe: `Test-AdmanCapability` (RSAT present, domain reachable, current rights, transport availability).
   - Safety core: `Resolve-AdmanTarget`, `Test-AdmanTargetAllowed`, `Confirm-AdmanAction`, `Invoke-AdmanMutation` (private gate), `Assert-AdmanBulkPolicy` (cap placeholder).
   - Fail-closed audit: `Write-AdmanAudit` (write-ahead PENDING → mutate → OUTCOME; named mutex; JSON-lines; no secrets).
   - Test harness: Pester 6 + PSScriptAnalyzer 1.25.0 with custom SAFE-08 rule.
   Add: "Phases 1–5 (TUI menu, reports, lifecycle verbs, remoting, bulk, workflows, hardening) are planned but not implemented. See `.planning/ROADMAP.md`."

6. **Prerequisites.** Bulleted:
   - Windows PowerShell 5.1 (required baseline). PowerShell 7.6 LTS supported once the Phase 5 CI matrix passes; `CompatiblePSEditions` is currently `Desktop` only.
   - **ActiveDirectory module (RSAT)** — prerequisite, NOT bundled. Install on Windows 10 1809+/Win11 (Pro/Enterprise/Education) via Optional Features, or on Server via `Install-WindowsFeature RSAT-AD-PowerShell`.
   - **PSFramework 1.14.457** (exact-pinned via `RequiredVersion` in `adman.psd1`). Install with `Install-PSResource PSFramework -RequiredVersion 1.14.457 -Scope CurrentUser` (or `Install-Module PSFramework -RequiredVersion 1.14.457 -Scope CurrentUser` on legacy PowerShellGet).
   - Dev toolchain (contributors only): Pester 6.0.0, PSScriptAnalyzer 1.25.0.

7. **Install.** Numbered steps:
   1. Install RSAT/ActiveDirectory module (see Prerequisites).
   2. Install PSFramework 1.14.457 (see Prerequisites).
   3. Clone the repo.
   4. `Import-Module ./adman.psd1` from the repo root.
   Note: the module is not yet published to a feed; install-from-source is the only path today.

8. **Basic usage.** Minimal example:
   - `Initialize-Adman` — loads and validates config, runs capability probe, resolves protected SIDs / deny-list.
   - `Start-Adman` — entry point (currently a stub; full TUI lands in Phase 1).
   - `Test-AdmanCapability` — re-run the startup probe on demand.
   - `Get-AdmanConfig` / `Set-AdmanConfig` / `Export-AdmanConfig` / `Import-AdmanConfig` — manage the non-secret config.
   Note: no destructive verbs are exported yet; the gate is private and there are no public write wrappers in Phase 0.

9. **Configuration.** Explain the split:
   - Non-secret config: portable plain-JSON (`config/adman.defaults.json` ships defaults; `config/adman.example.json` is the annotated tracked example; `config/adman.schema.json` is the schema). Managed OU, deny-list, caps, paths — diff/backup friendly.
   - Credential file: separate, opt-in, DPAPI-encrypted (`Export-Clixml` CurrentUser scope), written only on explicit "remember me"; re-prompts on restore failure (0x8009000B / empty / non-PSCredential).
   - Both live in `.store/` at runtime. **`.store/` is gitignored — NEVER commit it.** (Cite `.gitignore`.)

10. **Project layout.** Tree-style listing of what is actually in the repo:
    ```
    adman.psd1                  # Module manifest (Desktop-only, PSFramework pinned)
    adman.psm1                  # Root module / loader
    Public/                     # Exported functions (Initialize-Adman, Start-Adman, Test-AdmanCapability, *-AdmanConfig)
    Private/                    # Internal implementation
      AD/                       # Adman.AD.Write.* wrappers (gate-only, 9-verb allow-list, no hard-delete)
      Audit/                    # Write-AdmanAudit, Find-AdmanAuditOrphans, AdmanAuditIO
      Config/                   # Initialize-AdmanConfig
      Foundation/               # Get-AdmanCredential, Test-AdmanAuditWritable, Resolve-AdmanDomainSid, etc.
      Safety/                   # Resolve-AdmanTarget, Test-AdmanTargetAllowed, Confirm-AdmanAction, Invoke-AdmanMutation, Assert-AdmanBulkPolicy, AdmanWriteVerbs, Escape-AdmanLdapFilterValue, Get-AdmanProtectedIdentity
    config/                     # adman.defaults.json, adman.example.json, adman.schema.json
    rules/                      # AdmanSafetyRules.psm1 (custom PSScriptAnalyzer SAFE-08 rule)
    tests/                      # Pester 6 tests (unit + integration; integration doubly gated by -Tag Integration + ADMAN_TEST_OU)
    PSScriptAnalyzerSettings.psd1
    .planning/                  # GSD planning artifacts (PROJECT.md, ROADMAP.md, STATE.md, phases/)
    .store/                     # GITIGNORED — runtime config + credential files; never commit
    ```

11. **Contributing / dev setup.** Bulleted:
    - Install dev toolchain: Pester 6.0.0, PSScriptAnalyzer 1.25.0 (CurrentUser scope).
    - Run lint: `Invoke-ScriptAnalyzer -Path . -Settings ./PSScriptAnalyzerSettings.psd1 -Recurse`.
    - Run unit tests: `Invoke-Pester -Configuration ./tests/PesterConfiguration.psd1`.
    - Run integration tests: requires a lab AD with a disposable test OU; set `$env:ADMAN_TEST_OU` and run with `-Tag Integration`. Integration tests are doubly gated and will skip without both.
    - Hard rules: no exported function may call AD write cmdlets directly (SAFE-08); every state-changing function must declare `SupportsShouldProcess` with `ConfirmImpact='High'`; `.store/` must never be committed; no secrets in repo or logs.
    - Planning artifacts live in `.planning/`; see `.planning/ROADMAP.md` for phase structure and `.planning/PROJECT.md` for decisions.

12. **License / notice.** State: "License: TBD — no LICENSE file has been added yet. Internal use only until one is chosen." Do NOT pick a license.

**Constraints while writing:**
- Do NOT claim Phase 1+ features exist. The TUI menu, reports, lifecycle verbs, remoting, bulk, and workflows are all planned but not implemented.
- Do NOT document `.store/` in a way that encourages committing it; explicitly call out that it is gitignored.
- Do NOT bundle or vendor the ActiveDirectory module; document it as a prerequisite.
- Do NOT pick a license; mark as TBD.
- Keep total length reasonable (~150–250 lines of Markdown). Terse, function-first.
- Use fenced code blocks for commands and the directory tree.
- No emojis.
  </action>
  <verify>
    <automated>test -f C:/Users/nhdinh/dev/adman/README.md && grep -c '^## ' C:/Users/nhdinh/dev/adman/README.md | awk '{exit ($1>=10?0:1)}' && grep -q 'Status: Phase 0' C:/Users/nhdinh/dev/adman/README.md && grep -q 'gitignored' C:/Users/nhdinh/dev/adman/README.md && grep -q 'License: TBD' C:/Users/nhdinh/dev/adman/README.md && grep -q 'PSFramework 1.14.457' C:/Users/nhdinh/dev/adman/README.md && grep -q 'ActiveDirectory' C:/Users/nhdinh/dev/adman/README.md && grep -q 'Invoke-AdmanMutation' C:/Users/nhdinh/dev/adman/README.md && grep -q 'WhatIf' C:/Users/nhdinh/dev/adman/README.md</automated>
  </verify>
  <done>
    - `README.md` exists at the repo root.
    - Contains all 12 required sections (title/pitch, what it is, why it exists, safety guarantees, what works today, prerequisites, install, basic usage, configuration, project layout, contributing/dev setup, license/notice).
    - Status line explicitly says Phase 0 complete / Phases 1–5 not implemented.
    - `.store/` is described as gitignored and never-to-be-committed.
    - ActiveDirectory module is documented as a prerequisite, not bundled.
    - PSFramework 1.14.457 is named with the exact pinned version.
    - License is marked TBD (no license chosen).
    - No emojis; tone is terse and function-first.
    - Automated verify command exits 0.
  </done>
</task>

</tasks>

<verification>
- `README.md` exists at `C:\Users\nhdinh\dev\adman\README.md`.
- All 12 required sections present.
- Content matches repo reality (Phase 0 only; no invented features).
- `.store/` gitignore constraint honored.
- License marked TBD.
</verification>

<success_criteria>
- A new contributor can read the README and correctly understand: what adman is, why it exists, what is implemented today (Phase 0), what is not yet implemented (Phases 1–5), how to install prerequisites, how to import the module, how to run lint/tests, and that `.store/` must never be committed.
- No false claims about feature completeness.
- No license chosen (TBD).
</success_criteria>

<output>
Create `.planning/quick/260714-n4w-generate-a-readme-md-for-the-project/260714-n4w-SUMMARY.md` when done.
</output>
